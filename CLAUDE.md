# Nexum — Brief técnico para la IA

Plataforma colombiana de transporte (base: Pamplona, N. de Santander): viajes urbanos
(taxi/moto/particular), envíos, mandados, pedidos a negocios e **intermunicipal** con
empresas de transporte habilitadas (requisito legal). Todo es **funcionalidad real**
(nada de demos); los seeds solo crean datos de prueba (conductor/usuario/negocios) y
NUNCA empresas — esas entran por `/empresa/registro` + verificación del admin.

**Responde siempre en español.** El usuario usa PowerShell: nunca le des comandos con
`&&`; dáselos en líneas separadas.

## Superficies y verificación

| Superficie | Stack | Cómo verificar (SIEMPRE antes de commit) |
|---|---|---|
| `backend/` | Node+Express+TS, Prisma 5.22, PostgreSQL+PostGIS, `ws`. Puerto 3000 | `cd backend` → `npm run typecheck` y `npm test` (vitest) |
| `app/` (web Next.js 16) | React 19, Tailwind. `/empresa` portal empresas, `/negocio` negocios. Deploy: Vercel | raíz → `node_modules/.bin/tsc --noEmit` y, si tocaste `app/`, `node_modules/.bin/next build` |
| `AppCliente/` | Flutter (Riverpod, Dio singleton `DioClient`, go_router, flutter_map/OSM) | **No hay Flutter local** → CI (`flutter analyze` + APK build) |
| `AppTransport/` | Flutter, paquete `nexum_driver` (misma base) | Igual: CI |
| Panel admin | HTML embebido en `backend/src/routes/admin.routes.ts` (`PANEL_HTML`), servido en `/admin` | typecheck del backend; el JS del panel es string (revisar escapes `\\'`) |

- **Rama de trabajo:** `claude/pr-blocked-duplicate-commits-tztum7`. Commit descriptivo en español y push al terminar cada tanda verificada.
- **CI (GitHub Actions):** workflows `CI` (backend typecheck+test, `flutter analyze` de ambas apps) + `Build Nexum Driver/Cliente APK`. Para consultarlos: `mcp__github__actions_list` filtrando por `head_sha` — la salida es enorme, se guarda en archivo; parséala con `python3 -c "import json..."`.
- **Prisma sin DB local:** las migraciones se generan offline: `prisma migrate diff --from-schema-datamodel <backup del schema> --to-schema-datamodel prisma/schema.prisma --script` → `prisma/migrations/YYYYMMDDHHMMSS_nombre/migration.sql`. La columna `Driver.geo` es `Unsupported("geography(Point, 4326)")` (PostGIS), gestionada a mano en la migración `add_postgis` — no la toques con Prisma.
- **Producción:** Render (render.yaml Blueprint + Dockerfile: `prisma migrate deploy && node dist/index.js`); la web en Vercel (`NEXT_PUBLIC_BACKEND_URL`).

## Config crítica

- Flutter `api_config`: `kDebugMode ? (kIsWeb ? localhost:3000 : 10.0.2.2:3000) : <Render>`.
- OTP en dev: código `123456` (cuando `NODE_ENV !== 'production'`).
- `ADMIN_PHONES` habilita el panel admin (OTP→JWT, `admin.middleware`).
- `INTERCITY_DUAL_MODEL` (default `'false'`): al activarlo, las rutas troncales SOLO se despachan a flotas INTERCITY/MIXED verificadas con `OperatorRoute.authorized=true`. Activar en Render cuando existan empresas verificadas con rutas autorizadas.
- `CHAIN_NEAR_DEST_M` (default `1500`): a qué distancia de su destino un conductor ocupado vuelve a ser despachable (viajes encadenados). `STUCK_ON_TRIP_MIN` (default `45`): minutos sin latido tras los que un conductor marcado «en viaje» se libera solo.
- `INTERCITY_SIMULATE` (default `'false'`): simulación de conductores intercity — no encender en prod.

## Dominio y flujos (lo esencial)

- **Matching urbano** (`backend/src/services/matching.service.ts`): PostGIS `ST_DWithin`, candidatos ONLINE+`isVerified`+frescura 120s, radio 5 km, oferta secuencial (5 candidatos, 15 s c/u) por WS `trip_request`; `onDriverAccept` es transaccional y **sella `Trip.operatorId`** con la empresa del conductor. Ciclo paralelo para mandados (errands). Si se agotan candidatos → `registerOnNoDrivers` avisa al pasajero (CANCELLED, `NO_DRIVERS_AVAILABLE`).
- **Intermunicipal** (`backend/src/services/intercity.service.ts`): reservas `IntercityBooking` (ciudades enum: PAMPLONA/CUCUTA/BUCARAMANGA/CHITAGA/MALAGA/OCANA/BOGOTA), matching radio 25 km, frescura 600 s, requiere `Driver.intercityEnabled`; con DUAL_MODEL las troncales exigen flota habilitada (filtro `EXISTS` sobre `operators`+`operator_routes`).
- **Empresas** (`operator.service.ts`, `operator.routes.ts`, `operator-auth.middleware.ts`): registro público → `PENDING` → admin verifica (`ACTIVE`+`isVerified`) → afilia conductores por teléfono (se normaliza a E.164 `+57XXXXXXXXXX` con `normalizeColombianPhone` — el login OTP casa por match exacto; si la empresa es INTERCITY/MIXED se activa `intercityEnabled` automáticamente) → el conductor ve "Conduces para X" (affiliation en `driver-profile.service.ts`) → viajes sellados → portal ve `/operator/trips` + CSV `/operator/trips/export.csv` → declara rutas (`/operator/routes`) → admin autoriza (`/admin/routes/:id/authorize`).
- **Liquidación del viaje real**: `updateClientTripStatus('completed')` en `client.service.ts` calcula con `lib/fare.ts` (`calcFare` compartido), persiste `finalFare/netEarning/commission`, llama `recordCompletedTrip` (alimenta `DriverEarning` → wallet + `/earnings/*`) y libera al conductor a ONLINE. Cancelación del cliente → `cancelClientTrip` avisa al conductor por WS `trip_cancelled` y lo libera.
- **Inyección WS**: los servicios NO importan sockets; `ws.handler.ts` registra callbacks al arrancar (`registerSendToDriver`, `registerNotifyTripUpdate`, `registerOnNoDrivers`, `registerClientSendToDriver`, `registerIntercitySendToDriver`). Sigue ese patrón para nuevos avisos (evita imports circulares).

## Convenciones de código

- Flutter web-safe: `MultipartFile.fromBytes` (nunca `fromFile`), widget `PickedImage` para previews (Image.network en web), heartbeat geo con fallback centro Pamplona `(7.3754, -72.6486)` cuando no hay GPS.
- Web `/empresa`: cliente HTTP compartido `app/empresa/api.ts` (`createOperatorApi`), componentes separados (`FleetMap.tsx` Leaflet vía CDN sin dependencia npm, `RoutesManager.tsx`). Sin `any`; tipado estricto.
- Cualquier simulación/mock nuevo debe ser **no-op en release** (`if (!kDebugMode) return;`) — patrón ya aplicado en `intercity_provider.dart`.
- Backend: rutas validan body a mano y responden `{ success, data | error }`; errores de negocio con mensajes en español.

## HECHO y verificado (no re-auditar)

Ciclo empresa completo (registro→verificación→afiliación→sellado→liquidación+CSV+rutas troncales+matching restringido), **onboarding del portal** (afiliar conductores + registrar vehículos con estado de verificación: `DriversManager.tsx`/`VehiclesManager.tsx`), mapa Leaflet del portal, liquidación real de viajes urbanos, cancelación coherente cliente↔conductor, aviso "sin conductores", estado `in_progress` en el cliente, afiliación E.164, `intercityEnabled` automático, ganancias reales (`/earnings/history` → `trip_history_provider`), subida de documentos web-safe, panel admin (docs/conductores/empresas/rutas/SOS/promos/payouts), **ciclo intermunicipal completo** (WS `intercity_start`/`intercity_complete` → CONFIRMED→IN_PROGRESS→COMPLETED con `finalFare`/`completedAt` + liquidación al conductor vía `recordCompletedTrip`; avisos al conductor en confirm/reject/cancel del cliente; UI de viaje activo con fases en `intercity_requests_screen.dart`; feedback `intercity_accept_ok`/error en `ws_service.dart`, que además ya no parsea `trip_request` muerto).

También HECHO (Tanda 3): `IntercityBooking.operatorId` sellado al aceptar (migración `20260707000000_add_intercity_operator`) y fusionado en `/operator/trips` + CSV (urbano + intermunicipal); historial del conductor sin viajes sintéticos (`trip_summary_screen` refetchea vía `tripHistoryProvider.refresh()` — la liquidación es 100 % del backend); estado `arriving` enviado al iniciar navegación al pickup (`active_trip_screen`, guardado por `isToPickup`); eliminados `acceptClientTrip`/`_startTripSimulation` (demo muerto).

También HECHO (Tanda 4 — liquidación en vivo + de-mock total de release): `trip_status_ack` devuelve `settlement {finalFare,netEarning,commission}` al completar (`getTripSettlement` en client.service; parseado en `driver_ws_service.dart` con buffer por tripId) y `trip_summary_screen` muestra los montos del backend con badge "Liquidación confirmada" (fallback 6 s a estimación; `TripModel.id` ahora es el id real del backend); `ClientTripDTO.finalFare` expuesto. Fixes WS multi-conductor: `trip_status`/`driver_mode` ya no exigen el socket singleton; presencia condicional `noteDriverConnected/Disconnected` (OFFLINE↔ONLINE sin pisar ON_TRIP; antes `onClose` leía `driverIdByWs` tras borrarlo y nunca marcaba offline). De-mock: conductor — home/drawer/ajustes/perfil con identidad real (`driverProfileProvider`), rendimiento y calificaciones 100 % reales (perfil + historial; distribución calculada; comentarios = estado vacío honesto), tickets de soporte = estado vacío → chat, historial sin `_buildSeedTrips` (purga `seed-`), portal/registro de negocio del app solo `kDebugMode` ("(demo)"), "Mis documentos" → `/verification` (real), 401 → login (`AuthInterceptor.onSessionExpired` registrado en `app.dart`); cliente — sin `_mockDrivers`/`_simulateLifecycle`/`_buildMockHistory`/`_seedVehicles`: solicitudes fallan con error honesto si el POST no llega (booking/checkout/mandado muestran snackbar), fallback sin WS = **polling REST 5 s** (`GET /client/trips/:id` nuevo, `/client/orders/:id`, `/client/errands/:id`), mapa del home con conductores reales (`GET /client/drivers/nearby` → `getNearbyDriverPositions` PostGIS, refresh 15 s), negocios sin fallback mock (error → `_buildError`), llamada real `tel:` al repartidor (chat demo eliminado).

También HECHO (Tanda 5 — mandados a la empresa + purga legacy + tokens intercity): `Errand.operatorId` sellado al aceptar (migración `20260708000000_add_errand_operator`; `acceptClientErrand` consulta la empresa del conductor) y fusionado en `/operator/trips` + CSV como servicio `MANDADO` con estados normalizados (`_errandStatusForPortal`: DELIVERED→COMPLETED, SHOPPING/ON_THE_WAY→IN_PROGRESS) y `fare = serviceFee` — **Order NO se sella**: los pedidos aún no asignan conductor en el backend (no hay punto de accept). Eliminados del app conductor: `features/business_portal` y `features/documents` completos (el canal real de negocios es la web `/negocio`; documentos van por `/verification`), capas `data/`, `presentation/`, `domain/usecases`, `domain/repositories` de `trip_requests` (solo sobreviven `domain/entities`, usadas por home y active_trip) y `core/mock_data/{passengers,trips,errands}_mock.dart` (`driver_mock.dart` queda: seed del perfil editable + auth mock web). Pantallas intercity del cliente 100 % tokenizadas (bloque `AppColors.intercity*` — tema nocturno slate — y `starText`; 96 hex fuera) e `intercityBrand` compartido en el conductor; sonido real de notificación (`NotificationService.playTripRequestSound` → `AudioService` + `assets/sounds/trip_request.wav`).

También HECHO (Tanda 6 — despacho REAL de pedidos a repartidores): eliminada la simulación server-side de pedidos (`MOCK_DRIVERS`/`_startSimulation` en client.service — el último teatro de la plataforma); `startOrderMatchingCycle` en matching.service (oferta secuencial `order_request` anclada a coords del negocio, fallback Pamplona; `getOrderOfferInfo` vive en `order-offer.service.ts` para evitar el ciclo client↔matching) disparado por POST /client/orders; WS `accept_order`/`reject_order`/`order_status` (at_pickup|in_transit|delivered) en ws.handler; `acceptClientOrder` sella driverId+identidad+**operatorId** (migración `20260709000000_add_order_dispatch`) y `updateOrderStatusByDriver` liquida el `deliveryFee` (menos comisión) vía `recordCompletedTrip` y libera al conductor; fusión `PEDIDO` en `/operator/trips` + CSV (`_orderStatusForPortal`); app conductor: `orderRequests` stream + `sendAcceptOrder/sendRejectOrder/sendOrderStatus` en driver_ws_service, `TripRequestEntity.orderId`/`isOrder`, oferta mapeada en home (`_orderRequestFromMap`) y ramas de pedido en accept/reject/arrived/pickup/finish del viaje activo (at_pickup→in_transit→delivered); guard del `arriving` para no mandar trip_status a pedidos/mandados.

También HECHO (Tanda 7 — pedidos v2 + barrido de color): oferta `order_request` con coords reales (`businessLat/Lng` + `deliveryLat/Lng` en `getOrderOfferInfo`/`OrderRequestDTO`), mapeadas en `_orderRequestFromMap` del conductor (origen=negocio, destino=entrega; fallback Pamplona solo si faltan); **cancelación de pedido por el cliente** (`POST /client/orders/:id/cancel` → `cancelClientOrder`, permitido hasta AT_PICKUP) que emite WS `order_cancelled` al repartidor asignado y lo libera (ONLINE) — el cliente lo dispara en `orders_provider.cancelOrder` (`_cancelOnServer`), y el conductor lo maneja en home (retira la oferta pendiente) y en `active_trip_screen` (`orderCancellations` → snackbar + `_handleCancelled`). Barrido de color de `transport_home_screen` del cliente: 25 `Color(0x…)` inline → `AppColors` (familia dark + `intercityBrand/intercityBrandDark/intercityAccent` + `liveGreen/liveGreenBright` nuevos); solo queda `0x55000000` (sombra).

También HECHO (Tanda 8 — producción: seguridad OTP, diagnóstico remoto, firma APK, marca neutral): **admin nunca acepta el código piloto en producción** (`requestAdminOtp`/`validateAdminOtp` en otp.service validan directo contra `OTP_FALLBACK_CODE` — sin sesión en BD, inmune a sembrar código desde el login de conductor; sin fallback ⇒ 503 "cerrado" con instrucción exacta; comparación timing-safe) + límite de intentos de verificación por teléfono (8/15 min, `_assertVerifyLimit`) en TODAS las superficies + mensajes OTP en español; **`/health` diagnóstico**: `commit` (RENDER_GIT_COMMIT), `otp` y `otpAdmin` (modos en español: twilio-sms/codigo-fijo-propio/piloto-123456/cerrado) y el login del panel muestra ese pie (`#diag`) — una captura del login dice qué build corre Render y qué código espera; portal `/empresa` y panel con `autoComplete="one-time-code"` (fix "no deja escribir": autofill de Chrome llenaba el maxLength); panel admin rebrand esmeralda/slate (antes azul default); **firma Android estable**: ambos `build.gradle.kts` leen `android/key.properties`, workflows APK escriben keystore desde secrets `ANDROID_KEYSTORE_BASE64`/`ANDROID_KEYSTORE_PASSWORD` (alias `nexum`; sin secrets → debug) + `--build-number=github.run_number` (versionCode creciente → las actualizaciones instalan); marca neutral sin "Pamplona" en textos de cara al usuario (landing/metadata web, splash/onboarding/perfil/banners de ambas apps; enums de ciudades intercity intactos) y geofence de Pamplona eliminado (`go_online_usecase` ya no valida bounds); portales con fallback a backend real en build de producción (nunca localhost).

También HECHO (Tanda 8.1 — arranque del piloto + base iOS): **login del portal /empresa sin callejón sin salida** (PR #24: `send-otp` crea sesión SIEMPRE en modo local — con Twilio solo a miembros, anti SMS-pumping; `verify-otp` valida el código PRIMERO y luego responde 403 instructivo "Este teléfono no está asociado a ninguna empresa…" — sin enumeración; E.164 normalizado también al registrar empresa y al buscar miembros); límite de envío OTP 5→10/hora (el operador se auto-bloqueaba probando; el contador es en memoria: un redeploy lo resetea); **base iOS COMPILANDO** (PR #25+#26): workflow manual `Build iOS (sin firma)` en macOS (`--no-codesign`, artifacts `nexum-{cliente,driver}-ipa-unsigned`, run #2 completamente verde), Info.plist de ambas apps con permisos (ubicación/cámara/fotos; background para conductor), íconos/splash con `ios: true` + `remove_alpha_ios`, y `AppDelegate` del conductor sin el init muerto de GoogleMaps (la app usa flutter_map/OSM). Para instalar en iPhone real: cuenta Apple Developer (US$99/año) + firma + TestFlight; mientras tanto, la app web de GitHub Pages funciona en Safari iOS.

También HECHO (Tanda 9 — intermunicipal protagonista + radiografía del despacho): conductor con tarjeta **Intermunicipal fija en el panel del home** (switch de disponibilidad → GET/PUT `/driver/intercity/availability` vía `intercityDriverProvider` con `loadAvailability()` al abrir; badge de pendientes; tap → `/intercity-requests`); cliente con el hero intermunicipal como **primera tarjeta** del sheet (antes 4º bloque bajo el pliegue — causa del "no da opción de reservar"); **`GET /admin/matching/diagnose?lat&lng`** (`diagnoseMatching` en admin.service) evalúa por conductor los 4 filtros del matching urbano (ONLINE, verificado, GPS ≤120 s, radio ≤5 km) con distancia/edad GPS y veredicto — tabla en panel admin pestaña Conductores (form lat/lng + `runDiag`); `/admin/drivers` expone `intercityEnabled`. Regla de diagnóstico: "las apps no interactúan" = leer esa tabla, no adivinar (culpable típico: cliente y conductor en ciudades distintas — radio 5 km).

También HECHO (Tanda 10·B1 — barra de vidrio): **píldora de navegación flotante translúcida con blur** (referencia Rappi/Instagram) en ambas apps. Cliente: `HomeShell` reemplaza la NavigationBar M3 por `_GlassNavBar` (ClipRRect+BackdropFilter blur 22, blanco .72, sombra fuera del clip, badges `Badge.count`, háptica) con `extendBody`; el ListView del sheet de movilidad ganó `padding.bottom: 84`. Conductor: variante oscura (.66) como overlay del home — Inicio activo + push a Ganancias/Intermunicipal/Perfil — que se OCULTA con oferta en pantalla (`pendingRequest != null`); el panel inferior compensa solo vía `MediaQuery.padding.bottom`. El conductor NO tiene shell de pestañas: refinamiento futuro = StatefulShellRoute con estado por pestaña + ocultar/mostrar con el scroll.

También HECHO (Tanda 10·A5 — horarios de empresa): **salidas programadas intermunicipales publicadas por la EMPRESA** sobre el motor pooled existente. `PooledTrip.operatorId` (migración `20260710200000_add_pooled_operator`, patrón String?+índice); `publishPooledTrip` acepta `opts {operatorId, licensedOperator}` — empresa verificada ⇒ las rutas troncales del DUAL_MODEL SÍ pasan; `getOperatorPooledTrips`/`cancelPooledTripByOperator`; rutas `GET /operator/pool`, `POST /operator/pool/publish` (valida conductor AFILIADO, vehículo activo como descripción por defecto, sella operatorId) y `POST /operator/pool/:id/cancel` (roles OWNER/DISPATCHER); portal `/empresa` con **SchedulesManager** (form conductor/ciudades lowercase/datetime/puestos/tarifa + lista con estados y cancelar) montado junto a RoutesManager; búsqueda del cliente devuelve `operatorName` (batch a operators, tradeName??legalName) y la tarjeta de Cupos muestra el sello "Empresa: X"; de-mock: el publish del conductor usa su identidad real (antes `MOCK_DRIVER`). OJO: `INTERCITY_ROUTES` es Partial — pares sin definir rechazan publicación con mensaje claro.

También HECHO (Tanda 11 — servicios unificados + de-mock final del despacho): el backend **ya despachaba TODOS los tipos** (viaje/mandado/envío/pedido) a cualquier conductor ONLINE cercano — `matching.service` no filtra por modo y los 3 streams del home del conductor (`tripRequests`/`errandRequests`/`orderRequests`) entran sin filtro. Eliminado el ÚLTIMO teatro del servidor: `dispatch.service.ts` (simulador de `MOCK_ERRANDS` que era **código muerto** — `startDispatch` no se llamaba, `isOnline` siempre false) + `MOCK_ERRANDS` de constants (se mantiene `ERRAND_SERVICE_FEE`, usado por el flujo real) + `setDriverWorkMode` del ws.handler (el modo ahora es solo informativo). UI del conductor reframeada para hacer visible la unificación: "En línea · Recibes viajes, envíos, mandados y pedidos"; el selector de modo pasó de "¿Qué quieres hacer?" (parecía filtro excluyente) a "Tu enfoque principal · En línea recibes de todo — tú decides cada solicitud". Verificado: backend typecheck + 9 tests. PENDIENTE real de la unificación (opcional): preferencias que FILTREN (hoy el conductor recibe todo y decide; si se quiere que una moto no reciba pedidos de mercado grande, agregar toggles que viajen al backend y filtren candidatos).

También HECHO (Tanda 12 — B2 home conductor full DiDi + preferencias REALES): `Driver.acceptsTrips/acceptsErrands/acceptsOrders` (migración `20260711000000_add_driver_service_prefs`, default true); `findNearestAvailableDrivers` gana param `serviceKind` (`'trip'|'errand'|'order'`) y filtra con el patrón `(kind != X OR d."acceptsX")` — los 3 ciclos (viaje/mandado/pedido) pasan su tipo; `GET/PUT /driver/service-prefs` (PUT acepta también `intercity` → intercityEnabled). App conductor: top bar DiDi (menú · **`_EarningsPill`** negra con ganancias del día al centro → /earnings · capas · campana; fuera `_OnlineToggle` y atajos redundantes — perfil vive en la glass bar); **botón grande Conectarse/Desconectarse** al pie del panel (verde/slate); fila "Preferencias de servicio" con resumen dinámico (`_servicePrefsSummary`) que abre **`_ServicePrefsSheet`** (4 SwitchListTile: viajes/envíos/pedidos vía `servicePrefsProvider` nuevo + intermunicipal vía `intercityDriverProvider` — una sola fuente de verdad por switch, optimista con rollback). El marcador del vehículo (Tanda C1: `VehicleMarker` rota al rumbo con `bearingBetween`, variante moto) y la unificación (Tanda 11) ya estaban en verde.

También HECHO (Tanda 13 — B3 selector de servicios del cliente): `_ServiceTab` rediseñado en transport_home_screen — Stateful con **AnimatedScale 0.95 al presionar + háptica**, tarjetas 110→122 px (fila 114→124), chip circular de ícono, **gradiente del color del servicio al seleccionar** (`Color.lerp` a negro 0.28) + sombra rica, check de selección, y badge "conductores cerca" como píldora con punto vivo (`liveGreen/liveGreenBright`) en vez de circulito. Producción confirmada al día por el usuario (ambos servicios Render Live con 89f1d09; migración service-prefs aplicada).

También HECHO (Tanda 14 — conectar centrado + 2 servicios + anti-confusión): videos del usuario confirmaron apps al día y revelaron el bug: **el botón Conectarse de la Tanda 12 quedaba oculto TRAS la glass bar** (el `MediaQuery` del context del State — sobre el Scaffold — no incluye el inset del bottomNavigationBar con extendBody). Fix definitivo: **Conectar/Desconectar es el CENTRO prominente de la glass bar** (círculo 52 flotante FUERA del ClipRRect — un Stack clipBehavior.none envuelve la píldora; verde/slate según estado, háptica media) y el ítem Ganancias salió de la barra (vive en la píldora superior); el panel compensa con `bottom: +92` fijo. **Dos servicios, no cuatro**: `_ServiceToggleCard` Pasajeros(trips) y Encargos(errands+orders JUNTOS — para el conductor es una sola decisión) reemplazan el `_WorkModeSelector` estático de 4 chips (clase y `_selectWorkMode` eliminados); `_ServicePrefsSheet` a 3 switches (Pasajeros/Encargos/Intermunicipal) y resumen acorde. **`kBuildTag`** (`--dart-define=BUILD_TAG=run_number` en build-apk.yml y deploy-web.yml) visible en el drawer: "v1.0.0 · build N" — se acabó adivinar qué versión corre el teléfono. Backend: `GET /` raíz amigable (service/status/salud/panel) — el "Route not found" parecía servicio caído. Web `/negocio/[token]` 404: mensaje que explica que los seeds no existen en producción + botón "Registrar mi negocio" → /negocio/registro.

También HECHO (Tanda 15 — barra simple + limpiezas pedidas por el usuario): **Conectar/Desconectar INTEGRADO en la glass bar** (círculo 34 dentro de la píldora — el flotante de la Tanda 14 "quedó muy mal ubicado"; barra final: Inicio · Conectar · Perfil, ítem Intermunicipal ELIMINADO — su acceso vive en la tarjeta del panel); **"máximo legal" fuera de la UI** de publicar cupos (sin getter/validación/errorText/banner — ahora muestra "Sugerido: $X / puesto" + km/min; la validación del backend se apaga con `INTERCITY_REMOVE_CAP=true` en Render); **back restaurado** en Ajustes→Mi perfil (`context.go`→`push` en settings_screen:220 — `go` borra el historial); **modo oscuro DESHABILITADO temporalmente** en ambas apps (tema dark a medias = textos ilegibles): `ThemeNotifier` fijo en claro (sin SharedPreferences), switches "Próximamente" deshabilitados en ajustes/cuenta, locals+imports huérfanos limpiados. Rehabilitar = restaurar persistencia del historial git + pase de contraste completo.

También HECHO (Tanda 16 — registro real de negocios): **`/negocio/registro` dejó de ser el callejón sin salida** ("El registro lo hace el repartidor" + demo Sabor Pampero — flujo eliminado en Tanda 5, seed inexistente en prod): ahora es formulario de autoservicio (nombre/dueño/teléfono/WhatsApp opcional/dirección/categoría lowercase) → `POST /business/register`, y al éxito muestra el **enlace único del portal** con Copiar/Abrir + advertencia ámbar de guardarlo. Backend: esa ruta pasó de `authMiddleware` (JWT de conductor — inalcanzable desde la web) a **pública con `authLimiter`**; `PORTAL_BASE_URL` default corregido `https://nexum.app` (muerto) → `https://nexum-store.onrender.com`. Tanda 15 CI verde total (APK Driver run#206 = build 206, Cliente run#104).

También HECHO (Tanda 17 — fotos de perfil reales en ambas apps): backend `POST /driver/profile/photo` y **perfil del cliente completo** (`GET/PUT /client/profile` + `POST /client/profile/photo` — antes el User no tenía endpoints de perfil; PUT valida email único con mensaje en español) sobre `avatarUrl` que ya existía en ambos modelos (sin migración); multipart reutiliza `documentUpload`/`fileToUrl` con guard `image/*`. `ApiConfig.resolveUrl` en ambas apps (URLs `/uploads/...` relativas → absolutas; S3 pasa intacto). Conductor: `EditableProfile.photoUrl` + `uploadPhoto` (bytes, web-safe) en editable_profile_provider, avatar del perfil con `foregroundImage` + cámara que ABRE GALERÍA y sube (antes "próxima versión"), drawer del home con foto (fallback iniciales con errorBuilder), y `updateIdentity` ahora persiste `fullName` vía PATCH. Cliente: `image_picker` agregado al pubspec, `clientProfileProvider` nuevo (load/uploadPhoto/updateName), header de cuenta con foto + cámara + **lápiz para editar nombre** (dialog → PUT). OJO producción: sin `S3_BUCKET` los archivos van al disco EFÍMERO de Render (se pierden en redeploy) — configurar S3/R2 para fotos permanentes. **Además fix "no permite iniciar el viaje" (compartidos)**: `pooled_driver_provider._action` tragaba TODO error de depart/complete/cancel (tocar "Iniciar" rechazado = silencio absoluto); ahora las 3 acciones devuelven el error y `my_pooled_trips_screen._run` muestra snackbar de éxito/motivo; mensajes de esas rutas del backend en español.

También HECHO (Tanda 18 — fix crash del portal de negocios): la captura del usuario mostró `/negocio/[token]` reventando con `Cannot read properties of undefined (reading 'name')`. Causa: `GET /business/:token/orders` respondía `{orders,stats}` **sin** `business`, pero la página hace `const {business}=data` y pinta `business.name`. Fix: backend incluye `business:{name,token}` en `/orders` (ya consultaba el negocio para su id); frontend usa `data.business?.name` con fallback 'Mi negocio' y `ApiResponse.business` opcional. El WS `business_auth` ya estaba bien en `ws.handler.ts` (el error de consola era transitorio; reconecta cada 5 s). OJO: el fix del backend por sí solo mata el crash aunque el portal siga en build viejo — basta redeployar `nexum-api`.

También HECHO (Tanda 19 — catálogo del negocio con fotos): raíz de "restaurantes poco interactivos" = no había forma de crear productos (menús vacíos). Ahora el dueño arma su catálogo desde **`/negocio/[token]/catalogo`** (nueva página): alta de producto (nombre/precio/categoría/descripción) + foto por producto + toggle disponible/agotado + eliminar. Backend: `ProductDTO`+`imageUrl`, mapper compartido `_productToDTO`, `getManagedProductsForBusiness` (incluye no disponibles), `createBusinessProduct`/`updateBusinessProduct`/`deleteBusinessProduct` (validan pertenencia al negocio), rutas token-auth `GET/POST /business/:token/products`, `PATCH/DELETE /:productId`, `POST /:productId/photo` (multipart, reutiliza `documentUpload`/`fileToUrl`). Cliente: `ProductEntity`+`imageUrl` (parser real datasource), `ProductTile` con miniatura 64px (`Image.network(ApiConfig.resolveUrl)` + fallback `restaurant_menu` + loading). Link "Catálogo" en el header del portal. Verificado: backend typecheck+9 tests, portal `next build` (ruta `/negocio/[token]/catalogo`). PENDIENTE opcional: editar nombre/precio inline (hoy = eliminar y recrear), foto de portada del local (`Business` no tiene campo de imagen), y que el cliente pinte foto de portada del negocio en la lista.

También HECHO (Tanda 20 — panel del conductor arrastrable): el panel principal del home del conductor era un `Positioned` fijo al fondo (estático). Ahora es un **`DraggableScrollableSheet`** (min .30 / inicial .52 / max .92, con `snap`) cuyo contenido vive en un `ListView(controller: scrollController)` — mismo patrón que el sheet de movilidad del cliente (`transport_home_screen`): colapsado deja ver el mapa, expandido muestra saludo/meta, servicios, stats, intermunicipal y prefs. El asa quedó centrada; Conectar sigue en la barra de vidrio. `_buildBottomPanel` ahora recibe `(ServiceType, ScrollController)`.

También HECHO (Tanda 21 — foto de portada del negocio): `Business.imageUrl` (migración `20260712000000_add_business_cover`, patrón `String?`) + `BusinessPublicDTO.imageUrl` + interfaz `Business.imageUrl` (en `_dbToBusinessInterface`, mappers `getAllBusinessesPublic`/`getBusinessPublicById`, y expuesto en `/orders` e `/info`). Backend: `updateBusinessCover` + `POST /business/:token/cover` (multipart). Portal: `CoverSection` en `/negocio/[token]/catalogo` (carga la portada actual vía `/info`, sube/cambia con preview). Cliente: `BusinessEntity.imageUrl` (parser), `BusinessCard` con `_CoverHeader` (foto 108px o fallback gradiente+ícono de categoría), y header del detalle (`SliverAppBar`) con la foto de fondo + scrim + `_InfoRow` en píldora clara. Verificado: backend typecheck+9 tests, portal `next build`, balance Dart OK.

También HECHO (Tanda 22 — entrada de empresas de transporte en la landing): el usuario reportó que "al registrar solo da opciones de comercio". Causa: la landing (`app/page.tsx`) tenía UN solo CTA "Registra tu negocio" → `/negocio/registro` (categorías de comercio); el portal de empresas de transporte (`/empresa/registro`, que EXISTE y postea a `/operator/register` — razón social/NIT/tipo TAXI·INTERCITY·MIXED) era **inalcanzable desde el inicio**. Fix: la landing ahora tiene DOS CTAs — "Registra tu negocio" (→ /negocio/registro, comercio) y "Empresa de transporte" (→ /empresa/registro, flota) + link "Ingreso de empresas" → /empresa. El login `/empresa` ya enlazaba a `/empresa/registro` ("Regístrala aquí"); el backend de operadores estaba intacto (era solo descubrimiento en la web).

También HECHO (Tanda 23 — E2E de empresa PROBADO + normalización de teléfono en login): se levantó Postgres+PostGIS local y se recorrió el ciclo completo con peticiones reales: registro empresa (PENDING) → **admin verifica** (ACTIVE+isVerified) → login empresa (OWNER) → **afiliar conductor** → el conductor ve "Conduces para Trans Norte Limpio (ACTIVE, verified, AFFILIATED)" + `intercityEnabled` auto-true por ser INTERCITY. **Todo el ciclo sano.** Bug encontrado y corregido: `auth.service` (`sendOtp`/`verifyOtp`/`registerDriver`) y `client.service` (`sendClientOtp`/`verifyClientOtp`) buscaban/creaban por teléfono **crudo**, mientras `affiliateDriver` y el login del portal normalizan a E.164. La ruta `/auth/send-otp` valida con `isValidColombianPhone` (que **quita espacios**), así que un `+57 300 222 3344` PASA validación pero antes no casaba con el conductor guardado (`+573002223344`) → cuenta fantasma sin afiliación. Fix: `normalizeColombianPhone` en esos 5 puntos (idempotente para el `+57` que ya manda la app; la app NO manda espacios, así que era defensivo pero real para otros formatos). Verificado en vivo: login con espacios ahora SÍ ve la empresa, sin conductores duplicados. Backend typecheck+9 tests OK.

También HECHO (Tanda 24 — Fase A del modelo de CARGA): fundamento para logística de carga/flota (mulas, turbos, acarreos). Schema (migración `20260712120000_add_cargo_fleet`): `VehicleType` += `TURBO`/`CAMION`/`MULA`, `OperatorType` += `CARGA`, `Vehicle.capacityKg Int?` (capacidad de carga). Backend: `OPERATOR_TYPES`/`VEHICLE_TYPES` (operator.routes) incluyen los nuevos valores, `CreateVehicleDTO.capacityKg` + `createOperatorVehicle` lo persiste, POST `/operator/vehicles` lo acepta, `registerDriver` vehicleTypeMap += turbo/camion/mula. Portal: `/empresa/registro` con botón **Carga** (`OPERATOR_TYPE_LABEL`, grid 2×2), `VehiclesManager` con tipos pesados + campo **Capacidad de carga (kg)** visible solo para carga (`CARGO_TYPES`) y mostrado en la lista. **Verificado E2E real** (Postgres local): empresa CARGA registrada → verificada → afilia conductor → registra MULA Kenworth T800 con 32.000 kg → aparece en la flota. Backend typecheck+9 tests, portal next build. **Roadmap de carga (siguiente):** Fase B = solicitud de flete del cliente (peso/tipo de camión, urbano/intermunicipal) + matching por camión + comisión; Fase C = panel financiero/trayectos de la empresa (control total); Fase D = acarreos urbanos programados.

También HECHO (Tanda 25 — Fases B+C+D del modelo de CARGA, E2E COMPLETO): **el modelo de ingresos de carga opera de punta a punta** (probado real: comisión $37.500 sobre flete de $250.000). (1) **Personas naturales con camiones**: `Operator.kind EMPRESA|PERSONA` (migración `20260713000000_add_freight`) — `/empresa/registro` pregunta "¿Quién registra?" y para PERSONA los campos son Nombre completo + Cédula (mismos legalName/nit). (2) **Fase B — fletes marketplace** (`freight.service.ts`): `FreightRequest` (origen/destino/ciudades, descripción, peso kg, tipo TURBO|CAMION|MULA, precio ofrecido, `scheduledFor` opcional = programado/acarreo, Fase D incluida); cliente: `POST /client/freight/request`, `GET /client/freight`, `POST /:id/cancel`; flota: `GET /operator/freight/available` (filtra por los tipos de camión de SU flota), `POST /:id/accept {driverId,vehicleId}` (valida afiliación, tipo y `capacityKg`; toma atómica con updateMany status-guard), `POST /:id/status` (`in_progress`→`completed` liquida con `COMMISSION_RATE` + `recordCompletedTrip`; `cancelled` devuelve al tablero). (3) **Fase C — panel financiero** `GET /operator/finance/summary?from&to` (`getFleetFinance`): consolida VIAJE+INTERMUNICIPAL+MANDADO+PEDIDO+FLETE → bruto/comisión/neto + por servicio/conductor/vehículo. Portal: **FreightManager** (tablero ámbar, selects conductor/vehículo, Tomar/Iniciar/Completar/Soltar, polling 15 s, solo CARGA/MIXED) y **FinancePanel** (rango fechas, 4 stats + tablas, TODAS las flotas) en `/empresa`. App cliente: pantalla **`/freight`** (selector turbo/camión/mula, direcciones/ciudades, peso/precio, programar, "Mis fletes" + cancelar) + `_FreightEntryCard` ámbar bajo el hero intermunicipal. HECHO en Tanda 26: el conductor ve sus fletes asignados en SU app (GET /driver/freights + driver_freights_screen en /freights, ítem 'Mis fletes de carga' en el drawer). HECHO en Tanda 27 (E2E verificado con cliente WS real): aviso EN VIVO a flotas — freight.service registerNotifyFleetsNewFreight (query distinct operatorId por tipo de camión, fire-and-forget), ws.handler case operator_auth (verifyOperatorToken nuevo en operator-auth.middleware) + operatorSockets Map con cleanup en close + wiring en setupWebSocket → freight_new; FreightManager recibe token (prop desde page.tsx), conecta WS con reconexión 5 s, recarga al instante y muestra badge "En vivo" (polling 15 s queda de respaldo). Tipos WS operator_auth/_ok/_error/freight_new en ambos unions de types/index.ts.

También HECHO (Tanda 28 — login /empresa blindado + conductor toma fletes): (1) **Bug "no deja escribir ningún número/código" en /empresa** (reaparecía pese al autoComplete de Tanda 8): los dos inputs del login ahora nacen `readOnly` y se vuelven editables al enfocar (`phoneFocused`/`otpFocused`), con `autoComplete="off"` y `name` no reconocible (`nx-acceso`/`nx-codigo`) — Chrome ya no puede fijar/bloquear el campo; + Enter envía. (2) **El conductor ve y TOMA fletes disponibles desde su app** (antes solo veía los ya asignados por la flota → por eso "no los ve"): `GET /driver/freight/available` (fletes REQUESTED de los tipos de camión de SU flota + sus vehículos de carga), `POST /driver/freight/:id/take {vehicleId}` (`takeDriverFreight` resuelve su operatorId y reusa `acceptFreight` — toma atómica); `driver_freights_screen` con sección "Disponibles para tomar" (botón Tomar + selector de camión si tiene varios) además de "Mis fletes". **E2E real verificado**: cliente publica TURBO → el conductor lo ve en disponibles con su camión → lo toma → ACCEPTED → pasa a Mis fletes. Backend typecheck+9 tests, portal next build, Dart balanceado. OJO: producción al día confirmado por el usuario (portal 0344db5 businessSelfRegistration:true; backend 0344db5 otp codigo-fijo-propio).

También HECHO (Tanda 29 — S3/R2 + push FCM COMPLETOS y E2E): descubrimiento clave: la base ya existía de sesiones previas (upload.ts env-gated con multer-s3; push.service.ts mock-sin-llave + hooks de OFERTA en matching/intercity + payment/document-expiry; PUT /driver/fcm-token y /client/fcm-token; ambas apps con firebase_core/messaging, init try/catch en main, plugin gradle CONDICIONAL a google-services.json, POST_NOTIFICATIONS, y CI que decodifica secrets `GOOGLE_SERVICES_BASE64` (driver) / `GOOGLE_SERVICES_CLIENTE_BASE64` (cliente); render.yaml con placeholders S3_*/FIREBASE_SERVICE_ACCOUNT). Lo NUEVO de la tanda: push de FLETES (freight.service — `freight_new` a conductores con camión activo del tipo pedido vía query distinct driverId, `freight_assigned` solo si asigna la FLOTA — `acceptFreight` ganó param `notifyDriver`, takeDriverFreight pasa false —, `freight_cancelled` al conductor si el cliente cancela un ACCEPTED, y al cliente `freight_accepted`/`freight_in_progress`/`freight_reopened`/`freight_completed`) + push al CLIENTE en pedidos (`order_accepted` en acceptClientOrder, `order_in_transit`/`order_delivered` en updateOrderStatusByDriver — con guard `updated.userId` nullable), mandados (`errand_accepted`/`errand_delivered` en errand.service) e intercity (`intercity_update` en driverAcceptIntercity: contraoferta o confirmación); **diagnóstico en `/health`**: `uploads: s3-r2|disco-efimero` y `push: firebase|apagado` (+ pie del login /admin). **E2E verificado con Postgres local**: (A) PUT fcm-token real → flete TURBO publicado → `[Push:mock] driver=… type=freight_new` al dueño del TURBO + take → `[Push:mock] user=… type=freight_accepted` (y SIN freight_assigned, correcto); (B) backend en modo S3 contra fake-S3 local → `PUT /nexum-uploads/driver-documents/….png` recibido y `photoUrl=https://cdn.nexum.test/driver-documents/….png` (S3_PUBLIC_URL+key OK — config exacta de R2). **Guía de activación para el usuario: `docs/ACTIVAR_S3_FCM.md`** (R2 paso a paso + Firebase con los dos applicationId `com.nexum.driver_app`/`com.nexum.nexum_client` + PowerShell base64 + secrets + cómo probar). OJO E2E local: verify-otp usa campo `otp` (no `code`); rutas cliente = `/client/auth/send-otp`; y `pkill -f` con un patrón que aparece en el propio script se suicida — matar por puerto (`fuser -k 3000/tcp`).

También HECHO (Tanda 30 — documentos reales en el perfil + prueba de entrega al backend): (1) la tarjeta "Documentos" de `profile_screen` del conductor dejó el `_documents` hardcodeado (fechas falsas) y ahora pinta los documentos REALES de `driverProfileProvider` (`DriverDocument`: label/status con color+etiqueta en español/expiresAt formateado es_CO/rejectionReason del admin) con chips de resumen (rechazados/pendientes/en revisión/Verificados), estado vacío honesto, carga perezosa (`Future.microtask` si se entra sin pasar por el home, que ya hace `load()`), y botón "Gestionar documentos" → `/verification`. (2) **`POST /driver/proof/:kind/:id`** (kind trip|order|errand; multipart `file` + campo `phase` pickup|delivery): `updateMany` con `driverId` en el where = pertenencia+escritura atómica sobre `pickupPhotoUrl`/`deliveryPhotoUrl` (en mandados la recogida es `proofPhotoUrl`); guard image/*; reutiliza `documentUpload`/`fileToUrl` (→ R2 cuando esté activo). App conductor: `shared/services/proof_upload.dart` (`uploadProofPhoto` top-level, bytes web-safe + DioMediaType) llamado con `unawaited` desde `active_trip_screen` en `_handlePickupConfirm` (phase pickup) y `_handleFinishTrip` tras el DeliveryProofSheet (phase delivery) — best-effort: jamás bloquea la entrega. **E2E real** (Postgres local, mandado sembrado por SQL asignado a Nelson): pickup y delivery → 201 con URL y columnas escritas en BD; kind inválido → 400; id ajeno → 404. OJO seed SQL de errands: exige `requestRef` y `updatedAt` NOT NULL.

También HECHO (Tanda 31 — el conductor maneja el ciclo del flete desde SU app): reporte del usuario "llega el flete pero no inicia desde la app" — los botones Iniciar/Completar solo existían en el portal `/empresa` (rutas operator). Ahora: `updateDriverFreightStatus(driverId, freightId, 'in_progress'|'completed')` en freight.service (verifica `f.driverId === driverId`; el core de transiciones se extrajo a `_applyFreightStatus` compartido con `updateFreightStatus` del portal — misma liquidación `COMMISSION_RATE`+`recordCompletedTrip` y mismos push `freight_in_progress`/`freight_completed` al cliente; soltar/cancelled queda solo en el portal); ruta `POST /driver/freight/:id/status`; `driver_freights_screen` con botón **"Iniciar viaje"** (ámbar, status ACCEPTED) y **"Completar entrega"** (esmeralda, IN_PROGRESS, con diálogo de confirmación y neto en el snackbar; errores del backend mostrados vía DioException). Aplica igual si el conductor tomó el flete o si la flota se lo asignó. **E2E real** (flete ACCEPTED sembrado a Nelson): iniciar → IN_PROGRESS → completar → COMPLETED con neto 255.000/comisión 45.000 (15% de 300.000) + ambos push mock al cliente; completar dos veces → 400 "El flete no está en ruta".

También HECHO (Tanda 32 — la prueba de entrega VISIBLE para cliente y negocio, cierre del nivel "Importantes" de la evaluación): app cliente — `custody_proof_card` deja de ser recibo-demo: con foto real (`pickupPhotoPath`/`deliveryPhotoPath`, que `fromApi` ya parseaba) muestra `_RealPhoto` (Image.network vía `ApiConfig.resolveUrl`, scrim con hora + chip Firmado, tap = visor a pantalla completa con `InteractiveViewer`; errorBuilder → recibo estilizado como fallback; firma-sin-foto conserva el recibo). Portal `/negocio` — `ClientOrderCard` con miniaturas "Recogida ✓ / Entrega ✓" (h-16, abren la foto en pestaña nueva; helper `resolveImg` por página) y el detalle `/pedido/[id]` con el chip de custodia convertido en enlace con miniatura (`CustodyStep.photoUrl`); **fix real de copy-paste**: el paso 2 evaluaba `hasPickupProof` en vez de la prueba de entrega (ahora `!!order.deliveryPhotoUrl`; el backend `getOrderDetail` ya devolvía todo vía `_toSummaryDTO`). Backend — `ClientErrandDTO` += `pickupPhotoUrl`/`deliveryPhotoUrl` (mapea `proofPhotoUrl` de BD; dato listo para UI de mandados). Verificado: backend typecheck+9 tests, portal tsc + next build, Dart balanceado.

También HECHO (Tanda 33 — botones atrás que cerraban la app): helper `safeBack(context, fallback)` en `core/utils/safe_back.dart` de AMBAS apps (pop si `canPop`, si no `go(fallback)` — '/home' o '/login'). Aplicado en cliente: `intercity_booking_screen`, `intercity_history_screen` (se llega vía `context.go` al completar viaje — el atrás cerraba la app), `otp_screen`; conductor: `profile_screen`, `earnings_screen`, `otp_screen` (blindaje: hoy llegan por push). Las pantallas de flujo ya estaban sanas (canPop:false + go('/home')); el home del conductor navega todo con push. REGLA NUEVA: todo botón atrás de AppBar usa `safeBack`, nunca `context.pop()` directo.

También HECHO (Tanda 34 — Torre de Control del portal /empresa, referencia BoraUm del usuario): el Dashboard dejó de ser página apilada → **sidebar oscura fija** (slate-950, escritorio) con secciones `torre|equipo|viajes|finanzas|carga|intermunicipal` (estado `section`, las 2 últimas solo CARGA/MIXED e INTERCITY/MIXED) + header móvil con chips horizontales; **Torre de control** = badge "EN VIVO · 10 s" + KPIs `Kpi` (en línea/disponibles [`online&&status==='ONLINE'`]/en viaje [`ON_TRIP`]/conductores/vehículos/completados) + FleetMap protagonista + FleetRows; los managers existentes viven en sus secciones SIN cambios de lógica/API; `Stat` e íconos sin uso eliminados; contenido a `max-w-5xl`. Dirección visual acordada: monocromo (negro/blanco/slate) + esmeralda como único acento. Referencias del usuario en `/root/.claude/uploads/…/`: panel BoraUm (torre + detalle de corrida con línea de tiempo/acciones rápidas — el detalle de viaje individual queda como mejora futura) + app inDrive (oferta de tarifa con +/−, CTA gigante — referencia para el pulido del front de las apps).

También HECHO (Tanda 36 — auditoría de seguridad + dark mode real + Nexum Pro + purgas): **auditoría A→B→C con 8 fixes** (PR #58): WS `trip_status`/`errand_status` validan que el servicio esté asignado al conductor emisor (antes cualquier conductor completaba/cancelaba servicios ajenos); mandados LIQUIDAN al entregar (`recordCompletedTrip` con serviceFee−comisión — antes billetera en cero) + guard terminal (sin revivir cancelados ni doble liquidación) + `cancelClientErrand` avisa (WS `errand_cancelled` que la app ya escuchaba + push) y libera al conductor (antes quedaba ON_TRIP); mandado activo POR conductor (Map, antes singleton que bloqueaba mandados simultáneos); fletes con transiciones atómicas (`updateMany`+guard en iniciar/completar/soltar/cancelar — sin doble liquidación concurrente, sin reabrir cancelados). **`Driver.totalTrips` por fin se incrementa** en recordCompletedTrip (estaba congelado en 0). **Nexum Pro (B4)**: `pro.service.ts` (niveles Bronce/Plata/Oro/Diamante por servicios liquidados reales + rating — promedio intercity si existe), `GET /driver/pro-status`, pantalla `/pro` (header gradiente por nivel, stats, progreso al siguiente, escalera con requisitos/beneficios) + ítem del drawer. **Dark mode REAL**: extensión `AdaptiveColors` (`context.surfaceColor/textPrimaryColor/...` — en claro devuelven EXACTAMENTE las constantes Light; en oscuro las Dark), barrido scriptado de 69 archivos (reemplazo de tokens + de-const automático + import; pantallas de mapa excluidas a propósito: home conductor/cliente y glass bars mantienen diseño claro sobre mapa estilo DiDi), `ThemeNotifier` con persistencia SharedPreferences restaurada del historial y switches habilitados (ajustes conductor + cuenta cliente). **Purga driver_mock**: eliminado `core/mock_data/driver_mock.dart` + toda la cadena muerta `features/profile/{data,domain}` (datasource/repo/usecases sin un solo consumidor); `EditableProfile.empty()` (estado vacío honesto + `displayName`); `AuthMockDataSource` movido a `test/support/`. **Veredicto ride-negotiation**: es el flujo VIVO "Pon tu precio" (cliente `_NegotiateCard`→/request-ride; conductor home→/ride-pool) — se queda; su única deuda es vivir en memoria.

También HECHO (Tanda 37 — CAUSA RAÍZ del login /empresa "no se ve lo que escribo / no deja ingresar"): el bug NO era autofill (las Tandas 8/8.1/28 arreglaban lo equivocado). **Dos causas reales:** (1) **texto invisible** — `globals.css` pone `body { color: #F8F9FA }` (casi blanco, tema oscuro de la landing) y los `<input>` del portal NO fijaban color de texto → HEREDABAN blanco → texto blanco sobre input blanco. Fix global en `globals.css`: `input,textarea,select { color:#0f172a; color-scheme:light; caret-color:#059669 }` + placeholder slate-400 (arregla login, registro, catálogo y TODOS los managers de una vez; ningún input del portal usa fondo oscuro, verificado). (2) **no deja escribir en móvil** — el hack anti-autofill `readOnly={!focused}` (Tanda 28) ROMPE el teclado: tocar un input `readOnly` en iOS/Android no invoca el teclado virtual → foco sin teclado → imposible teclear. Fix: eliminado el `readOnly`/`phoneFocused`/`otpFocused`; el teléfono usa `type=tel autoComplete=off`, el OTP `type=text inputMode=numeric maxLength=6 autoComplete=one-time-code` (valor semántico correcto: el móvil ofrece el código del SMS). Verificado: tsc raíz + next build (rutas /empresa y /empresa/registro OK).

También HECHO (Tanda 38 — solicitar intermunicipal + de-mock del dark + mapa de flete): (1) **Cliente no veía cómo solicitar intermunicipal** — el botón "Buscar conductor" vivía SOLO en el `bottomNavigationBar` de `intercity_booking_screen`, que en ciertos dispositivos/insets no queda visible ("solo deja escribir en los campos pero no sale opción de solicitar"). Fix: CTA "Solicitar viaje intermunicipal" movida al cuerpo del `ListView` (siempre alcanzable) y `_BottomBar` eliminado. (2) **Modo oscuro DESHABILITADO otra vez** (forzado a claro): el barrido de la Tanda 36 hizo el TEXTO adaptativo pero dejó ~236 fondos hardcodeados sin adaptar (`backgroundColor: Colors.white` 8, `BoxDecoration color: Colors.white` 40, el resto de los 223 `color: Colors.white` son foreground de iconos/texto que NO se tocan) → texto claro sobre fondo claro = ilegible. `ThemeNotifier` fijo en claro, switches "Próximamente". La extensión `AdaptiveColors`/`AdaptiveThemeColors` se conserva (en claro = constantes Light exactas). **Un dark mode correcto exige QA visual en dispositivo (iterativo con capturas), no un barrido a ciegas** — pendiente real. (3) **Salidas programadas como salida** en el estado de búsqueda intercity: `_SearchingHelpCard` ofrece ir a `/pooled/search` mientras el matching on-demand busca (no depende de conductor en línea). (4) **Mapa del trayecto del flete**: `FreightRequest` += `originLat/Lng`+`destLat/Lng` (migración `20260715000000_add_freight_coords`), resueltas al crear desde el centroide de ciudad (`INTERCITY_CITY_COORDS`, fallback Pamplona — sin geocodificador externo); DTO expuesto; widget `FreightRouteMap` (flutter_map/OSM, marcador origen verde/destino rojo + línea) en la tarjeta de flete de AMBAS apps cuando está ACCEPTED/IN_PROGRESS. (5) **Operador**: `SchedulesManager` muestra puestos **vendidos**/total (antes disponibles, confundía) + **manifiesto de pasajeros** expandible por salida (nombre/puestos/teléfono enmascarado/recogida/notas — el dato ya lo devolvía `getOperatorPooledTrips`). Verificado: backend typecheck+9 tests, web tsc+next build; Flutter en CI. **OJO fletes viejos sin coords no muestran mapa** (solo los nuevos tras el deploy).

También HECHO (Tanda 39 — P0/P1 para competir: KYC+antifraude, checkout de pago, chat de viaje + soporte con tickets). **(A) KYC de identidad del conductor + antifraude** (patrón env-gated como OTP/Wompi/S3): schema `Driver.kycStatus/kycProvider/kycReference/kycCheckedAt/selfieUrl`+`fraudFlags`, enum `KycStatus` (migración `20260716000000_add_kyc_fraud`); `kyc.service` (proveedor `manual`→IN_REVIEW, nunca auto-aprueba; punto de integración Truora/Metamap; `KYC_ENFORCE` gatea el "conectarse" — con false = solo documentos, igual que hoy); rutas conductor `GET /driver/kyc`, `POST /driver/kyc/selfie` (multipart), `POST /driver/kyc/submit`, y `PUT /driver/status` bloquea online si `!isDriverCleared`; admin `/admin/drivers` expone kycStatus/selfie/fraudFlags + `POST /admin/drivers/:id/kyc` (decisión manual) + columnas/botones en el panel; app conductor `driver_kyc_provider` + sección "Verificación de identidad" en `verification_screen` (selfie cámara frontal). `fraud.service`: `evaluateGeoJump` (velocidad GPS imposible→`fraudFlags`, best-effort, cableado en `updateDriverGeo`) + `assertClientRequestRate` (ventana deslizante en memoria) via `client-rate-limit.middleware` en las 5 rutas que CREAN solicitud (trip/errand/order/freight/intercity)→429; `pruneRateLimits` en timer. `/health` += `kyc`/`kycEnforce`. **(B) Checkout de pago cerrado en app cliente**: antes "lanzaba y olvidaba" (abría Wompi y nunca confirmaba). `payment_checkout.dart` `showPaymentCheckout()` reutilizable: inicia pago→abre Wompi→**sondea** `GET /client/payments/:ref` (`pollUntilResolved` 3 s/4 min) hasta aprobado/rechazado con UI de resultado (reabrir/reintentar/pagar más tarde); cableado en viaje urbano (`_PaymentSheet` = selector puro En línea/Efectivo) y checkout del carrito. Backend de pagos (Wompi+webhook+reconciliación) ya estaba. **(C) Chat de viaje normal + soporte con tickets** (persistente): migración `20260716120000_add_chat_support` (`trip_messages`, `support_tickets`+`support_messages`, enum `SupportStatus`); `trip-chat.service` (autoriza participante del Trip, fan-out en memoria + persistencia) con WS `subscribe_trip_chat`/`trip_chat_send`/`trip_chat_message`/`trip_chat_history` en `ws.handler`; REST `GET /{client,driver}/trips/:id/chat`; `support.service` (crear/listar/detalle/responder solicitante + admin listar/responder/estado con push al solicitante) con rutas cliente/conductor `*/support/tickets*` y admin `/admin/support*` + pestaña "Soporte" en el panel (hilo + responder + estados). Apps: `TripChatScreen` en ambas (WS singleton; botón chat en `_DriverCard` del cliente y `onMessage` real en `going_to_passenger_card` del conductor), `support_api`+pantallas de tickets (cliente: "Ayuda y soporte" de la cuenta; conductor: `SupportScreen` reescrito a 2 pestañas FAQ+Mis tickets **eliminando el chatbot falso `_ChatTab`**). Verificado: backend typecheck+9 tests; Flutter en CI. **PENDIENTE opcional**: proxy de llamadas (hoy número protegido→chat), proveedor KYC real (env `KYC_PROVIDER`+llaves), notificación WS de nuevo mensaje de soporte fuera de la pantalla.

También HECHO (Kill-switch documental — Fase 1 del plan de cumplimiento, E2E verificado): `Driver.complianceStatus CLEAR|EXPIRING|BLOCKED` + `blockedReason/blockedAt` (migración `20260719000000_add_doc_compliance`); `document-expiry.service` reescrito — el barrido diario ahora RECALCULA el cumplimiento por conductor (obligatorios: cédula/licencia/SOAT/tarjeta con `expiresAt`; vencido ⇒ BLOCKED + push/WS `compliance_update` vía `registerComplianceSendToDriver` en ws.handler) y con **`DOC_KILL_SWITCH_ENFORCE=true`** (default false, patrón KYC_ENFORCE; visible en `/health` como `docKillSwitch`) además: fuerza OFFLINE, lo excluye del matching urbano/intercity (filtro `complianceStatus <> 'BLOCKED'`), bloquea `acceptFreight` y rechaza `PUT /driver/status` online con 403 `documents_expired`. Renovar el documento (upload → PENDING) o la revisión del admin recalculan al instante (`evaluateDriverCompliance` hooked en driver-profile.service). Admin: columna "Docs" en el panel + `POST /admin/drivers/:id/compliance/clear` (override; el barrido re-bloquea si el doc sigue vencido) + `complianceStatus` en `diagnoseMatching`. App conductor: `DriverProfileEntity.complianceStatus/blockedReason`, banner rojo en el panel del home + `_toggleOnline` bloquea el conectarse con CTA a `/verification`. **E2E real (Postgres+PostGIS local)**: barrido→BLOCKED+OFFLINE→no dispatchable en diagnóstico→renovar→CLEAR→re-vencer→BLOCKED→override admin→CLEAR.

También HECHO (Paridad intermunicipal/flota — Fase 3 del plan, E2E verificado): **intermunicipal por defecto** — `Driver.intercityEnabled` default `true` + backfill (migración `20260719100000_intercity_default_on`): un conductor ONLINE recibe intermunicipales sin activar nada (opt-out en el toggle/preferencias existentes, que siguen leyendo/escribiendo `intercityEnabled`); **seguimiento EN VIVO**: `IntercityBookingDTO.driverLat/driverLng` (enriquecido en `getActiveIntercityBooking`/`ById`/`Snapshot` vía `_withDriverPosition` con `Driver.lastLat/lastLng` del heartbeat, solo CONFIRMED/IN_PROGRESS) y `FreightDTO.driverLat/driverLng` (`_toDTOsWithDriverPos` en `listClientFreights`/`listDriverFreights`, batch); app cliente — `IntercityRequestEntity.driverLat/Lng`, `_trackTimer` en intercity_provider (sondea `GET /client/intercity/:id` cada 8 s con viaje confirmado/en curso) y **`_LiveTripMap`** en intercity_status_screen (flutter_map, ruta origen→destino + `VehicleMarker` con rumbo + chip "Conductor en vivo"); `FreightRouteMap` de AMBAS apps pinta el camión (`driverLat/Lng` del JSON) y las pantallas de fletes auto-refrescan (cliente 15 s, conductor 20 s); conductor — `PooledCity.coords` + mapa de ruta en la tarjeta del viaje intermunicipal activo. **E2E real (Postgres local)**: conductor nuevo → `intercityEnabled=true`; booking CONFIRMED → `driverLat/Lng` en DTO; flete ACCEPTED → posición en la lista.

También HECHO (Seguridad operativa — Fase 4 del plan, E2E verificado): **`safety-alerts.service.ts`** alimentado por el heartbeat (`updateDriverGeo` → `onDriverHeartbeat`, fire-and-forget, lookup del servicio activo cacheado 30 s por conductor): (1) **geocerca de destino** (urbano 400 m / intercity-flete 2.5 km, env `GEOFENCE_*_M`) → push al cliente "tu conductor está llegando" + alerta; (2) **detención prolongada** (`SAFETY_STALL_MIN`, default 10 min, sin moverse ≥150 m con servicio IN_PROGRESS); (3) **desvío** del corredor recto origen→destino (`max(2 km, 35 % de la ruta)`, env `SAFETY_DEVIATION_*`). Aplica a trip (coords propias), intercity (centroides) y freight. Todo pasivo/best-effort, una alerta por tipo por servicio, anillo en memoria de 200 (`pruneSafetyState` en timer). Torre de Control: `GET /admin/alerts` (todas) + tabla "Alertas de ruta" en la pestaña SOS del panel; `GET /operator/alerts` (filtradas por flota) + **`AlertsPanel`** en la sección torre de `/empresa` (poll 15 s). **E2E real (Postgres local)**: desvío 5 km → alerta; detención → alerta; entrar a 100 m del destino → geofence + push mock, sin repetirse.

También HECHO (OCR + antecedentes — Fase 2 del plan, E2E verificado, TODO env-gated): **`ocr.service.ts`** (`OCR_PROVIDER`: `none` default no-op · `truora|metamap|azure-di` puntos de integración con warning, jamás inventan datos · `fake` solo dev para E2E): al subir documento (`uploadDriverDocument`/`upsertDriverDocument`) extrae número/nombre/vencimiento → **autollena `expiresAt` solo si el conductor no lo escribió** (la fecha del conductor manda), persiste `DriverDocument.ocrFields/ocrConfidence` (migración `20260719300000_add_ocr_background`) y el **mismatch de nombre incrementa `fraudFlags`**; **`background-check.service.ts`** (`BACKGROUND_CHECK_PROVIDER`, mismo patrón): `Driver.backgroundStatus UNCHECKED|PENDING|CLEAR|HIT` + provider/reference/checkedAt; se dispara fire-and-forget en `submitDriverKyc` y a demanda vía `POST /admin/drivers/:id/background`; **HIT jamás bloquea** — marca para revisión del admin. Panel admin: línea "🔎 OCR: N°/nombre/vence (confianza%)" + "Vence:" bajo el archivo en Verificaciones, y chip de antecedentes (✓/pendiente/⚠ HALLAZGO) en la celda KYC de Conductores. `/health` += `ocr` y `background`. **E2E real (Postgres local)**: fake OCR → expiresAt autollenado + ocrFields + mismatch flag; fecha del conductor manda; antecedentes CLEAR/HIT persistidos; y **sin proveedores todo es idéntico a hoy** (no-op verificado). El prellenado editable en la app conductor queda para cuando haya proveedor real (hoy con `none` es invisible).

También HECHO (Blindaje legal — Fase 5 del plan, E2E verificado): **`legal.service.ts`** con documentos VERSIONADOS (`legal_documents` TERMS/PRIVACY, migración `20260719400000_add_legal`; la v1 se siembra sola al primer uso con texto es-CO que incluye **cláusula de arbitraje + renuncia a acciones colectivas**, **transparencia de IA** y **procedimiento de retiro DMCA**; publicar versión nueva desactiva la anterior ⇒ re-aceptación) servidos por `GET /legal/terms|privacy`; **constancia de consentimiento** `legal_consents {sujeto, versión, fecha, IP}` grabada en los 3 registros (conductor `/auth/register`, cliente `verify-otp` — que también crea cuenta —, empresa `/operator/register`) cuando llega `acceptedTerms: true`; **enforcement env-gated `LEGAL_CONSENT_ENFORCE`** (default false: apps viejas siguen; true: registro sin aceptar ⇒ 400/403 `legal_consent_required`; visible en `/health` como `legalConsent`); **takedown DMCA**: `POST /legal/takedown` público (rate-limited) → tabla `takedown_requests` → panel admin pestaña SOS "Retiros DMCA" (Retirado/Rechazar con constancia); **clickwrap en las 3 superficies**: checkbox NO preseleccionado en el paso final del registro del conductor (register_screen, gatea el botón), en el teléfono del cliente (phone_input_screen, gatea Continuar; verify-otp envía acceptedTerms) y en `/empresa/registro` (con links a /legal/*); **transparencia de IA** en la landing (footer) y en las pantallas de privacidad de AMBAS apps (sección "Uso de inteligencia artificial"); `docs/PRIVACIDAD_DATOS.md` (inventario de datos por app = base de privacy labels App Store/Play) y `docs/DMCA_AGENTE.md` (procedimiento + pendiente del representante legal: registrar agente, renovable cada 3 años). **E2E real**: siembra v1 con arbitraje/IA/DMCA → consentimiento con IP → versión nueva exige re-aceptación → takedown crear/listar/resolver.

También HECHO (Rutas profesionales — Google Maps por proxy, listo para la llave): el backend YA tenía el proxy `/geo/*` (`geo.service.ts`: Places New autocomplete/details, Geocoding reverse, **Routes API computeRoutes** con `GOOGLE_MAPS_API_KEY` solo-servidor + `/geo/health` de diagnóstico). Esta tanda cablea la **polyline real por las calles** en los 5 mapas: decodificador `core/utils/polyline_decoder.dart` (ambas apps), cliente `GeoRoutePoints.routePoints()` (extensión de GeoService con caché en memoria por par de coords), conductor `shared/services/route_service.dart` (mismo patrón con DioClient); mapas: tracking urbano del cliente (`_TripMap`), `_LiveTripMap` intermunicipal, `FreightRouteMap` de AMBAS apps (StatefulWidget con fetch en initState) y el viaje activo del conductor (la ruta real REEMPLAZA la esquina en L simulada de `_generateWaypoints`, manteniendo el render consumida/restante). TODO con fallback elegante: sin `GOOGLE_MAPS_API_KEY` en Render, `routePoints` devuelve null y los mapas dibujan las líneas de siempre. Activación = crear key con Places API (New) + Geocoding API + Routes API y ponerla en Render; verificar en `/geo/health`.

También HECHO (Google Maps REAL de tiles + rediseño total del mapa): (1) **imagen real del mapa de Google** por proxy — `geo.service` sesión de Map Tiles (`createSession` cacheado) + `fetchMapTile`; `GET /geo/tile/:z/:x/:y` (key server-side, auth por `?t=` o header); widget `GoogleMapTiles` en ambas apps con `fallbackUrl`=OSM (el mapa NUNCA queda en blanco); `/geo/health` prueba las 4 APIs (geocoding/places/routes/mapTiles). Activar = habilitar **Map Tiles API** en Google Cloud sobre la misma key. Portal `/empresa` FleetMap (Leaflet) también proxea tiles de Google con el token del operador + fallback OSM por `tileerror`. (2) **Pines gota** (`MapPin`, `Icons.location_on`, punta anclada con `alignment.topCenter`) y **vehículo ILUSTRADO** (`VehicleGlyph` + assets `assets/vehicles/{car,moto,truck}.png` en ambas apps: carro, moto-repartidor magenta, camión) que **se desliza A→B** (interpolación con AnimationController entre fixes GPS, estilo Uber/DiDi) y se voltea según el rumbo, en los 8 mapas (tracking urbano/intermunicipal/pedidos/fletes/home cliente + viaje activo/fletes conductor). (3) **login arreglado** — el interceptor de auth leía el token de `FlutterSecureStorage` SIN timeout: en algunos dispositivos el Keystore se colgaba → petición nunca salía → "cargando" infinito. Fix: `.timeout(5s)` + try/catch en `onRequest` de ambas apps. Además: permiso `ACCESS_FINE_LOCATION` faltaba en el AndroidManifest del **cliente** (causa de "no se pudo obtener tu ubicación"); mensajes de error de red concretos; warm-up del backend en `main()` (Render cold-start); `BUILD_TAG` visible en el login del cliente. (4) sonido del conductor reemplazado por una campana suave. Registro de conductor con **Moto** ya no da 400 (la validación de `/auth/register` solo aceptaba particular/taxi). **OJO producción: el mapa de Google real necesita `GOOGLE_MAPS_API_KEY` en Render + Map Tiles API habilitada; sin eso, todos los mapas caen a OpenStreetMap sin romperse.**

También HECHO (Intermunicipal protagonista + paradas + trazabilidad de carga + íconos por vehículo real — plan 4 módulos): **(D)** el ícono del mapa se elige por el **vehículo REAL** del conductor (no por tipo de servicio): `driverVehicleType` en `ClientTripDTO`/`IntercityBookingDTO` (del vehículo activo del conductor) + `vehicleType` en `/client/drivers/nearby` (JOIN lateral) + `DriverProfileEntity.vehicleType`; helper `vehicleGlyphKindFor(vehicleType, fallback)` en ambas apps (MOTO→moto; TURBO/CAMION/MULA→camión; resto→carro; fallback por servicio si falta). **(A)** el conductor VE el trayecto intermunicipal/flota: `FreightRouteMap` (tiles Google + polyline real) en `my_pooled_trips_screen` y en la oferta/viaje intermunicipal activo. **(B)** **paradas ("pasa por")**: `IntercityBooking.stops`/`PooledTrip.stops` (Json `[{name,lat?,lng?,order}]`, máx. 6, migración `20260721300000_add_trip_stops`); helper `lib/trip-stops.ts` (`sanitizeStops`/`stopsFromDb`); `POST /client/intercity/request` y `/operator/pool/publish` aceptan `stops[]`; editor de chips en el portal (SchedulesManager) y en la reserva del cliente (`intercity_booking_screen`); "Pasa por:" en estado del viaje, tarjeta de Cupos, oferta del conductor y salidas de flota. **(C)** **trazabilidad del flete**: modelo `FreightEvent` (FUEL|STOP|NOTE, monto/galones/odómetro/nota/foto/GPS, migración `20260721310000_add_freight_events`); `addFreightEvent`/`listFreightEventsForDriver`/`listFreightEventsForOperator` (+ total combustible); `POST/GET /driver/freight/:id/events`, `GET /operator/freight/:id/events`; sheet "Tanqueo/parada" + "Bitácora" en `driver_freights_screen`; timeline + total de combustible en `FreightManager` del portal. Todo aditivo: sin el dato, comportamiento idéntico al actual.

También HECHO (Catálogo profesional del negocio — 5 fases, la raíz de "el portal de restaurantes es muy limitado"): **Fase 1 — seguridad del pedido** (era un hueco real): `placeClientOrder` ya NO confía en el precio que manda la app — valida que cada `productId` pertenezca al `businessId`, que esté disponible y con stock, y toma el **precio de la BD** (`Math.min(Math.max(producto.price, enviado), producto.price * 3)`); descuento de stock **atómico** con rollback si falla, y `restoreOrderStock(orderId)` en los 3 caminos de cancelación. **Fase 2 — el formulario se adapta al negocio**: `Product.barcode/sku/stock/unit/brand` (migración `20260730000000_add_product_inventory`, con `@@index([businessId, barcode])`); helper `manejaInventario(categoria)` en el portal — un restaurante NO ve código de barras ni existencias (solo estorban), un supermercado/farmacia SÍ; existencias vacías = no controlar, con número = se descuenta por pedido y deja de venderse en cero. **Fase 3 — escáner de código de barras** (`BarcodeScanner.tsx`): API nativa `BarcodeDetector` (cero dependencias; formatos ean_13/ean_8/upc_a/upc_e/code_128/itf), **escaneo en cadena** (la cámara queda abierta), antirrebote de 2 s por código, vibración de confirmación, y **entrada manual SIEMPRE visible** (Safari/Firefox no tienen la API — el dueño nunca queda encerrado); `GET /:token/products/barcode/:code` distingue "ya lo tienes" (precarga el formulario para editar) de "es nuevo". **Fase 4 — buscador**: en el portal (nombre/marca/código + filtro por sección + "solo agotados") y en el detalle del negocio de la app cliente (`business_detail_screen` pasó a `ConsumerStatefulWidget`; el buscador aparece desde 12 productos). **Fase 5 — carga masiva por CSV**: `lib/product-csv.ts` (`splitCsvLine` respeta comillas y acepta `,` y `;` de Excel-es; precios con separador de miles "3.500"/"$12.000"; cabecera opcional pero si parece cabecera exige nombre+precio; detecta códigos repetidos DENTRO del archivo) + **15 tests** (`product-csv.test.ts`, 45 en total en el backend); rutas `GET /:token/products/csv-template`, `POST /:token/products/csv-preview`, `POST /:token/products/csv-import` (`applyProductImport` usa `updateMany` con guard de `businessId`); UI `CsvImport.tsx` con **vista previa obligatoria** (N nuevos / N se actualizan / N con error, errores primero con número de línea) — nunca se importa a ciegas porque un CSV con la columna de precio corrida destrozaría el catálogo y el dueño lo sabría cuando un cliente comprara al precio equivocado. **PENDIENTE: el usuario debe probar el escáner en un teléfono real** (la cámara no se puede verificar desde el entorno de desarrollo).

También HECHO (Auditoría de portales + los 2 bloqueantes del portal de negocios): auditoría de `/empresa`, `/admin` y landing (`/negocio` acababa de recibir las 5 fases del catálogo). **Hallazgos con evidencia**: (a) el negocio que perdía su enlace mágico quedaba fuera para siempre — `/negocio` no existía (404), sin recuperación, y el panel admin **no tenía pestaña de Negocios** ni ruta `/admin/business*`; (b) el pedido llegaba por WS y solo pintaba un toast — **sin sonido, sin `document.title`, sin Notification** (grep de Audio/sound/Notification: cero), invisible con la pestaña de fondo; (c) `GET/POST /operator/documents` **existen en el backend pero el portal nunca los usa** y el admin no puede abrirlos (solo `/verify`, `/suspend`, `/routes`) → la habilitación de empresas se aprueba a ciegas y la columna "Docs pend." solo puede ser 0; (d) `OperatorMember` con roles existe y `requireOperatorRole` los exige, pero **ninguna ruta crea miembros** → una flota solo puede tener un usuario; (e) no hay `PUT /operator/profile` → la empresa no puede corregir sus datos; (f) `/operator/trips` no acepta rango de fechas y el CSV exporta todo → no se puede liquidar un mes. **Resueltos (a) y (b)** (PR #124): `findBusinessesByPhone` (filtra por últimos 4 dígitos y compara normalizado — el teléfono se guardó como lo escribió el dueño; sirve para los ya registrados), `POST /business/recover/{send-otp,verify-otp}` (valida el código PRIMERO y solo entonces revela los negocios ⇒ sin enumeración; con Twilio el SMS solo sale si hay negocios, anti-pumping), página `/negocio` nueva (teléfono→código→enlaces con copiar/entrar), pestaña **Negocios** en el panel admin (`listBusinessesForAdmin`/`setBusinessActive`, buscador, "Abrir portal", activar/desactivar), enlace de rescate en la pantalla de token inválido y en la landing; `useOrderAlert` (campana WebAudio de dos notas ×3 repiques, vibración, Notification si la pestaña está oculta, título parpadeante con contador mientras haya pedidos sin atender, y botón "Activar el sonido" cuando el AudioContext quedó suspendido). **Además `PortalTabs`**: Catálogo y Ajustes estaban `hidden sm:inline-flex` ⇒ **inalcanzables desde el celular**; ahora hay pestañas Pedidos·Catálogo·Ajustes en las 3 páginas solo en móvil, y el header pasó a `sm:sticky` para que lo fijo al desplazar sea la navegación (evita el `top-[68px]` calculado a mano). `/empresa` revisado: su barra de chips ya lista todas las secciones, sin cambios. **Verificación del panel admin**: se ejecuta su JS con datos falsos (`new Function` + fetch simulado) y se comprueba que los `onclick` salen con comillas reales — leer el código fuente engaña, ahí `\\'` dentro de una cadena es lo correcto. **(c) RESUELTO** (bloqueante 3): `DocumentsManager` en `/empresa` sección **Habilitación** (subida multipart con tipo + vencimiento opcional, lista con estado/motivo de rechazo/enlace al archivo, y aviso ámbar mientras no haya HABILITACION aprobada y vigente); admin `GET /admin/operators/:id/documents` + `POST /admin/operator-documents/:docId/{approve,reject}` (`listOperatorDocumentsForAdmin` marca `expired`; `reviewOperatorDocument` guarda motivo y `reviewedBy`), panel con botón **Docs** por empresa y sub-panel de revisión (el rechazo pide motivo por `prompt` — sin él la empresa vuelve a subir lo mismo). `AdminOperatorRow` += `habilitacionOk` (aprobada Y vigente) ⇒ la columna "Docs pend." pasó a **Habilitación** (Aprobada / Sin habilitación + "N por revisar") y **`setOperator(id,'verify',habilitacionOk)` pide confirmación si se verifica sin habilitación** — no bloquea (el admin pudo ver los papeles en físico) pero deja de ser un descuido. **(d)(e)(f) RESUELTOS**: `listOperatorMembers`/`addOperatorMember` (E.164 obligatorio — el login casa por teléfono exacto; bloquea duplicado y pertenencia a otra empresa)/`removeOperatorMember` (guard: nunca dejar cero OWNER) + `GET/POST/DELETE /operator/members` (POST y DELETE solo OWNER) y **`MembersManager`** en la sección Equipo; **`PUT /operator/profile`** (`updateOperatorProfile`: tradeName/contactName/contactPhone/contactEmail/city — `legalName`/`nit`/`type` NO, son la identidad sobre la que el admin verificó) + **`ProfileForm`**, y la sección «Habilitación» pasó a **«Mi empresa»** (perfil + documentos); **rango de fechas** en `listOperatorTrips(operatorId, limit, from, to)` y `exportOperatorTripsCsv(operatorId, from, to)` vía `lib/date-range.ts` (`rangoFechas`, **5 tests** — el `to` se toma hasta las 23:59:59 o el último día del mes quedaría fuera del reporte; fecha inválida se ignora en vez de reventar), rutas `?from&to`, y selector Desde/Hasta + atajo **«Mes pasado»** en la sección Viajes, con el CSV descargando exactamente el mismo rango que la tabla. **Auditoría de portales cerrada.**

También HECHO (el despacho INSISTE en vez de callarse): reporte del usuario "cuando se acepta el pedido no redirige a la app conductor". Cadena verificada sana de punta a punta (`acceptOrderByBusiness` → PREPARING → `startOrderMatchingCycle` → `findNearestAvailableDrivers(...,'order')` → WS `order_request`; `registerSendToDriver` inyectado; el app conductor parsea `trip_request`/`errand_request`/`order_request`/`intercity_request`). **La causa real era otra**: pedidos, mandados e intermunicipales buscaban conductor **UNA sola vez** — si en ese instante no había nadie ONLINE con GPS fresco a ≤5 km, quedaba un `console.log` que nadie lee y el servicio esperaba para siempre; aunque un repartidor se conectara 30 s después, **nunca se le volvía a ofrecer** (solo el viaje urbano avisaba, vía `_onNoDrivers`). Fix: `scheduleSearchRetry`/`cancelSearchRetry` en matching.service (clave `order:|errand:|intercity:<id>`, **20 intentos × 30 s = 10 min**, `timer.unref()`), aplicado a los tres ciclos tanto cuando no hay candidatos como cuando se agotan; el contexto del reintento viaja en el estado de la oferta (`OrderOfferState.attempt`, `ErrandOfferState.retry`) para no reiniciar la cuenta al declinar; se corta al aceptar (`onOrderAccept`/`onErrandAccept`/`driverAcceptIntercity`) y al cancelar (`cancelClientOrder`/`rejectOrderByBusiness`/`cancelClientErrand`/`cancelIntercityBooking`). **Y al agotarse, se avisa**: push al cliente (`order_no_driver`/`errand_no_driver`/`intercity_no_driver`) y WS `order_no_driver` al negocio vía `registerNotifyBusinessNoDriver` (inyectado en ws.handler sobre `businessSockets`), con banner rojo en el portal — el negocio tiene la comida hecha y es quien debe decidir. **El viaje urbano NO reintenta a propósito**: el pasajero está parado en la calle y prefiere saber ya.

También HECHO (PIN, geografía real del pedido y direcciones con mapa — 4 fases): reporte del usuario "no arroja el pin", "no se muestra el trayecto", "solo se puede escribir la dirección". **(1) El PIN nunca llegaba**: el backend lo generaba y exponía bien; en la app eran DOS fallos — `placeOrder` tomaba de la respuesta solo `id`/`orderRef` (descartaba `deliveryPin`) y **`CustomerOrderEntity.copyWith` omitía `deliveryPin`**, así que la primera actualización en vivo lo ponía en null (las actualizaciones no lo traen ni deben: es el mismo DTO que ve el repartidor). Ahora se conserva al crear, sobrevive a copyWith y se adopta del sondeo REST; los mandados ya estaban bien. **(2) El mapa era simulación**: `_TrackingMap` derivaba las posiciones del `hashCode` del nombre del negocio y del texto de la dirección, y animaba un repartidor ficticio — el último teatro del cliente. Reemplazado por coordenadas reales (`ClientOrderSummaryDTO` += businessLat/Lng, deliveryLat/Lng, driverLat/Lng vía `_withOrderGeo`, patrón de `_withDriverPosition`), `VehicleGlyph` deslizándose entre fixes GPS, ruta real con `routePoints()` (línea recta si no hay llave) y **sin mapa si no hay geografía** (`hasRealGeo`) en vez de uno falso; `trackOrder(id)` sondea al abrir la pantalla. **(3) Los negocios no tenían punto**: `geocodeAddress` nuevo en geo.service (Geocoding, acotado a ciudad+Colombia), best-effort al registrar, y `LocationPicker` en ajustes del portal (`PUT /business/:token/location`) — única vía para los ya registrados. **(4) Direcciones**: `AddressAutocompleteField` existía pero solo estaba en `transport_booking_screen`; ahora también en mandados, fletes y direcciones guardadas, + `MapLocationPicker` (pin fijo al centro, "estoy aquí", `reverseGeocode` nuevo en el GeoService del cliente). **Vacío crítico encontrado y cerrado**: `Order.deliveryLat/Lng` NUNCA se guardaban al crear el pedido ⇒ el mapa de la fase 2 no se habría mostrado jamás; `POST /client/orders` ya las acepta, `AddressEntity` guarda el punto y el checkout lo pasa. En mandados, la recogida elegida MANDA sobre el GPS del teléfono. **OJO: las fases 3 y 4 no se notan sin `GOOGLE_MAPS_API_KEY` en Render** (Places New + Geocoding + Routes + Map Tiles): sin llave el autocompletado devuelve vacío, la ruta cae a línea recta y los tiles a OSM — todo degrada limpio, nada rompe.

También HECHO (Auditoría del código reciente — 8 fallos + las apps que no compilaban): repaso de las tandas del PIN de custodia y de los municipios. **PIN del envío**: nunca llegaba al cliente (`transport_provider.request()` descartaba `deliveryPin` de la respuesta, `toJson` no lo persistía, `_applyTripUpdate` no lo leía) ⇒ el repartidor pedía un PIN que el cliente no veía y **el ENVÍO no se podía cerrar nunca**; y al revés, `_toTripDTO` lo metía en el DTO compartido, que es el que se le devuelve al conductor en `trip_accepted`. Ahora existe `ClientTripWithPinDTO` (patrón ya usado en pedidos/mandados/fletes) y el PIN solo sale por creación, `/client/trips/active` y `GET /client/trips/:id` (con propiedad verificada). **Las 4 suscripciones WS** (`subscribe_trip/_order/_errand/_intercity`) no comprobaban nada — cualquier socket podía pedir un id ajeno y recibir el DTO completo; ahora `_puedeSuscribirse` exige ser el cliente dueño o el conductor asignado. `handleSubscribeTrip` no hacía `await` (snapshot inicial `trip: {}`). **Municipios**: `startIntercityMatching` resolvía la ciudad con el mapa de los 7 valores del enum viejo y caía a `?? 'pamplona'` ⇒ toda reserva desde un municipio nuevo buscaba conductor en Pamplona; `_findIntercityDrivers` leía el centroide del mismo mapa fijo (habría reventado en `c.lng`); `addOperatorRoute` guardaba MAYÚSCULAS mientras el matching comparaba slug minúscula ⇒ **con `INTERCITY_DUAL_MODEL=true` ninguna ruta habría casado jamás** (ahora guarda slug y la SQL compara con `lower()`, así las rutas viejas siguen valiendo; el `count` de Prisma usa `mode:'insensitive'`); fletes y alertas de ruta con el mismo mapa de 7 (ahora `coordsOfSync`); `/client/intercity/request` no validaba el municipio. **Buscador de municipios**: la lista ya venía del backend pero la UI seguía siendo un desplegable — `showCitySearchSheet` en ambas apps (filtro sin tildes, agrupado por departamento) y `CityInput` en el portal; `useMunicipios` compartido (RoutesManager y SchedulesManager tenían los 7 escritos a mano: la tanda de municipios nunca tocó el portal).

**Y la causa de "sigue saliendo la lista de siete" en el teléfono: las apps NO COMPILABAN.** El APK del conductor llevaba una tanda fallando y el del cliente **dos** (último bueno: 31 de julio); el CI llevaba esas mismas dos tandas en rojo y se mergeó igual. Cuatro errores del compilador: `transport_tracking_screen` usaba `TransportRequestStatus` (el enum es `TransportStatus`); dos `switch` de EXPRESIÓN sobre enums a los que la tanda anterior añadió `driverToPickup`/`atPickup` sin añadir los casos (en Dart eso es error, no aviso); y `static const Map<IntercityCity, …>` dejó de ser válido al convertir el enum en clase (una clase con `==` propio no puede ser clave constante) más un `_cityCoords[c]!` que habría reventado con cualquier municipio nuevo — ahora `IntercityCity`/`PooledCity` guardan su centroide (lo trae `/geo/municipios`) y `coordsOf` devuelve nulo en vez de estallar. **REGLA: el `Deploy live` en verde de Render NO prueba que la migración corriera** — el `CMD` es `prisma migrate deploy || echo WARN; node dist/index.js`. Comprobado con Postgres real: la migración de municipios aplica bien en base limpia y sobre datos previos (PAMPLONA→pamplona, 45 filas) y `/geo/municipios` devuelve los 45. Los workflows de CI y APK ahora corren también en `claude/**` (antes solo tras el merge a main, que es cómo se coló todo esto).

También HECHO (Control total de carga — 5 bloques, E2E contra Postgres real): lo que el cliente de carga pedía (gasolina, dónde está el conductor, tiempos) chocaba con cinco huecos concretos; los cinco cerrados en orden. **(1) Rastro GPS histórico** — `Driver.lastLat/lastLng` era UN punto que cada heartbeat sobreescribía: se veía dónde está, no por dónde pasó. Nuevo `DriverTrackPoint` (migración `20260804220000_add_driver_track`) + `track.service.ts`, enganchado en `onDriverHeartbeat` **reutilizando el lookup del servicio activo ya cacheado** (grabar no cuesta consulta extra). Tres reglas acotan el volumen: solo con servicio EN CURSO (un punto sin dueño no le sirve a nadie), filtro por distancia y no por tiempo (`TRACK_MIN_DIST_M` 75 m — el heartbeat de 4 s daría ~900 filas/hora repetidas) y piso en parada (`TRACK_IDLE_INTERVAL_S` 300 s, para poder probar "estuvo quieto aquí"); purga diaria por `TRACK_RETENTION_DAYS` (90). `GET /operator/freight/:id/track` con km reales/duración/tiempo detenido/promedio en movimiento, y `TrackMap` en el portal (cargador de Leaflet extraído a `app/empresa/leaflet.ts`, compartido con FleetMap). **Bug que encontró el E2E**: `metersFromPrev` quedaba null cuando el estado en memoria estaba frío ⇒ **cada reinicio del backend perdía la distancia de un tramo en silencio** (el mapa se veía completo y el total salía corto); `_prevPoint` ahora hidrata desde la BD. **(2) Costos y rentabilidad** — la bitácora solo tenía FUEL|STOP|NOTE y el combustible **no entraba en `getFleetFinance`**: el panel decía cuánto facturó la flota, nunca cuánto ganó. `lib/freight-costs.ts` (fuente única: `EXPENSE_TYPES` FUEL/TOLL/PERDIEM/MAINTENANCE — `type` es String en BD, sin migración), gasto sin monto rechazado, y el resumen gana `costs`/`totalMargin`/`realKm`/`costPerKm` + costo por conductor y vehículo; tarjeta "Margen real" en rojo cuando la operación pierde plata. **(3) Tiempos y cumplimiento** — `startedAt` y `promisedAt` (migración `20260804230000_add_freight_times`): sin salida real la espera en bodega quedaba escondida dentro del viaje, y sin fecha comprometida "llegó tarde" es una opinión. `lib/freight-times.ts` deriva toAccept/wait/transit/total + `onTimeStats` (solo cuenta los que TENÍAN promesa: incluir el resto inflaría el indicador); ETA real con Routes API en el panel de recorrido (a demanda, no por fila); la app cliente pregunta "¿para cuándo la necesitas?". **(4) Rendimiento km/galón** — el dato ya se capturaba y nadie lo usaba: `lib/fuel-efficiency.ts`, método tanque a tanque, descartando tramos con odómetro que retrocede o imposible (un cero de más arruinaría el mes). **(5) Fletes en la liquidación** — `/operator/trips` y su CSV cubrían viaje/intermunicipal/mandado/pedido pero **no fletes**: para una empresa de carga el reporte con el que cierra el mes dejaba fuera su negocio entero; ahora se fusionan y el CSV trae columna `Gastos_COP`. Verificado: backend typecheck + **93 tests** (50→93), web tsc + next build, y **E2E contra PostgreSQL 16 + PostGIS real** (ciclo completo del flete: rastro 7 puntos/59,9 km con antirrebote comprobado, gastos 1.044.000, margen, 12 km/gal, 100 % a tiempo, flete en reporte y CSV, purga por retención) más **migración sobre datos previos** (fila vieja intacta, columnas nulas) y **comportamiento con datos legados** (ceros honestos, sin inventar cumplimiento). Flutter lo confirma el CI.

También HECHO (Cuenta de cobro de carga — el documento que pedía el cliente): la captura del usuario («DETALLE DE ENTREGA CUENTA DE COBRO 066 · DETALLE VIAJES NACIONALES») no se podía representar: **un camión lleva mercancía de VARIOS clientes**, cada línea con su referencia, destino y fecha de entrega, y el modelo asumía flete = un cliente = un destino. Confirmado por el usuario ("sí, y diferente tipo de mercancía"). Nuevo `CargoTrip` (migración `20260805060000_add_cargo_trip_cobro`) que agrupa remitos: `FreightManifest` YA era exactamente una línea (referencia, destinatario, ciudad, bultos, medida), así que solo se le añadió `cargoTripId` + `deliveredOn` y no se duplicó nada. El **peso va en el viaje, no en la línea** — deducido de la aritmética del documento: en los siete viajes que lo traen da 69–78 kg por rollo de forma consistente, y por línea daría 285. `cargo-trip.service` (consecutivo por empresa, líneas como remitos, despachar sella conductor/placa, un viaje de carga vacío no despacha pero el urbano sí) y `cobro.service` (`CobroAccount` numerada por empresa, `fillCobroFromPeriod` con toma atómica `cobroId: null`, emitir sella, anular libera para refacturar, `cobroToCsv`). Reglas que sostienen el documento como prueba: **un viaje solo puede estar en UNA cuenta** (si no, dos cuentas cobrarían lo mismo) y **un viaje facturado no se edita**. Portal: `CargoTripsManager` (crear viaje, añadir líneas con las medidas al dictado) y `CobrosManager` en la sección Carga, más **`/empresa/cobro/[id]`** que reproduce el formato en papel para imprimir o guardar como PDF —número de viaje y origen solo en la primera línea, peso una sola vez en la última, `URBANO` en los acarreos—. Verificado: backend typecheck + **100 tests** (93→100, con `cobro-csv.test.ts` sobre los datos reales del papel) y **E2E contra PostgreSQL real** reproduciendo los viajes 08 (tres destinatarios, 131 rollos), 14 (cinco referencias, 103 rollos) y el urbano: 234 rollos, 18.000 kg, 8 líneas, y todos los guards (número duplicado, robo de viaje entre cuentas, emitir vacía, doble emisión, editar facturado). ~~PENDIENTE del dashboard analítico~~ **YA NO: las seis piezas que listaba aquí se hicieron después** (serie por día, comparativa, espera de aceptación y ETA medio en la tanda de mapas oscuros; ETA en vivo en `eta_vivo.dart` y paradas urbanas en la tanda de viajes encadenados). Esta nota quedó obsoleta y se creyó vigente al volver a ella — **al retomar un PENDIENTE, medir primero si sigue siéndolo**.

También HECHO (Pagos de la cuenta de cobro — anticipo, abono parcial y saldo): el usuario pidió la opción de pago «parcial como anticipo o el total». Hueco de fondo: **el modelo no tenía ningún importe** — el viaje llevaba rollos, metros y peso, pero ni un peso, así que no había contra qué abonar. `CargoTrip.freightAmount` (valor del flete de ESE viaje; el total de la cuenta es la suma, así el importe queda respaldado por su detalle y se audita viaje por viaje) y `CobroPayment` (migración `20260805090000_add_cobro_payments`: monto, `PaymentKind` ANTICIPO|ABONO|SALDO, medio, comprobante, foto del soporte, `paidAt`, `voidedAt`). **El saldo se DERIVA siempre** (`lib/cobro-balance.ts`, puro y testeado): total − pagos vigentes, nunca se guarda, porque un saldo guardado y unos pagos guardados acaban discrepando y nadie sabe cuál miente. Guards: `assertPaymentAmount` rechaza el pago mayor al saldo **diciendo el saldo exacto** (un cero de más es el error típico y descuadra el cierre) salvo `allowOverpay` explícito; nada de pagos sobre cuenta sin valor ni anulada; un pago **no se edita ni se borra, se anula** (`voidedAt`) porque un recibo que cambia de monto después de entregado no prueba nada; redondeo a peso entero para no dejar saldos fantasma de céntimos. Se admite pagar sobre cuenta en BORRADOR además de emitida, porque el anticipo se cobra AL DESPACHAR, antes de que exista el documento. Portal: tarjetas Facturado/Pagado/Saldo + barra de progreso + lista de pagos + alta con «Pagar el saldo completo»; el impreso `/empresa/cobro/[id]` gana columna VALOR (una vez por viaje, como el peso) y un pie TOTAL − anticipos/abonos = SALDO PENDIENTE; el CSV gana `Valor_flete_COP` y pie de conciliación. Verificado: backend typecheck + **114 tests** (100→114) y **E2E contra PostgreSQL real**: anticipo 1.5M sobre 5M → 30 % y saldo 3.5M, abono + saldo → PAGADA, anular un pago devuelve el saldo dejando constancia, sobrepago deliberado → SOBREPAGADA con saldo a favor, y los rechazos (monto sobre saldo, cero, tipo desconocido, cuenta sin precio, cuenta anulada, doble anulación). **OJO — pendiente de confirmar con el usuario:** esto modela lo que el CLIENTE le paga a la EMPRESA por el flete. Si además necesitan **recaudo contra entrega** (que el conductor cobre al destinatario el valor de la mercancía y lo consigne), es otra pieza distinta y no está.

También HECHO (Unificación de carga — el viaje como unidad única): reporte del usuario «al ingresar, cuenta de cobro, fletes de carga y remitos de salida está todo separado». Correcto, y la causa era mía: se había construido un **segundo modelo de viaje** (`CargoTrip`) al lado del existente (`FreightRequest`) y la trazabilidad quedó repartida — rastro GPS, gastos, tiempos, rendimiento, alertas y finanzas colgaban del flete; remitos, cuenta de cobro y pagos colgaban del viaje. Medido antes de tocar: `cargoTrip` aparecía **cero veces** en `freight.service` (panel financiero) y **cero** en `operator.service` (reporte de liquidación), y el heartbeat solo reconocía `'trip'|'intercity'|'freight'` — un viaje de carga en ruta no dejaba rastro ni generaba alertas. **Unificado** (migración `20260805120000_unify_cargo_trip`): `CargoTrip` gana `startedAt`/`promisedAt`/`destCity` y la bitácora; `FreightEvent.freightId` pasa a nullable y gana `cargoTripId` (cuelga del viaje o del flete legado); `FreightRequest.cargoTripId` **único** enlaza la solicitud con el viaje que `_ensureCargoTripForFreight` crea al aceptarla (idempotente, best-effort, con la mercancía como primera línea); el ciclo del flete mueve el viaje (`_syncCargoTrip`, **await, no fire-and-forget** — es una transición de estado, no un aviso); `serviceKind 'cargo'` en el rastro y en `_activeServiceFor`. Finanzas: los viajes entran como servicio `CARGA` y **los fletes con viaje se excluyen** (`cargoTripId: null`) para no contar la plata dos veces; gastos, km, rendimiento y cumplimiento leen de ambos. **`GET /operator/cargo-trips/:id/report`** + página **`/empresa/viaje/[id]`**: el informe final tras cada viaje y entrega — mercancía y conciliación, recorrido con mapa, gastos desglosados, tiempos contra lo prometido, margen, costo por km y en qué cuenta se cobró. La sección Carga pasa a ser la lista de viajes; el tablero del marketplace y los remitos sueltos quedan plegados. **Dos bugs que encontró el E2E**: el DTO del viaje no exponía `startedAt`/`promisedAt`/`destCity` (el informe los mostraba vacíos), y la consulta de gastos del panel se saltaba entera cuando todos los fletes del período ya tenían viaje (`freightIds.length` en cero) — los gastos de los viajes nunca llegaban al panel. Verificado: backend typecheck + 114 tests, web tsc + next build, y **E2E contra PostgreSQL real** (aceptar crea viaje con su línea y su valor → iniciar despacha y sella salida → rastro de 3 puntos sobre el viaje → gastos en el viaje → completar cierra → informe con todo → panel sin doble conteo).

También HECHO (E2E del portal de empresa por HTTP + 4 huecos que destapó): recorrido completo con peticiones reales (registro → verificación del admin → login OTP→JWT → equipo con roles → afiliación → vehículo → viaje de carga → cuenta de cobro → pagos → liquidación → desafiliar), 61 comprobaciones incluido el aislamiento entre empresas (la vecina no ve vehículos, informes ni cuentas ajenas; no puede registrar vehículo a un conductor ajeno), los guards de rol (DISPATCHER no da accesos ni edita el perfil; nunca se queda la empresa sin OWNER) y que el conductor ve desde SU app exactamente la misma ficha que afilió el portal (mismo id, sin cuenta fantasma). **Cuatro defectos reales, todos del mismo tipo — el dato existía y no llegaba a quien decide con él:** (1) **el viaje de carga no estaba en `/operator/trips` ni en su CSV** — listaban viaje/intercity/mandado/pedido/flete pero no `CargoTrip`, así que una flota que crea sus viajes en el portal (el flujo del documento en papel, el principal) cerraba el mes con su negocio entero fuera del reporte; ahora entran como servicio `CARGA` con sus gastos de ruta y **los fletes que ya tienen viaje se excluyen** (`cargoTripId: null`) en el listado, el CSV, los `count` y los `aggregate` — mismo criterio que ya seguía `getFleetFinance`; (2) **despachar no sellaba `startedAt`** ⇒ espera en bodega y tiempo en ruta siempre en blanco en el informe (despachar ES la salida en el portal: no hay «aceptar» previo como en el flete del marketplace; se respeta el que ya traiga); (3) **`CreateCargoTripDTO` no aceptaba `promisedAt` ni `destCity`** ⇒ el cumplimiento de un viaje del portal no se podía calcular nunca y el destino salía vacío (campos nuevos en crear/editar + formulario de `CargoTripsManager`; el destino cae a la ciudad de la primera línea si no se declara); (4) **la flota no veía por qué su conductor no recibe nada** — `listOperatorDrivers` no traía `complianceStatus` ni `intercityEnabled`, así que con documentos vencidos el kill-switch lo sacaba del despacho y el portal seguía mostrando «verificado, en línea» sin una pista (chips «Docs vencidos» con el motivo, «Por vencer» e «Intermunicipal» en `DriversManager`; `CARGA` en los mapas de etiquetas de page/Finance/Analytics y `DRAFT`/`DISPATCHED` en los badges de estado). Verificado: backend typecheck + 161 tests, web tsc + next build, y **E2E contra PostgreSQL real** — el del portal completo y **el de la invariante de la fusión**: un flete del marketplace convertido en viaje aparece UNA vez en el listado, en el CSV, en el bruto y en el panel financiero (el listado sí lo duplicaba: se cazó con esa prueba).

También HECHO (Estética del seguimiento — la ficha del conductor y el mapa protagonista): el usuario pasó una captura de Uber BR pidiendo que los vehículos y la trazabilidad se vieran así. **El hallazgo no fue de diseño: la pantalla estaba desnutrida de datos.** Seis de los ocho elementos de esa tarjeta ya existían en nuestra BD y el backend no los mandaba, porque `client.service` hacía `` `${brand} ${model} • ${plate}` `` y enviaba UNA cadena — con eso la app no puede maquetar una placa en su recuadro ni teñir nada, solo imprimirla en gris. **(1)** `ClientTripDTO` gana la ficha suelta (`vehiclePlate/Color/Brand/Model/PhotoUrl`, `driverPhotoUrl/Rating/TotalTrips/Since/Verified`); `driverVehicle` se mantiene para las apps instaladas. **(2)** Tarjeta nueva: foto real del conductor con **iniciales** de respaldo (no un muñeco), calificación en insignia **solo si existe**, "Identidad verificada", "Conductor desde AAAA", y bloque del vehículo con su foto (la que la flota sube al registrarlo — mejor que licenciar un catálogo de renders como hace Uber), "Blanco Toyota Corolla" con el color primero y la **placa en recuadro** a 17 px con letras separadas. **(3)** El mapa deja de ser una tarjeta de 290 px dentro de un scroll y pasa a ocupar la pantalla, con `DraggableScrollableSheet` (.30/.52/.92 con snap) encima; el estado sube al primer lugar del panel porque es lo único visible plegado; ruta 4→6,5 px con `strokeCap/Join` redondeados; el origen pasa de gota a punto sólido (el pin con punta se reserva al destino); cerrar y SOS a botones circulares flotantes. `_TripMap` gana `expand` para conservar el modo tarjeta. **(4)** `lib/driver-card.ts` (backend) y `shared/widgets/driver_info_card.dart` + `shared/models/driver_card_info.dart` (app): **los cinco servicios** —viaje, envío, mandado, pedido, intermunicipal— usan la MISMA ficha y la MISMA tarjeta. `fichaPorConductor` para pedidos/mandados (guardan el nombre desnormalizado y solo tienen `driverId`) y `fichasPorConductores` en lote para los listados; en intermunicipal no hay consulta nueva (`_withDriverPosition` ya buscaba al conductor). `DriverCardPalette` viste la tarjeta de oscuro en el intermunicipal — **no se arrancó su tema nocturno**: es decisión de marca, no un descuido, y merece su propia tanda. **Botones muertos barridos**: `onTap: () {}` del teléfono en intermunicipal Y en mandados (ahora explican el contacto protegido, que es lo que hace el resto de la app), la tarjeta "Restaurantes" que no filtraba, y **fuera el teaser "ZIPA Fest · Domicilios desde $0 · Solo este mes"** — no era solo un botón sin cablear, prometía un descuento inexistente y no hay pantalla de promos. Se quedan dos `onTap` vacíos por correctos (pestaña activa; absorbe-toques de la oferta, ya comentado). **Regla de la tanda: nada se inventa cuando el dato falta** — sin foto van las iniciales, sin calificación no hay estrella, sin verificación no hay sello. Verificado: backend typecheck + 161 tests, **E2E contra PostgreSQL real** comprobando los nueve campos en los cuatro servicios y que con el viaje aún buscando la ficha viaja VACÍA; Flutter en CI (runs 373–379). **Dos fallos que solo cazó el CI** y que conviene recordar: `safeBack` con el fallback posicional, y un `export` sin su `import` (re-exporta la clase pero no la trae al ámbito del propio archivo). **PENDIENTE de QA visual del usuario**: proporciones y solapes solo se juzgan en un teléfono — sobre todo la altura inicial del panel sobre el mapa.

También HECHO (Sonido de solicitud propio del conductor): el usuario subió un MP3 pidiéndolo como aviso; era **literalmente el de Uber**, así que se retiró y se sintetizó uno propio. `tools/generar-sonidos.py` (solo biblioteca estándar: `wave` + síntesis a mano, sin grabación de nadie y sin licencia que respetar) genera tres opciones; el usuario eligió la **C** (`assets/sounds/trip_request.wav`, Do5→Mi5). Las primeras versiones salieron chillonas y tenía razón: usaban parciales **inarmónicos** (1,41×, como una campana de metal) en registro 880–1975 Hz, justo donde el oído es más sensible (Fletcher-Munson). Corregido con **armónicos enteros** (2×, 3×, 4×) entre 400 y 800 Hz, cada uno con su propia caída (el brillo se va antes que el cuerpo), ataque de 14 ms y rampa final que evita el chasquido al repetir. Todo está documentado en la cabecera del script — si hay que rehacerlo, las cuatro reglas están escritas.

También HECHO (Barra inferior igual en ambas apps + la burbuja que cortaba el nombre): pedido del usuario. `HomeShell` del cliente y el home del conductor comparten ahora la misma barra de vidrio (oscura `#1A1D27` al .66, altura 66). La burbuja del ítem activo era un **círculo de 54 px** y el nombre no le cabía; pasó a píldora (`Padding` 6/7 + `DecoratedBox` + `SizedBox.expand`), que se adapta al texto.

También HECHO (Términos visibles + combustible y peajes que declara quien maneja): dos cosas que el usuario marcó como fundamentales. (1) **Los términos se pueden leer desde dentro de las apps**: `legal_doc_screen.dart` en AMBAS (consulta `/legal/terms` y `/legal/privacy`, muestra versión y fecha de publicación, `SelectableText`, y estado de error con reintento) — antes el clickwrap del registro enlazaba a un texto que en la app no se abría. (2) **Costos declarados**: `sanearCostos()` y `getMaxFarePerSeat(origen, destino, puestos, costos)` en `config/constants.ts` aceptan combustible y peajes del conductor con tope de cordura (20.000 $/km, 2.000.000 de peajes) — las tarifas varían y el que maneja es quien las sabe. Eliminados los dos `TODO(legal/finanzas)`. **Corrección de un dato que di mal**: esas constantes alimentan la **tarifa sugerida del cupo compartido**, no el margen del panel de carga.

También HECHO (Barrido de estados pantalla por pantalla + la guarda que estaba en una puerta que nadie usa): (1) **`driver-online-guard.ts`** — las dos comprobaciones para ponerse en línea (KYC verificado y documentos vigentes) estaban escritas dentro de `PUT /driver/status`, y **ninguna app llama a esa ruta**: el conductor se conecta al abrir el WebSocket, donde la transición era un `updateMany` pelado. Las guardas estaban puestas en una puerta por la que nadie entra, y `KYC_ENFORCE` era un interruptor que no hacía nada. Ahora `motivoParaNoConectar(driverId)` decide en un solo sitio y la llaman los dos caminos; `handleDriverAuth` responde WS `driver_blocked` con código y motivo. (2) **Cancelar y confirmar dejan de fingir que salió bien**: tres métodos pasaron de `Future<void>` a `Future<String?>` devolviendo el motivo, y la pantalla muestra el error en vez de tragárselo — mismo patrón que el arreglo de los viajes compartidos. (3) Tres paneles del portal (`AlertsPanel`, `AnalyticsPanel`, `FinancePanel`) mostraban «todo bien» cuando la petición había **fallado**: ahora distinguen cargando / falló / vacío, que son tres cosas distintas. (4) Dos borrados del portal (`RoutesManager`, `CargoTripsManager`) no preguntaban nada.

También HECHO (Auditoría a fondo de los portales + el panel admin con prueba automática): **el panel de administración es HTML y JavaScript dentro de una cadena de TypeScript** (`PANEL_HTML`): ni `tsc` ni el linter lo miran, y solo se rompe delante de un administrador. Se ejercitó de verdad (DOM mínimo, `fetch` simulado, cada función de render llamada) y el primer barrido dio 233 «undefined» — que resultaron ser **todos de mi dato falso**, no del panel: devolvía la MISMA fila inventada a los 33 endpoints. Repetido modelando cada endpoint con la forma **real de su DTO** (`AdminMetrics`, `AdminDriverRow`, `AdminBusinessRow`, `AdminOperatorRow`, `AdminOperatorDocRow`, `AdminSosRow`, `MatchingDiagRow`, `AdminDocumentItem`, `TakedownDTO`, `AdminPayoutDTO`, `SupportTicketDTO`, `SafetyAlert`, `UploadsProbe`): **cero**. Punto ciego que el recuento de texto NO ve: `esc()` hace `String(s ?? '')`, así que un campo ausente sale como **hueco en blanco**, no como «undefined» — cazado con un Proxy que anota toda lectura de clave inexistente (verificado borrando campos a propósito: `blockedReason` solo aparece en la lista del Proxy). Único fallo real: `show(tab)` llamaba al cargador sin comprobar que existiera — añadir una pestaña y olvidar su cargador reventaba `show()` ENTERO, sin sección y sin mensaje. **`backend/src/routes/admin-panel.test.ts`** deja el comprobador en el repo: extrae el código de la cadena y verifica sintaxis, que todo botón llame a una función existente, que cada pestaña tenga sección y cargador, y que `esc()` siga escapando — las cuatro validadas rompiendo el panel de cuatro formas y comprobando que cada rotura cae. **REGLA: para verificar el panel hay que EJECUTARLO; leer su fuente engaña, ahí `\\'` dentro de una cadena es lo correcto.** Backend en **165 pruebas** (hoy 181).

También HECHO (Trazabilidad del conductor + alertas que sobreviven al reinicio): las **alertas de seguridad** vivían en un array en memoria de 200 y cada despliegue de Render las borraba — nueva tabla `safety_alerts` (migración `20260812120000_persist_safety_alerts`), `_pushAlert` escribe sin `await` (cuelga del heartbeat: un fallo de BD no puede frenar el GPS) y `listSafetyAlerts` es **async** y lee de ahí, con el anillo como respaldo; el `id` pasó de contador de proceso a cuid (`SafetyAlert.id` es **string**) y el orden desempata por `id` además de por fecha porque el barrido de conductores sin señal levanta varias en el mismo milisegundo; purga diaria por `SAFETY_ALERT_RETENTION_DAYS` (180). **`GET /operator/drivers/:id/track?from&to`** (`getDriverTrackForOperator` → `getDriverTrack` en track.service) da el recorrido del conductor a lo largo del día cruzando TODOS los servicios (urbano, intermunicipal, flete, carga; antes solo se podía ver flete por flete), con tramos por servicio y `gapMin` = minutos sin reportar con servicio abierto — la respuesta a "¿se desconectó?"; los km se suman POR TRAMO (encadenar dos servicios mediría en recta el salto que nadie grabó) y la **ventana del día la calcula el navegador** porque el día del operador empieza en su huso. Botón en `DriversManager` (prop `token` nueva) con `TrackMap`. Portal: **corregir bultos** de un remito en borrador (`PUT /operator/manifests/:id/items`, que existía sin pantalla) y `parsearMedidas` compartido — la coma entre dígitos ahora es DECIMAL (antes "124,5" creaba dos bultos) y lo que no se entiende se muestra en ámbar en vez de descartarse. App conductor: acta firmada al recibir un remito (`POST /driver/manifests/:id/receipt-photo`), subida DESPUÉS de que `/receive` cuaje.

También HECHO (Tres rutas retiradas — código muerto con agujero): **`POST /driver/documents/:type/review`** estaba marcada como "demo review action" y montada bajo el JWT del CONDUCTOR: con `{approve:true}` cualquiera se aprobaba sus propios documentos y `_syncIsVerified` lo ponía `isVerified` — a recibir pasajeros sin que un admin viera licencia ni SOAT, y de paso apagaba el kill-switch documental. **`PATCH /business/orders/:orderId/status`** aceptaba cualquier `orderId` con JWT de conductor sin comprobar pertenencia: cualquier conductor marcaba como entregado el pedido de otro y disparaba el WhatsApp al negocio. **`POST /business/orders`** igual de muerta (resto del portal de negocios que vivía dentro de la app del conductor). Con ellas se fueron `createOrder`/`updateOrderStatus`/`onOrderUpdate` de business.service. Detector en `/tmp/rutas-sin-boton.py` (ojo: la interpolación de plantilla da falsos positivos — comprueba prefijo Y cola).

También HECHO (Cuatro cosas que la app enseñaba y no eran ciertas — reportadas con capturas): (1) **ubicación falsa del conductor**: sin fix de GPS el latido enviaba el centro de Pamplona, así que salía parado en el obelisco para todos y PostGIS lo emparejaba con viajes del centro estando en otro sitio. Ahora `LocationService._sendLocationBatch` **no manda nada** sin lectura real (`hasRealFix`), `getCurrentLocation` lanza `LocationUnavailableException` en vez de devolver el parque, y el banner del home explica el silencio y se retira solo con `onFixChanged`. El filtro de frescura de 120 s lo saca del despacho, que es lo correcto. (2) **conductores intermunicipales inventados**: «Ramiro Sepúlveda · Nissan X-Trail Negra 2023 · OPS 876» es `MOCK_INTERCITY_DRIVERS[2]`; `INTERCITY_SIMULATE` **ya no se puede encender en producción** (se ignora y se avisa; el render.yaml dice "false" así que estaba puesta A MANO en el panel de Render). Visible en `/health` como `intercity: simulado|conductores-reales`. (3) **Wompi**: sin llaves se armaba el enlace con `pub_test_nexum_demo`, que no es de nadie → «No se pudo cargar la información del undefined». `createPaymentLink` ahora lanza `PaymentError`; las propinas piden el enlace ANTES de marcar `tipAmount`. (4) **el chat no era un problema de la foto**: `_assertParticipant` solo miraba `Trip`, así que en MANDADOS y PEDIDOS todo moría en 403 (historial, texto y foto) — ahora autoriza contra Trip, Errand u Order (`TripMessage.tripId` es texto sin FK, sin migración).

También HECHO (Fuera los datos inventados que quedaban): `getWeeklyHistory` tenía una **semana entera escrita a mano** (95.000, 87.000, 112.000, 78.000, 103.000, 138.000, 121.000) y `getDailyEarnings` caía a una lista en memoria; inalcanzables (van tras `authMiddleware`) pero eran un arma cargada — `driverId` es ahora **obligatorio** y lo exige el compilador. Los 16 `req.driverId ?? MOCK_DRIVER.id` de driver.routes eran peores: `MOCK_DRIVER.id` es un conductor REAL de los seeds, así que una ruta fuera del middleware habría operado sobre SU cuenta; sustituidos por un guard 401 explícito + `req.driverId!`, y borrados `MOCK_DRIVER`/`MOCK_PASSENGERS`/`MOCK_ROUTES`. **Revisados y dejados como están, porque son honestos**: `_mockDrivers` de intercity_provider (cliente, `if (!kDebugMode) return;` — sus nombres NO son los del backend), proveedores `fake` de OCR/antecedentes (env-gated con warning), modo mock de push, el ETA del pedido (sale de `Business.etaMinutes`, lo declara el dueño) y toda la cadena del SOS (devuelve `sin_canal` y ambas apps lo dicen). La bitácora del flete distinguía mal: un `catch (_) {}` hacía que un fallo de red se viera como «Aún no has registrado tanqueos».

También HECHO (Categorías de viaje — arranque por TAXI, el precio lo pone el servidor): el pasajero ya elige entre Taxi, Particular y Moto con precios cotizados por el backend. Al abrirlo salieron tres agujeros de fondo. **(1) El precio lo decidía el teléfono**: `POST /client/trips/request` guardaba `estimatedFare`/`distanceKm`/`etaMinutes` tal como llegaban — probado contra Postgres real, una petición pidiendo una carrera de $1 se guardaba como carrera de $1. Ahora el servidor mide el trayecto (`medirTrayecto`: Routes API, y línea recta × factor de calle si no hay llave, diciéndolo con `rutaReal:false`) y calcula; los números del cliente se descartan. **(2) Se cotizaba con una tabla y se cobraba con otra**: la liquidación usaba `calcFare` genérica sin mirar la categoría (cotizado $8.700 → cobrado $5.000). Nuevo `liquidarViaje(serviceType, km, min, surge)` en lib/fare.ts. **(3) Un viaje pedido como TAXI se le ofrecía a un particular** — lo cazó `e2e/taxi-extremo-a-extremo.ts` poniendo moto y particular MÁS CERCA que el taxi: la oferta se fue al particular a 31 m, porque el despacho tenía su propio mapa donde TAXI y PARTICULAR compartían lista mientras la pantalla contaba con la tabla nueva (podía decir "0 taxis cerca" y mandar el viaje a otro vehículo). `vehicleTypesForService` sale ya de `lib/tarifa-categoria.ts`: **una sola fuente**. **La tarifa del taxi NO la fija Nexum**, la fija el decreto municipal: se carga con `TAXI_BANDERAZO_COP` + `TAXI_POR_KM_COP` + `TAXI_CARRERA_MINIMA_COP` (+ opcional `TAXI_POR_MIN_COP`) y se exige el **juego completo** — media tarifa daría un número que no es ni el oficial ni el nuestro; sin cargar se usa la genérica y `/health` lo dice (`tarifaTaxi: generica|decreto-municipal`). Y **a la tarifa regulada no se le aplica multiplicador por demanda** (`admiteSurge:false`): subir un 50 % un precio de decreto es cobrar por encima de lo autorizado; la regla vive en el único sitio donde se calcula el precio. Otros detalles: carrera mínima como piso ANTES del redondeo, redondeo a $50 (el efectivo no tiene monedas de $7), "la más barata" que no se marca sin con qué comparar ni en empate y solo entre las disponibles. `GET /client/trips/options` devuelve las tres categorías con precio, ETA del vehículo más cercano de SU tipo y cuántos hay; las que no tienen vehículo cerca van **apagadas, no escondidas** (si desaparecieran el selector cambiaría de tamaño solo), y si no queda ninguna el botón lo dice en vez de pedir que se elija algo que no está. App cliente: el enum solo tiene tres valores y "transporte" se mandaba como `particular`, así que **no se podía pedir un taxi** — ahora la categoría viaja aparte (`request(categoria:)`) y manda sobre la pestaña; el precio/distancia/tiempo que guarda la app son los del servidor (la estimación local y el `_random.nextDouble()` de la distancia quedan solo de respaldo para backend viejo); cambiar el destino tras elegir resincroniza la elección (antes el botón habría mostrado el precio del trayecto anterior). Verificado: typecheck + 229 pruebas (19 nuevas, las tres reglas comprobadas rompiéndolas) y **cinco E2E contra PostgreSQL real**; CI verde incluido el APK del cliente. **DECISIÓN PENDIENTE del usuario:** un viaje pedido como PARTICULAR ya no se le ofrece a los taxis (antes sí) — coherente con que la categoría signifique algo, pero reduce la oferta; se reabre cambiando `tiposVehiculo` de PARTICULAR.

También HECHO (Frontend estilo Uber — mapa protagonista, hoja arrastrable y el pago ANTES de confirmar): la pantalla de pedir era una lista de campos con el mapa en ninguna parte; se elegía taxi o moto sin ver el trayecto. Ahora, en viajes de pasajero: **mapa a pantalla completa** (`RoutePreviewMap` nuevo — el de seguimiento no sirve: se construye sobre un viaje que ya existe) con recogida, destino y ruta real por calles; con un solo punto también pinta, y **sin punto de recogida NO se centra en ningún sitio "por defecto"** (un mapa apuntando a un lugar que nadie eligió es peor que no tener mapa); **hoja arrastrable** (.28/.56/.92 con snap) con direcciones, categorías, pago y botón, que arranca casi entera mientras faltan puntos y se recoge sola al haber trayecto. Los ENVÍOS conservan el formulario (llevan datos del destinatario y no eligen categoría). **El método de pago se elige ANTES de confirmar** — era un defecto de orden, no de estética: la hoja de pago salía DESPUÉS de crear el viaje, así que quien la cerraba se quedaba con el viaje ya buscando conductor y sin método decidido, sin forma de volver a preguntarle; ahora es una fila de la hoja y la elección se recuerda entre viajes (`metodoPagoProvider`, SharedPreferences; sin llaves de Wompi no se ofrece elegir). Dos trampas resueltas a propósito: **el teclado ya no tapa el campo** (la hoja sube al 92 % y el alto del teclado se suma al margen — el mismo fallo que el usuario reportó en pedidos) y **`initialChildSize` solo se lee en el primer build**, así que la hoja no habría bajado NUNCA al resolverse el destino (se anima a mano en `_sincronizarConTrayecto`). De paso: fuera la llamada a Google Directions que hacía el teléfono al tocar "Pedir" (el servidor mide el trayecto y descarta lo que mande la app: solo añadía espera de red) y fuera `_PaymentSheet`/`_PayChoice` (118 líneas muertas). **App conductor:** la insignia de la oferta mostraba `workMode.displayName` —el modo del propio conductor— cuando el `serviceType` de la solicitud SÍ llegaba y no lo miraba nadie; ahora dice "Taxi"/"Particular"/"Moto"/"Envío" y en taxi añade "Tarifa autorizada" (`servicioEtiqueta`/`tarifaRegulada` en TripRequestEntity). **DECISIÓN a confirmar:** entrar por la pestaña "Transporte" **preselecciona TAXI** (moto preselecciona MOTO); se cambia en `_preseleccion` de transport_booking_screen. Verificado: CI verde (`flutter analyze` de ambas apps) en 049d653.

También HECHO (Modo oscuro: ya estaba encendido y las notas decían lo contrario): auditado antes de tocar nada, y el resultado contradice al propio CLAUDE.md. `ThemeNotifier` YA persiste en SharedPreferences, `AppTheme.darkTheme()` existe, `app.dart` pasa `themeMode` y los dos interruptores (cliente `account_screen._DarkModeTile`, conductor `settings_screen` «Apariencia») YA llaman a `setDark`. Las notas de las Tandas 15 y 38 que lo daban por deshabilitado estaban obsoletas — y por fiarme de ellas le dije al usuario que no funcionaba. **La cifra de «236 fondos hardcodeados» también era falsa**: contaba todos los `Colors.white`, y la mayoría son iconos y texto SOBRE botones de color, o blancos translúcidos sobre degradados y mapas, correctos en los dos temas. Medido: 361 usos adaptativos (`context.*Color`) en el cliente y 359 en el conductor; los tokens `AppColors.*Light` fijos solo viven en los archivos de tema y en el home del conductor (mapa, claro a propósito). El barrido buscó los DOS patrones que rompen —fondo fijo blanco con texto adaptativo, y superficie adaptativa con texto fijo oscuro— y dio **un solo error real**: la píldora de estado de `active_trip_screen` del conductor (`Colors.white` fijo + `context.textPrimaryColor`), blanco sobre blanco en oscuro, y es la que el conductor mira durante todo el viaje. Corregida a `AppColors.textPrimary` fijo, la regla de lo que flota sobre el mapa. Los demás candidatos eran sombras (`Colors.black.withValues`), no texto. **REGLA: para juzgar contraste hay que buscar la CO-OCURRENCIA de fondo fijo con texto adaptativo (o al revés) en el mismo subárbol; contar `Colors.white` a secas no dice nada.**

También HECHO (Las tres cifras del piloto + la comisión deja de ser constante + el panel por plaza): tres tandas seguidas para poder vender y para saber si el negocio existe. **(1) Métricas de negocio** (`GET /admin/metrics/negocio?dias=N`, bloque nuevo en la pestaña Métricas): viajes pedidos/completados por día, tasa de emparejamiento y retención semanal. Las reglas viven sueltas en `lib/metricas-negocio.ts` porque son donde una métrica miente sin que se note — **sin denominador no hay porcentaje** (un «0 % encuentran conductor» sin solicitudes acusa al despacho de algo que no hizo ⇒ `null` y «No hubo solicitudes»), **los días vacíos se dibujan** (agrupar por día en SQL solo devuelve los días con datos: pintar eso hace que una semana con dos días muertos parezca entera), y **una retención sobre base menor de diez enseña la fracción («1 de 2») y se calla el porcentaje**. **(2) Comisión por flota y por ciudad** (`lib/comision.ts`, `comision.service.ts`; `Municipality.commissionRate` nuevo, `Operator.commissionRate` era columna MUERTA con cero referencias): precedencia **flota → ciudad → global**, resuelta al LIQUIDAR en los cinco sitios que sellan dinero y guardada ahí — renegociar mañana no reescribe lo que se pagó ayer. Una flota con comisión CERO **no hereda** (el caso que un `||` rompería); `saneaTasa` acepta «12» y «0.12» y RECHAZA por encima del 40 % (quien escribe 15 queriendo decir 15 % fijaría un 1500 %); panel con columna «Comisión» + botón por empresa y tarjeta «Comisión por ciudad» (`upsertMunicipality` no tenía ruta: la de ciudad habría nacido inconfigurable). **(3) Panel por plaza**: `Trip.citySlug` y `Driver.citySlug` (migración `20260908170000_city_scope`, con relleno de lo existente), sellado al crear el viaje (los 3 sitios) y refrescado en el latido, con el MISMO criterio que la app (`plazaDeCoordenadas`, centroide más cercano ≤40 km) — si fueran dos criterios, la operación y el tablero dirían cosas distintas del mismo viaje; `?ciudad=` en `/admin/metrics`, `/metrics/negocio` y `/drivers` + selector en la cabecera. **Lo que no sabe de plazas va en `null`, nunca en cero** (pagos, SOS, mandados/pedidos/intermunicipales atascados) y el panel escribe «—»; «usuarios» pasa a ser «pasajeros con viajes aquí». **Alcance del admin**: `ADMIN_PHONES` acepta `+57300...:cucuta`; la ciudad se relee de la configuración en cada petición (no del token) y **manda sobre la URL** — un desplegable no es una restricción si basta con editar la dirección. **OJO, el límite es PARCIAL y está documentado como tal en `docs/PILOTO.md`**: filtra métricas, cifras del piloto y lista de conductores; el resto del panel (empresas, clientes, negocios, documentos, SOS, soporte, retiros, promos) sigue siendo global y las acciones por id no comprueban la ciudad de la ficha. Sirve para que quien lleva una plaza mire lo suyo, NO para dar acceso a alguien externo. `GET /admin/me` y `GET /admin/metrics/negocio.csv` (separador `;` y coma decimal: con `,` el Excel en español apelotona todo en la primera columna) + **`docs/PILOTO.md`** (qué mirar, la rutina de los lunes, y los cortes de decisión acordados ANTES de ver los números). Verificado: typecheck + **345 pruebas** (de 300; incluidas 12 que EJECUTAN el panel y 10 del alcance del admin, todas comprobadas rompiendo lo que vigilan) y tres E2E contra PostgreSQL real (`metricas-negocio.ts`, `comision.ts`, `plaza-admin.ts`), más el relleno de la migración probado sobre filas anteriores. **OJO: un backtick dentro del JS del panel CIERRA la plantilla de TypeScript** — un comentario con `<a href>` entre comillas invertidas rompió la compilación entera.

También HECHO (Navegación del conductor + la vitrina de pedidos: descuento, promoción y «lo más pedido»): **(A) El conductor navega.** NO había botón: leía la dirección en la pantalla y la escribía a mano en Waze, conduciendo. Y en Ajustes había un selector de «aplicación de mapas» que era decoración pura — `_selectedMapApp` era estado local de esa pantalla, ni llegaba a SharedPreferences, y NADIE lo leía. `AppTransport/lib/shared/services/navegacion_externa.dart` + pastilla «Navegar a recogida/al destino» flotando sobre el mapa del viaje activo (flotando y no en la tarjeta porque quien la busca va conduciendo); el destino sale del MISMO criterio que traza la ruta. Es TRASPASO a Waze/Google Maps, no navegación paso a paso (eso pide el Navigation SDK de Google, contrato aparte con costo por conductor). **Dos trampas que lo dejarían inservible en silencio**: desde Android 11 una app no puede ni PREGUNTAR si otra está instalada sin `<queries>` en el manifiesto (sin eso `canLaunchUrl('waze://…')` da false con Waze instalado) — declarados también en `LSApplicationQueriesSchemes` de iOS —, y detrás de cada esquema va SIEMPRE una URL `https` de respaldo. 5 pruebas sobre la construcción de URL: el punto decimal (con la coma de es-CO «7,3754» parte la coordenada en dos parámetros), la salida web, que iOS no reciba el intent de Android, y que (0,0) —el Golfo de Guinea, o sea «falta el dato»— NO ofrezca el botón. **(B) Selector de categorías**: ya tenía asientos, hora, precio, «la más barata» y la apagada-no-escondida; le faltaba lo que más se nota — el vehículo era `Icons.directions_car`. Ahora se dibuja el vehículo real reusando `VehicleTopDownPainter` (VECTOR pintado a mano, cero assets nuevos, cero licencias), girado 24° porque de frente un coche cenital se lee como una mancha. **(C) La vitrina de pedidos** (`lib/vitrina.ts`, 23 pruebas; migración `20260908220000_vitrina`): `Product.compareAtPrice` (precio tachado + `descuentoPct` calculado UNA vez en el servidor, para que el «-46 %» del listado no discrepe del detalle), `Business.promoMinAmount/promoDiscount` (banner «$6.000 OFF, agrega $30.000» con barra de progreso) y **«lo más pedido» derivado de OrderLine de pedidos DELIVERED**. Las tres reglas que impiden mentir: **un «antes» inflado se RECHAZA al guardar** (por encima del 90 % es precio engañoso y lo sanciona la SIC; al revés se rechaza diciendo los dos números), **el banner y la caja usan la MISMA función** `promoDeTienda` —una pantalla que promete lo que el cobro no aplica es la peor queja posible— y **sin 15 unidades vendidas no hay ranking** (igual que la retención sobre dos personas). El descuento se resta del subtotal, NUNCA del domicilio (es el pago del repartidor), y se SELLA en `Order.promoDiscount`: cambiar la promoción mañana no reescribe lo cobrado hoy. App cliente: `precio_producto.dart` con las tres piezas compartidas (precio+tachado, «#N más pedido», banner) y secciones derivadas «Lo más pedido» y «Descuentos» que se arman solas y **solo aparecen si tienen contenido**. Portal: campo «Antes» en el catálogo con el error del backend visible, y la promoción en Ajustes. **DOS TRAMPAS COLOMBIANAS que cazaron las pruebas**: el punto de «$30.000» es separador de MILES —leerlo como decimal convertía treinta mil pesos en treinta—, y **un spread NO pasa por el control de propiedades sobrantes de TypeScript**, así que las claves en español de `saneaPromoTienda` llegaron hasta Prisma sin que tsc dijera nada (lo cazó el E2E). Verificado: typecheck + **374 pruebas**, web tsc + next build, y `e2e/vitrina.ts` contra PostgreSQL real (22 comprobaciones; el vínculo banner↔caja y el umbral del ranking comprobados rompiéndolos). **YA EXISTÍA y no hizo falta tocarlo**: opciones/variantes del producto con precio (`OptionGroup`+`ProductOption`), instrucciones especiales, portada y galería, fila Entrega/Envío/Calificación, secciones por categoría y buscador. **Descartado a propósito**: «Ad»/patrocinados (es un negocio de publicidad, no una función) y «Oferta Exclusiva Pro» (suscripción con cobro recurrente, bloqueada además por no haber Wompi en producción).

También HECHO (El portal del negocio manda de verdad: horario, pausa, vigencia de la promoción y una calificación que existe): el usuario pidió que cada local publique sus promociones «y demás» sin que nosotros toquemos nada. La auditoría no encontró funciones que faltaran, encontró **tres defectos** — cosas que la app ENSEÑABA sin que fueran ciertas. **(1) `Business.rating` era `@default(5.0)` y NADIE lo escribía**: cada local de la app llevaba un cinco que nadie le había dado, y la app cliente tenía la hoja de estrellas… cuyo `rateOrder` solo tocaba el estado en memoria y nunca salía del teléfono (comprobado: no existía ninguna ruta que recibiera una calificación de pedido). Ahora `POST /client/orders/:id/rate` (solo el dueño del pedido, solo ENTREGADO, corregible) y el promedio **se RECALCULA de las filas**, nunca se suma encima; `rating` pasó a `Float?` con `ratingCount` (migración `20260909020000_horario_y_reputacion`, que además hace `UPDATE businesses SET rating = NULL WHERE ratingCount = 0` — el 5,0 heredado se borra), y la app dice **«Nuevo»** en vez de un número, con el conteo al lado cuando lo hay («4,8 (23)»: un 4,9 con dos votos y otro con doscientos no son la misma información). **OJO Flutter**: el parser era `(j['rating'] as num).toDouble()` y con el null habría reventado la lista entera. El dueño ve su nota, la distribución y los comentarios en su portal (`GET /business/:token/reviews`) — una nota sin los comentarios es un castigo sin explicación. **(2) `openingHours` era texto libre que no cerraba nada**: solo se cerraba con el interruptor manual, así que el dueño que se iba a dormir sin apagarlo recibía un pedido a las 3 de la mañana, cobrado, para una cocina apagada — y eso no lo paga el local, lo paga la plataforma. `lib/horario-tienda.ts` (21 pruebas) con horario estructurado por día (`Business.hours` JSON, editor de casillas y horas en el portal), **franjas que cruzan la medianoche** (el bar de 18:00 a 02:00 cuenta en los dos días) y la hora de **Colombia**, no la del servidor (a las 02:00 UTC del martes aquí son las 21:00 del lunes). **Sin horario declarado la tienda queda ABIERTA**: cerrar de golpe a todos los negocios ya registrados al desplegar sería un apagón, no una mejora. **(3) La promoción no se podía acotar en el tiempo**, así que un «solo este fin de semana» seguiría puesto en marzo: `promoFrom`/`promoUntil`, y **vencida ni se anuncia ni se cobra** (el E2E comprueba las dos mitades). Nuevo también: **pausa rápida** (15/30/60/120 min con motivo) que **se levanta sola** — la cocina copada quiere parar veinte minutos, y si eso exige acordarse de volver a encender se queda cerrada media tarde; más de 24 h se rechaza porque eso es cerrar, no pausar. La pieza que sostiene todo esto es **`tiendaRecibiendo()`, una sola función** que deciden juntos la vitrina, el detalle y **la caja**: si la pantalla y el cobro lo decidieran por separado, el cliente vería «cerrado» y aun así podría pedir. El error del checkout ahora dice el motivo concreto («… no está recibiendo pedidos: abre mañana a las 08:00»), y el portal le enseña al dueño **el estado que ve el cliente**, no el del interruptor. **Defecto de la tanda anterior corregido de paso: el botón «Guardar cambios» del portal no enviaba la promoción** — se escribían los dos números, se pulsaba guardar y no pasaba nada. Verificado: typecheck + **404 pruebas** (de 374), web tsc + next build, y `e2e/horario-y-reputacion.ts` contra PostgreSQL real (37 comprobaciones), con la vigencia de la promoción y la guarda del checkout comprobadas ROMPIÉNDOLAS. Flutter lo confirma el CI.

También HECHO (Push del viaje urbano + la calificación del conductor, que era el mismo defecto de los negocios): enfoque en taxi y urbano. **Corrección de un diagnóstico mío**: dije que sin push el conductor no se entera de nada con el teléfono en el bolsillo, y era inexacto — el rastreo corre en un **foreground service real** (geolocator con `ForegroundNotificationConfig` y wakelock, `location_service.dart:146`), así que con la app en segundo plano el proceso y el WebSocket siguen vivos y las ofertas llegan; el problema es solo con la app **cerrada**. **(A) Avisos que no existían** (`lib/avisos-viaje.ts`, 10 pruebas): «Tu conductor llegó» —en un taxi es LA notificación, el carro está en la puerta y el pasajero que cerró la app se enteraba cuando lo llamaban—, viaje iniciado, viaje completado **con el total** (lo primero que quiere saber quien paga en efectivo), «no encontramos conductor» **diciendo que no se cobró** (antes solo salía por WS: quien cerró la app volvía y encontraba el viaje cancelado sin explicación), y **la cancelación del pasajero AL CONDUCTOR por push** (iba conduciendo con la pantalla apagada; mandados y pedidos ya avisaban así, el viaje no). La tabla de qué estados avisan vive suelta porque es decisión de producto: se CALLA en `accepted` (ya lo avisa el matching, sonaría dos veces) y en `arriving` (cambia varias veces por trayecto — es el push que enseña a silenciar la app). Los pushes cuelgan de la misma guarda `count > 0` que ya evitaba repetir estados. **(B) Tocar la notificación no llevaba a ninguna parte**: ninguna de las dos apps escuchaba `onMessageOpenedApp` ni `getInitialMessage`. `push_routing.dart` en ambas (con pruebas, que el CI ejecuta) mapea tipo→pantalla; lo desconocido devuelve null y NO mueve al usuario; sin `tripId` cae al home en vez de romper la ruta paramétrica. Hizo falta un `rootNavigatorKey` en los dos routers (al tocar una notificación no hay `BuildContext`), y con la app cerrada se navega en cuanto el árbol está montado. **(C) `Driver.rating` era `@default(5.0)` y NADIE lo escribía** — el mismo defecto que los negocios, en la cifra que alguien mira antes de subirse al carro de un desconocido; y `transport_provider.rateRequest` solo tocaba memoria, así que la nota moría en el teléfono. Ahora `POST /client/trips/:id/rate` (dueño del viaje, solo COMPLETED, corregible) reusando `lib/reputacion.ts`; `rating` a `Float?` + `ratingCount` (migración `20260910030000_calificacion_conductor`, que hace `UPDATE drivers SET rating = NULL WHERE ratingCount = 0`); **`Nexum Pro` dejó de apoyarse en el valor de fábrica** — el E2E lo demostró: con la lógica vieja, un conductor con CERO calificaciones y 300 servicios alcanzaba **ORO**, un nivel que exige 4,7 estrellas. La tabla **`Rating` era código muerto** (cero escrituras y cero lecturas en todo el backend): eliminada; las notas viven en `trips.rating`/`ratingComment`, que ya existían. El compilador señaló los ~14 sitios que asumían nota no nula, incluidos **dos que habrían reventado el login del conductor** (`auth_repository_impl` hacía `(d['rating'] as num).toDouble()`) y **el panel admin** (`d.rating.toFixed(2)` tumbaba la tabla entera de conductores; ahora hay 2 pruebas que EJECUTAN `loadDrivers`, comprobadas rompiéndolo). Todas las pantallas dicen «Nuevo» en vez de un número, y la de calificaciones del conductor no pinta cinco estrellas vacías (se leen como «cero», que no es «todavía nadie te ha calificado»). **(D)** `_cancelOnServer` tenía un `catch (_) {}` con el comentario «la cancelación local ya se aplicó a la UI» — o sea, el pasajero veía «cancelado» con el backend siguiendo el viaje y el conductor en camino; ahora devuelve el motivo y la pantalla lo muestra. Verificado: typecheck + **416 pruebas** (de 404), web tsc, y `e2e/calificacion-conductor.ts` contra PostgreSQL real (18 comprobaciones), con la guarda de Pro y la de «solo viajes terminados» comprobadas ROMPIÉNDOLAS. Flutter en CI.

También HECHO (Revisión antes de mergear — la calificación mentía en las DOS direcciones): al repasar mi propio trabajo apareció el reverso exacto del defecto que acababa de corregir. **`matching.service` tenía `rating: 5.0` ESCRITO A MANO** en la oferta, así que el conductor veía la misma nota perfecta de todo pasajero en la pantalla con la que decide si acepta la carrera; y `oferta_mapper` de la app repetía el 5,0 para el negocio de un pedido y para el cliente de un mandado. **Y la hoja con la que el conductor califica al pasajero —521 líneas con confeti— no mandaba nada a ninguna parte**: puntuaba, se cerraba, y la nota se perdía. Cerrado el ciclo en esa dirección también: `User.rating`/`ratingCount` (migración `20260910040000_reputacion_pasajero`; `Trip.passengerRating` YA existía sin que nadie lo escribiera), `rateTripPassenger` con las mismas guardas (viaje del conductor, solo COMPLETED) y `POST /driver/trips/:id/rate-passenger`; la hoja ahora envía —best-effort, sin retener al conductor que ya terminó— y la oferta enseña la nota REAL. `PassengerEntity.rating` a nullable (el parseo `(p['rating'] as num).toDouble()` habría reventado la oferta). **REGLA: cuando se corrige un dato inventado, buscar el mismo dato en la dirección contraria** — negocio→cliente y cliente→conductor estaban arreglados mientras conductor→pasajero seguía siendo una constante. Barrido final: cero `rating: 5.0` en backend y en las dos apps. E2E ampliado a 23 comprobaciones.

**⚠ EL PANEL ADMIN FALLA CERRADO — NO VUELVAS A LA LISTA DE RUTAS.** `requireAdmin` era `router.use([...15 prefijos], requireAdmin)`, o sea una lista de rutas A PROTEGER: **toda ruta nueva nacía PÚBLICA** y había que acordarse de apuntarla. No nos acordamos dos veces, y una fue grave — `POST /admin/municipalities/:slug/commission` dejaba a CUALQUIERA sin identificarse fijar la comisión de una plaza al 40 % (comprobado ejecutándolo: 200 y la tasa cambiada), y `/admin/diagnostics` llevaba un comentario diciendo que estaba protegida sin estarlo (hace escrituras en el bucket y llama a Twilio). Ahora es un `router.use` que cubre TODO menos `RUTAS_PUBLICAS` (`/`, `/auth/send-otp`, `/auth/verify-otp`), y `src/routes/admin-auth.test.ts` (3 pruebas) falla si alguien reintroduce la lista, si la lista de excepciones cambia, o si una ruta queda declarada por encima del guard. **Para comprobar autenticación hay que GOLPEAR las rutas sin token; leer el código no basta** — la lista de prefijos parecía completa.

**⚠ HAY DOS DOCKERFILES Y SOLO UNO DESPLIEGA.** `/Dockerfile` es el que construye **Render** (`render.yaml` → `dockerfilePath: ./Dockerfile`, contexto la raíz, `COPY backend/ .`); `/backend/Dockerfile` lo usa **docker-compose** en local. Arreglar el de backend/ no cambia NADA en producción — pasó, y costó dos rondas enteras de «sigue el error»: el arranque corregido (marca de fallo, escotilla, recuperación automática) se escribió en el de backend/ mientras Render seguía con el viejo `migrate deploy || echo WARN`, así que el servidor arrancaba con el esquema viejo y el error de Prisma le salía al pasajero al pedir un viaje. Ahora `src/lib/dockerfiles.test.ts` (9 pruebas) compara el CMD de los dos y falla si divergen — comprobado devolviendo el raíz a su versión vieja: 4 fallos. También verifica que el script de recuperación viva en `prisma/` (que es lo que el runner copia) y que `render.yaml` siga apuntando al Dockerfile de la raíz, porque si eso cambiara la comparación miraría el fichero equivocado.

También HECHO (La migración que rompió producción: SQL contaminado + un fallo que se tragaba solo): el pasajero recibía «The column `paymentMethod` does not exist in the current database» al pedir un viaje. Causa exacta, y era mía: generé la migración con `> migration.sql 2>&1`, y el aviso de actualización de npm quedó escrito **dentro del SQL**. Prisma envuelve cada migración en una transacción, así que el `ALTER TABLE` se revirtió entero y la fila quedó marcada como FALLIDA en `_prisma_migrations`. El `CMD` del Dockerfile hacía `migrate deploy || echo WARN`, así que Render mostró «Deploy live» en verde y el servidor arrancó con el código nuevo contra la tabla vieja: el fallo solo se vio cuando alguien pidió un viaje. **Tres capas arregladas.** (1) El SQL, limpio. (2) `src/lib/migraciones.test.ts` (5 pruebas) revisa TODOS los ficheros de migración buscando salida de herramientas metida dentro, ficheros vacíos, sin `;`, y marcas de tiempo desordenadas o repetidas — comprobado reintroduciendo la basura exacta que rompió producción, y la caza. Hacía falta porque un `.sql` no lo compila nadie: ni tsc ni el linter lo miran, y Prisma solo se entera contra la base real. (3) El arranque ya no oculta el fallo: sigue levantando el servidor (quedarse muerto es peor) pero deja una marca que **`/health` publica como `migraciones: fallaron`** y que el pie del panel `/admin` muestra como «⚠ MIGRACIONES FALLARON». **Recuperación AUTOMÁTICA** (`prisma/recuperar-migraciones.mjs`, llamado por el CMD solo tras un fallo): consulta `_prisma_migrations` por filas con `finished_at IS NULL`, las marca como revertidas y reintenta el despliegue. Es seguro porque PostgreSQL ejecuta DDL en transacción y Prisma envuelve cada fichero en una, así que una migración fallida revirtió ENTERA — el script verifica esa premisa y se planta si alguna migración usa `CONCURRENTLY`/`VACUUM`/etc., donde dejaría de ser cierta. Probado contra el estado exacto de producción (columna ausente + fila fallida): P3009 → desatasca → aplica → «migraciones al día». Queda además la escotilla manual **`PRISMA_RESOLVE_ROLLED_BACK`**: una migración fallida bloquea TODAS las siguientes con P3009, y salir de ahí pide una consola que Render no siempre da; poniendo el nombre de la migración en esa variable de entorno, el arranque la marca como revertida y continúa (se quita después). Verificado contra PostgreSQL real: la migración corregida aplica y crea la columna; el P3009 se reprodujo insertando una fila fallida a mano y `migrate resolve --rolled-back` lo desatasca. E2E nuevo `e2e/metodo-de-pago.ts` (guarda el método, descarta uno inventado, normaliza mayúsculas, y una app vieja sin el campo sigue funcionando). **REGLA: nunca generar un fichero con `> archivo 2>&1`; la salida de error acaba dentro del artefacto.**

También HECHO (Mapas oscuros, ganancias del servidor, torre analítica, ETA en vivo y paradas — PR #137): **Mapas.** Estilo oscuro tipo Uber en los ocho mapas de las dos apps y en la torre; el estilo vive en UN sitio (`backend/src/config/estilo-mapa.ts`, array `styles` que acepta `createSession` de Map Tiles) con `estilo-mapa.test.ts` (9 pruebas de legibilidad: el suelo es `#1f2429` y no negro, la vía siempre más clara que el suelo en los tres niveles, el agua más oscura, las etiquetas nunca apagadas). **El fallo que lo hacía inerte**: `/geo/tile-ticket` entregaba pase aunque no hubiera `GOOGLE_MAPS_API_KEY`, así que la app tomaba la rama de Google y caía **tesela a tesela** a OSM sin filtro — el mapa salía claro pese a todo el trabajo. Sin llave ya no se entrega pase (`isGeoConfigured()` en geo.routes) y se usa la rama de respaldo entera. Los vehículos son las ilustraciones cenitales (`tools/procesar-vehiculos.py`, clave por tono magenta, 3 densidades × 2 apps) con `_Halo` para que el coche azul marino se lea sobre el mapa oscuro. **Dinero.** Ganancias muestra el neto y la comisión **que liquidó el servidor** — la pantalla los recalculaba con `FareCalculator` y una tasa escrita dentro, el mismo fallo ya corregido en el resumen del viaje; regla única: comisión = bruto − neto, los dos del servidor (`EarningsBucket` gana `netEarnings`/`commission`, los widgets dejan de derivar). `topeDeCordura(km, min)` en `lib/fare.ts`, derivado de la propia tabla de tarifas × 3, aplicado en `calcFare` y en la liquidación — reproducido con la carrera de 89.887 $ que lo destapó. **Torre.** Serie por día, comparativa contra el período anterior de la MISMA longitud, espera media de aceptación y ETA medio; las cuatro reglas que hacen mentir a un tablero viven sueltas y probadas en `lib/analitica.ts` (12 pruebas): de cero a algo es `null` y no «+100 %», sin muestras es `null` y no cero, un negativo se descarta en vez de contarse como servicio instantáneo, y `rangoAnterior` cuenta los mismos días. El `FleetMap` había desaparecido: se ocultaba con `mapPoints.length > 0` en vez de `fleet.length > 0`. **Viaje.** `core/utils/eta_vivo.dart` en el cliente — el ETA se movía nunca (se calcula una vez al reservar); con el pasajero a bordo escala el ETA del servidor por el trecho que queda, así que hereda su medición sin duplicar constantes, y solo el tramo de recogida usa velocidad propia (espejo anotado del backend). **Paradas** en el viaje urbano: `Trip.stops` (migración `20260825120000_add_trip_stops`), `medirConParadas` suma por tramos en trip-options.service, máximo seis, y el conductor las ve **antes** de aceptar; E2E `e2e/paradas-viaje.ts` (11 comprobaciones) captura la oferta `trip_request` real de un conductor sembrado para probarlo — no basta con mirar el DTO del cliente. El historial del conductor decía «Moto» a todo: `_Servicio`/`_servicioDe` cubre los ocho tipos con `serviceType` que ahora manda `earnings.service`. **Tres errores de compilación míos dejaron el CI en rojo cuatro commits seguidos** y ninguno era visible aquí (no hay Flutter en el entorno): `const ''` no es sintaxis válida como valor por defecto, `library;` va ANTES de todo import, y `AddressAutocompleteField` exige `requiredField`. **REGLA: sin Flutter local, cada commit de Dart se da por bueno solo cuando el CI lo confirma; y antes de commitear, comprobar `git log --oneline -1` y el número de pruebas — el entorno se rebobinó seis veces y una tanda estuvo a punto de revertirse sola.** Lo de «Nexum Conductor» en el arranque **no era un fallo**: el manifiesto y el splash dicen ZIPA desde el rebrand; el usuario tenía un APK viejo.

También HECHO (Los cuatro fallos de la captura + viajes encadenados): **(1) El pasajero se quedaba «en trayecto» con el viaje ya terminado.** La app del conductor mandaba `completed` por WebSocket y nadie comprobaba que llegara: su `_send` hace `if (_channel == null) return;` —descarte en silencio— y `finishTrip()` del datasource es puramente local (`Future.delayed` + `FareCalculator`), así que el resumen salía igual. Cuando el mensaje se perdía no se liquidaba nada, el pasajero esperaba para siempre y el conductor quedaba `ON_TRIP`, o sea **fuera del despacho** (y reconectar no lo arregla: `noteDriverConnected` respeta ON_TRIP a propósito) — esa es la causa de «no recibe más solicitudes». Ahora `POST /driver/trips/:id/status` (con 3 reintentos ante fallo de red, `shared/services/trip_completion.dart`) y la app NO navega al resumen hasta que el servidor confirme; si rechaza, se enseña SU motivo y el viaje sigue abierto. La liquidación que devuelve va al mismo buzón del `trip_status_ack` (`publicarLiquidacion`) para que el resumen siga mostrando los números del servidor. La comprobación de propiedad + la liquidación viven en `driverUpdateTripStatus`, compartida por HTTP y socket. **Barrido**: `liberarConductorSiNoTieneMas` + un rescate que libera a quien lleve más de `STUCK_ON_TRIP_MIN` (45) minutos ON_TRIP sin latido; **no toca el viaje** —cerrarlo paga una tarifa que quizá no se ganó, cancelarlo niega un servicio que quizá sí se prestó— y lo cuenta en `AdminMetrics.orphaned` para que lo resuelva un humano con el «Liberar» que ya existía. **(2) El mapa trabado y a parches**: eran DOS `ColorFiltered` anidados = dos `saveLayer` a pantalla completa por frame; compuestos en una matriz (verificada color a color en `tools/matriz-mapa.py`). Y `retinaMode` pedía las teselas un zoom más arriba —**cuatro por cada una**— contra OpenStreetMap, cuya política prohíbe que una app distribuida tire de sus servidores; desactivado en esa rama (se mantiene con la llave de Google). **OSM no es viable en producción: es un parche hasta que haya `GOOGLE_MAPS_API_KEY`.** **(3) Tardaba en trazar el trayecto al aceptar**: `startTracking()` nunca llamaba a `Geolocator.getLastKnownPosition()`, así que en frío no había posición durante decenas de segundos y `_trazarTramo` se iba sin dibujar; ahora se siembra con esa lectura si tiene <10 min (real, no inventada) y si no hay ninguna se dice en pantalla (`_BuscandoSenalGps`). Además `_buildPolylines` se saltaba la guarda `_origenRealDeRuta` y dibujaba una recta naciendo en el centro de Pamplona. **(4) VIAJES ENCADENADOS** (lo pidió el usuario, tipo Uber/DiDi): `findNearestAvailableDrivers` admite `ON_TRIP` vía `_sqlEncadenado()` solo si el conductor lo tiene activado (`Driver.acceptsChained`, migración `20260828100000_add_driver_chained`, default true, interruptor «Enlazar viajes»), su viaje ya va `IN_PROGRESS` (con el pasajero a bordo), está a menos de `CHAIN_NEAR_DEST_M` (1500) de su destino y **no tiene ya otro aceptado** (uno en cola, no una pila). La oferta viaja marcada (`chained` → `kEsEncadenado` dentro del mapa) y en la app **no ocupa la pantalla**: `SiguienteViajeCard` es una franja arriba con tarifa, recogida y cuenta atrás, porque quien la mira va conduciendo. Al aceptar no se entra: queda en `siguienteViajeProvider` y el resumen ofrece «Siguiente viaje». El home IGNORA las encadenadas (sigue montado debajo y se las guardaría para plantárselas a pantalla completa al volver, ya caducadas). Los tres mapeadores salieron de `home_screen` a `shared/models/oferta_mapper.dart`. **Tres fallos MÍOS que cazó la autorrevisión posterior** y conviene recordar: `unawaited(beginTrip(...))` + `context.go('/active-trip')` es una carrera (esa pantalla redirige al inicio si el viaje activo está vacío al construirse) — hay que esperar; añadí un `notifyClientTripUpdateById` que **sobraba** (`updateClientTripStatus` ya avisa, y solo si `avance.count > 0`, que es deliberado para no repetir estados) y encima deshacía esa guarda; y abrir el despacho a los encadenados dejó `disponibilidadPorTipoVehiculo` contando distinto, cuando su propio comentario exige «EXACTAMENTE los mismos filtros que el despacho» — de ahí que la condición viva ahora en una función compartida. Verificado: typecheck + **278 pruebas** del backend (4 nuevas que EJECUTAN el panel con DOM falso, comprobadas rompiéndolo dos veces), y contra PostgreSQL real `e2e/cierre-viaje.ts` (15 comprobaciones) y `e2e/viajes-encadenados.ts` (10) — las guardas más delicadas (cercanía al destino, «uno en cola», frescura del latido) comprobadas QUITÁNDOLAS y viendo caer la prueba. **PENDIENTE conocido del encadenado**: el pasajero del viaje encadenado no ve que su conductor está terminando otro (Uber sí lo dice); y solo encadena detrás de un `Trip` — un repartidor terminando un PEDIDO no entra (los pedidos tienen `deliveryLat/Lng`, extenderlo es posible).

También HECHO (Red de seguridad de las apps + `.gitignore` asimétrico): al medir si se puede trabajar sin rompernos las apps salió el dato que importa — backend 591 pruebas, **conductor 57 casos para 35 pantallas y cliente 72 para 39**: la mitad del producto que el usuario toca iba sin red. **`copywith_completo_test.dart`** (las DOS apps) fija que el constructor de dentro de `copyWith` reciba algo para cada campo de la clase — es el bug que perdió el `deliveryPin` y dejó envíos sin poder cerrarse, y no lo caza nada (los parámetros son opcionales con nombre: ni compilador ni linter dicen una palabra). La regla NO es «todo campo debe ser parámetro»: una copia parcial es legítima. Las 35 clases con `copyWith` la cumplen hoy; comprobado reproduciendo el bug histórico. **OJO al tocar ese parser: `copyWith({...}){...}` tiene DOS bloques de llaves —parámetros y cuerpo— y agarrar el primero da 35 falsos positivos de 35.** **`oferta_mapper_test.dart`** (19 casos): la pantalla con la que el conductor decide si acepta llega cruda del WS sin tipos; se fija que la tarifa del mandado sea el servicio y no el presupuesto de la compra, que un pedido sin coords de entrega caiga AL NEGOCIO y no al centro de Pamplona, que una oferta malformada devuelva null en vez de tumbar el home, y que sin calificación no se invente nota. Conductor: 57 → **77 pruebas** en CI. **`.gitignore`**: el del conductor ignoraba `google-services.json`/keystore y el del cliente **ninguno**, y el `.plist` de iOS no lo cubría ninguno de los dos — una copia local para probar un build se habría commiteado. Corregido y verificado con `git check-ignore` en los cinco caminos, con el `.example` aún versionado. **Auditoría que NO dejó prueba, a propósito**: se cruzaron los 40 casteos sin guarda de las apps contra los 140 campos que el backend declara opcionales; de 15 sospechosos, **cero son bugs** (unos guardados con `!= null`, el resto obligatorios en su DTO concreto — `lat`/`lng` de conductores cercanos vienen de PostGIS con `geo IS NOT NULL`). Una regla por nombre marcaría los siete DTO donde `distanceKm` sí es obligatorio, y una prueba que falla sin motivo se desactiva.

**⚠ `main` YA ESTÁ PROTEGIDA — no se empuja directo, se mergea por PR.** El usuario puso el ruleset (id 23635676, comprobado en la API el 2026-09-25): **PR obligatorio** con cero aprobaciones, los tres checks por su nombre exacto — `Backend (typecheck + test)`, `App Conductor (analyze + test)`, `App Cliente (analyze + test)` —, «strict» (la rama al día antes de mergear), y prohibido borrar la rama o forzar. Un `git push origin main` se rechaza con `GH013: Changes must be made through a pull request`, así que **la última tanda de cada sesión termina abriendo un PR** (sigue valiendo la regla de no abrirlo si no lo piden: aquí lo pide el propio repositorio cuando toca mergear). **OJO al leer el estado del PR**: con los tres obligatorios en verde y los builds de APK aún corriendo, `mergeable_state` dice `unstable`, no `blocked` — `unstable` SÍ se puede mergear; `blocked` es que falta un check obligatorio. Antes decía aquí que no estaba protegida y era cierto: dos tandas se mergearon rojas y el APK del cliente estuvo semanas sin compilar. Medido entonces: **acoplamiento entre features bajo** (44 imports cruzados en el cliente, 41 en el conductor) pero hay **ciclos** (`shell`↔`businesses`, `driver_status`↔`active_trip`) y cuatro imanes de conflicto (`home_screen.dart` 3.117 líneas, `admin.routes.ts` 2.046, `client.service.ts` 1.923, `transport_booking_screen.dart` 1.921). **No tenemos RIBs** (arquitectura de Uber, nativa Swift/Kotlin) ni tiene sentido adoptarla: sería reescribir las apps fuera de Flutter.

También HECHO (Frontend del cliente antes de la reunión — P0 mandados + P1 estética + P2 tacto): reporte del usuario «en envíos se volvió solo uno» y «el frontend quedó muy básico». **Los mandados NO se habían borrado**: las 7 categorías y su pantalla de 500 líneas estaban enteras — lo roto era el camino (home → Movilidad → hoja → Envíos → hoja → «Compra o diligencia», CINCO toques) y la tarjeta «Envíos» del home, que decía «Paquetes y mandados» y su `onTap` iba directo al formulario de paquetes sin tocar nunca la otra rama. Ahora: **`FilaCategoriasMandado`** en el home (Droguería·Mercado·Comida·Pagos·Documentos·Compras, cada una entra con SU categoría ya elegida vía `ErrandBookingScreen.categoriaInicial` + `state.extra` en el router; «Otro» fuera del carrusel porque no comunica nada; «Farmacia» se muestra como «Droguería», que es la palabra que usa la gente) y la tarjeta abre **`hoja_envios.dart`**, compartida por el home y la pestaña de movilidad con un parámetro `sobreMapa` para la paleta (se borraron las 143 líneas de la copia local: dos copias se separan en cuanto alguien toca una). **Estética**: el rediseño anterior había borrado `business_card.dart` (portada 108 px) y dejado filas con miniatura de 64 px — el argumento (densidad para comparar) es correcto y las filas SE QUEDAN; lo que faltaba es lo que hacen Rappi y Uber Eats, las dos cosas: **`CarruselDestacados`** («Para pedir ya», foto 136 px) arriba y la lista densa debajo. **Regla: solo entra quien TIENE foto, y si ninguno la tiene la sección no se dibuja** — ni marcos grises ni degradados de relleno; la nota dice «Nuevo» sin votos, nunca estrellas vacías. `TarjetaServicio` pasó de caja blanca a puerta con cuerpo: velo diagonal de su tinte, **marca de agua del glifo** (escalada desde el tamaño del catálogo con `Transform.scale` + `IgnorePointer` — NO se añade una medida a `ZipaIconSize`, que tiene dos y una prueba lo vigila) y cuadro del icono con volumen; **todo pintado, cero assets** (en `AppCliente/assets/` no había ni una ilustración de servicio pese a que el comentario de `zipa_icon.dart` decía que «la tarjeta usa la ilustración»). Tacto: escala 0,97 + háptica en puertas, categorías y carrusel; en `FilaComercio` solo háptica **a propósito** (vive en una lista que se desplaza y una fila que se encoge bajo el dedo se lee como un fallo). **⚠ FALLO DE MÉTODO QUE CAZÓ EL CI, no yo**: `zipa_icon_test` tiene DOS reglas sobre el fuente y antes de subir ejecuté solo la de `Icons.`; la otra prohíbe **emojis en todo `lib/`** y yo había escrito «★» en la nota (99 pasaron, 1 falló). Arreglado con `ZipaIconName.estrella` en el catálogo. **Al tocar iconos hay que correr las TRES reglas: `Icons.` en los siete rediseñados, emojis en lib/, y que enum y mapa de glifos cuadren.** Verificado: CI verde (run 566). **PENDIENTE de QA visual**: nada de esto se ha visto en un teléfono — altura del carrusel (206 px) y la marca de agua solo se juzgan en pantalla. **Y el riesgo que no es de diseño: el carrusel solo aparece si hay comercios con portada cargada en producción** (se suben desde `/negocio/[token]/catalogo`).

También HECHO (Reservas de taxi: el conductor las APARTA con antelación): reporte del usuario — «cuando se reserva un viaje para otro día no le llega a ningún conductor», con el caso concreto de Pamplona (estudiantes que entran a las 6:00). El viaje programado NO estaba roto (el barrido existía y estaba bien), pero hacía lo de Uber: guardar la reserva y **empezar a buscar 15 minutos antes**. Para una empresa de taxis eso no sirve — y para quien reserva tampoco: no quiere que a las 5:45 «se empiece a buscar», quiere dormirse sabiendo que tiene taxi. Ahora la reserva se publica como un **tablero** (`reservas.service.ts` + `GET /driver/reservas`, `/reservas/mias`, `POST /:id/apartar`, `POST /:id/soltar`; pantalla `/reservas` en el conductor con tarjeta en el panel del home y en el menú). **Cuatro reglas que lo sostienen:** apartar es **atómico** (`driverId: null` en el `where` del `updateMany` — dos taxistas tocando a la vez y el pasajero terminaría con dos carros); **tope por conductor** (`RESERVAS_TOPE_CONDUCTOR`, 6 — quien aparta diez y cumple dos deja a ocho tirados y el daño no lo paga él); **la que no se cumple se libera sola** (`liberarReservasIncumplidas`: pasada la hora + `RESERVA_GRACIA_MIN` y sin latido desde hace `RESERVA_SIN_SENAL_MIN`, vuelve a la búsqueda abierta — al que SÍ da señales no se le quita nada aunque no haya tocado «voy en camino», porque puede ir conduciendo); y **una reserva para mañana NO ocupa al conductor hoy** (`guardaNoOcupa` nuevo en `lib/estado-terminal`: con `guardaNoTerminal`, cerrar la carrera de hoy lo dejaba **ON_TRIP —fuera del despacho—** hasta cumplir la de mañana, y el `NOT EXISTS` del encadenado lo habría excluido igual). `despacharProgramados` ya no toca las que tienen conductor (`driverId: null` en su `where`) y su gemelo `activarReservas` las pasa a ACCEPTED avisando por WS `reserva_activa` + push; **el conductor NO se marca ON_TRIP por adelantado** (todavía no recogió a nadie). **El agujero que habría dejado la función inservible**: la app del conductor **no tiene forma de cargar un viaje ya asignado** —solo conoce los que aceptó en esa sesión por el socket—, así que a las 5:45 le habría llegado el aviso de un viaje que no podía empezar; `listarMisReservas` devuelve ahora la oferta con la MISMA forma que `trip_request` (`buildTripRequestDTO` exportado, no una copia) y el botón «Iniciar viaje» la mapea con `ofertaDeViaje` y entra al viaje activo de siempre. **Lado cliente**: `getActiveClientTrip` no incluía la ficha del conductor —al abrir la app en frío el viaje salía sin conductor aunque lo tuviera, y eso ya pasaba en cualquier viaje aceptado, no solo en reservas—, y `scheduledFor` llegaba del servidor y **la app lo tiraba** (entidad, parser, copyWith y `toJson`), así que quien reservaba para las 6:00 no veía en ninguna pantalla a qué hora era su viaje; ahora el seguimiento dice «Conductor apartado · Te recoge mañana a las 06:00». Verificado: typecheck + **601 pruebas** (10 nuevas, las tres reglas puras comprobadas rompiéndolas) y **`e2e/reservas-taxi.ts` contra PostgreSQL real** (38 comprobaciones), más los E2E de programados, encadenados, cierre y taxi sin regresiones. **OJO de método**: la comprobación de concurrencia «solo uno se la lleva» **sigue en verde aunque se quite la guarda atómica** —entre las dos llamadas hay bastantes idas y venidas a la base como para que la segunda ya vea la escritura de la primera—, así que el E2E comprueba **aparte** que repetir el `updateMany` sobre una reserva ya tomada escriba cero filas; esa es la que de verdad vale cuando Render levanta dos instancias.

También HECHO (Pulido a partir de cinco capturas del usuario): «perfeccionemos lo que ya está construido». **El precio salía «5.200 $»** en las dos apps — `NumberFormat.currency(locale:'es_CO')` pone el símbolo DETRÁS y el parámetro `symbol` solo cambia el carácter, no su posición; el comentario del propio archivo prometía `$15.750` y llevaba tiempo mintiendo. Arreglado formateando el número suelto y anteponiendo el peso, con **7 pruebas en cada app** que fijan también que `parse` lea el punto como separador de MILES (tratarlo como decimal convertía quince mil pesos en quince). Toca los 45 archivos que usan el formateador. **El timeline del cliente encendía «Buscando conductor» en un viaje RESERVADO** (`scheduled` y `searching` comparten el paso 0), así que una reserva para mañana a las 6:00 decía toda la noche que se estaba buscando a alguien cuando ni siquiera había salido al despacho; ahora el primer paso se llama «Reservado», y si ya tiene conductor apartado queda CUMPLIDO con el segundo activo. **El badge «12 min» junto a «mañana a las 06:00»** son los minutos que dura el trayecto y se leía como «llega en 12 minutos»: fuera en las reservas (la duración ya sale en el detalle). **El pin «en lugares por donde no va el conductor»** era el de RECOGIDA, que se seguía pintando con el pasajero a bordo — una gota suelta lejos de la ruta, en el sitio donde recogió hace diez minutos; ahora desaparece al arrancar, como en Uber y DiDi. **La pastilla «Navegar al destino» tapaba el vehículo** y chocaba con el chat y el SOS: pasa a la izquierda, donde la franja está libre en todos los estados. **El carrusel «Para pedir ya»** pintaba una tarjeta en blanco (foto caída → `ColoredBox` vacío; pasa de verdad, sin S3 las fotos viven en disco efímero) y además incluía locales CERRADOS bajo ese título — con la insignia «No está recibiendo pedidos» debajo de «Para pedir ya». Ahora solo entran los abiertos y la foto caída enseña el nombre del negocio. **Los iconos de las cuatro puertas** mezclaban `_outlined` y `_rounded` (pesos distintos) y el glifo ocupaba un tercio del cuadro: los cuatro a filled/rounded (taxi, restaurante, caja, van intermunicipal) y `Transform.scale(1.45)` dentro del cuadro — sin añadir una medida a `ZipaIconSize`, que tiene dos y una prueba lo vigila. **El tablero de reservas se contradecía a sí mismo**: enseñaba «No pudimos actualizar» Y «todavía no has apartado ninguna» a la vez — el estado vacío solo se pinta cuando de verdad se pudo preguntar; y `GET /driver/reservas` devuelve ahora un `aviso` cuando la lista está vacía POR UN MOTIVO (sin vehículo registrado, tipo de vehículo que no atiende reservas) en vez de dejar al conductor revisando cada mañana un tablero que nunca le iba a enseñar nada. **Portal de negocios**: el buscador de dirección ya decía «no está disponible» ante un error, pero se callaba cuando funcionaba y no encontraba nada — mismo callejón sin salida; ahora lo dice y sugiere el formato («Calle 5 # 3-40»). **OJO: el buscador vacío NO es un bug de código, es que falta `GOOGLE_MAPS_API_KEY`** — sin llave el backend responde 503 y Places devuelve vacío. Verificado: typecheck + 601 pruebas, web tsc + next build, las tres reglas de iconos y balance de llaves; Flutter en CI (run 572). **Y el círculo blanco vacío de la barra del conductor NO era un avatar sin foto** —como supuse al mirar la captura— **sino el botón de ATRÁS, invisible**: `Material(color: Colors.white)` con un `Icon` sin color, que hereda el del `IconTheme` y en modo oscuro sale casi blanco. Es exactamente la co-ocurrencia que la regla de contraste prohíbe. Se barrieron las dos apps buscando el mismo patrón (icono sin color dentro de un subárbol con blanco fijo): 6 candidatos, **5 falsos positivos** —AppBars del intermunicipal que sí fijan `foregroundColor`, y botones rellenos donde el blanco es primer plano— y **uno real**, ese. **Comprobado y NO tocado, porque funciona**: «0 % del trayecto recorrido» no está clavado — `_waypointIndex` avanza con el GPS real vía `_indiceMasCercano` (monótono, nunca retrocede); en la captura el ETA marcaba 3 min 49 s de un viaje de 4 min, o sea acababa de arrancar y el 0 % era correcto.

También HECHO (La reserva SÍ era un bug mío, no solo el despliegue): había atribuido «el cliente reserva un taxi y al conductor no le llega ninguna solicitud» a que Render no tiene las rutas nuevas. Eso es cierto **y además había un defecto real en `listarReservasLibres`**, que habría dejado el tablero vacío incluso con el backend al día. El filtro de plaza era `...(d.citySlug ? { citySlug: d.citySlug } : {})` — **igualdad estricta**, así que un viaje con `citySlug` NULO desaparecía para todo conductor que sí tuviera ciudad. Y la asimetría es sistemática: el conductor recibe `citySlug` en **cada latido**, mientras que el viaje solo lo tiene si `plazaDeCoordenadas` resolvió al crearlo (coords fuera de los 40 km de cualquier centroide, lista de municipios aún sin cargar, o viajes anteriores a esa columna). **Comprobado contra PostgreSQL real antes de tocar nada**: sembradas tres reservas idénticas —sin plaza, con `pamplona`, con `cucuta`— y un taxista con `citySlug='pamplona'`; la de plaza nula **NO se veía**. Arreglado a `OR: [{citySlug: d.citySlug}, {citySlug: null}]`: **un dato que falta no puede excluir; lo que excluye es un dato presente y distinto** (una reserva de Cúcuta no le sirve a un taxista de Pamplona, y eso sigue filtrado). Fijado en `e2e/reservas-taxi.ts` con las dos mitades (40 comprobaciones, verde dos veces seguidas). **Revisados los otros seis sitios que filtran por `citySlug` y dejados como están, porque ahí la igualdad estricta es CORRECTA**: son de reportes (`admin.service` métricas por plaza, contadores de `dispatch-recovery`) y un viaje sin plaza no pertenece a ninguna ciudad — contarlo en todas inflaría cada número. La distinción que importa: en reservas el filtro decide si alguien **ve trabajo**; en el panel decide si aparece en una **cifra**. **OJO de método**: al cambiar la firma de `listarReservasLibres` a `{reservas, aviso}` se rompieron cuatro llamadas del propio E2E y el script murió a la primera — y una corrida abortada deja huérfanos en la base que hacen fallar la SIGUIENTE por otro motivo («liberó 2» en vez de 1). Si un E2E falla raro justo después de otro que reventó, limpiar antes de creerse el fallo.

También HECHO (El mismo defecto de moneda, en los portales web): aplicada la regla del repo —cuando se corrige un dato mal formateado, buscarlo en la otra superficie—. El precio estaba copiado en **DIECIOCHO sitios** entre `/empresa` y `/negocio`, y las copias **ya habían divergido**: diecisiete usaban `Intl.NumberFormat('es-CO', {style:'currency'})`, que devuelve **«$ 5.200» con espacio duro** porque ese es el patrón de moneda del locale, y `empresa/page.tsx` lo escribía a mano como «$5.200» — o sea que el mismo panel enseñaba el precio de dos maneras según la pestaña. Es el primo hermano del bug de Dart (allí el locale ponía el símbolo DETRÁS). Ahora hay **un solo `app/moneda.ts`** con `formatCOP` y `formatNumero` (kilos, bultos y metros NO llevan peso; ponerlo diría que son dinero), los dos aceptando importe ausente → «$0»/«0» en vez de tumbar la página con `undefined.toLocaleString()` —dos pantallas los llaman con campos opcionales—. **`backend/src/lib/moneda-portal.test.ts`** (6 pruebas) importa el fichero de `app/` —es TypeScript puro sin nada de Next, igual que `dockerfiles.test.ts` lee fuera de `src/`— y la última **barre todo `app/` prohibiendo `currency: 'COP'` fuera del helper**: es la que impide que vuelvan a acumularse, porque copiar tres líneas siempre será más rápido que buscar el helper. **Esa prueba se ganó el sueldo al primer intento**: cazó una copia decimoctava en `negocio/[token]/pedido/[id]/page.tsx` que mi grep NO vio por estar partida en varias líneas. Comprobada rompiéndola (reintroduje una copia en `FinancePanel` y cayó, con el archivo y la línea). Verificado: backend typecheck + **607 pruebas** (de 601), web tsc + next build. **Comprobados y dejados como están**: los dos `onTap: () {}` del home del conductor son los documentados como correctos —pestaña activa y absorbe-toques de la oferta—, no botones sin cablear.

También HECHO (Glifos propios para las cuatro puertas de la home): las cuatro tarjetas de servicio —movilidad, comida, envíos, intermunicipal— usaban iconos de catálogo (`Icons.local_taxi_rounded` y compañía), o sea los mismos glifos que miles de apps, en lo primero que se ve al abrir ZIPA. Ahora están DIBUJADOS a mano en `AppCliente/lib/app/theme/zipa_glifos.dart` (taxi de perfil con cartel de techo, campana de servir, caja con la tapa más ancha que el cuerpo, buseta de perfil): vectores, cero assets, ninguna licencia ajena — mismo criterio que `VehicleTopDownPainter`. **Tres decisiones que costaron las iteraciones**: (1) **silueta RELLENA, no trazo** — los otros 17 iconos del catálogo son de la familia `rounded`, que es rellena, y un glifo de línea al lado se lee como de otra familia; además a 20 px los huecos de un trazo de 2 unidades se taponan; (2) **los huecos son agujeros de verdad** (`PathOperation.difference`), no formas del color del fondo, porque estos glifos van sobre el tinte de la categoría, que no es blanco; (3) **en la buseta los montantes van DESPUÉS de abrir la fila de ventanas** — al revés el agujero se los come y sigue compilando. Los trazados se cachean (`Path.combine` recorta de verdad; rehacerlo por fotograma sería trabajo tirado). **`tools/previsualizar-glifos.py`** dibuja los MISMOS números con Pillow y los saca a 20/29/128 px: sin Flutter local, lo primero que vería el dibujo sería un teléfono del usuario, y «compila» no es «se ve bien» — **tres de las cuatro formas cambiaron por completo tras mirarlas** (el taxi tenía las ruedas dentro de la puerta, la caja se leía como un regalo con lazo, la buseta de frente parecía un electrodoméstico). Los cuatro nombres CONSERVAN su entrada en `_glifos`: si se retira un dibujo el icono no desaparece, vuelve al de Material, y así la prueba del catálogo sigue exigiendo que todo nombre tenga glifo. `test/zipa_glifos_test.dart` (7 casos) vigila las dos roturas invisibles —una pieza fuera de la rejilla de 24 (no se ve, y parece fallo de pantalla) y un hueco que deja de serlo—; sus 13 puntos de tinta/hueco se comprobaron ANTES contra el modelo de Pillow, que es una implementación independiente de los mismos números. **Dejado a propósito**: `hoja_envios.dart` y las pantallas de transporte siguen en Material — mezclar un glifo pintado y uno de catálogo en la MISMA lista se ve peor que dos coherentes.


También HECHO (Envío entre ciudades — piezas 1 y 2 del modelo de encomienda en bus): el modelo que pidió el usuario es que las empresas intermunicipales de PASAJEROS (Brasilia, Copetran…) lleven mercancía en sus buses, y resuelve solo el problema que yo había señalado como bloqueante del modelo de camión: **ya tienen taquilla en cada terminal, o sea la bodega en destino, y ya salen todos los días**. El hueco medido antes de tocar: `Order` no tenía ciudad y `Business` tampoco —solo dirección y coordenadas—, así que un mayorista podía *contratar un flete* pero un cliente de otra ciudad no podía comprarle. **Pieza 1**: `Business.citySlug` (sellada con `plazaDeCoordenadas`, EL MISMO resolutor que viajes y conductores — con otro criterio, un pedido sería «de Cúcuta» para la tienda y de otra plaza para el tablero) + `Business.shipsTo` Json `[{city,fee,etaHours}]`; se sella al registrar y al fijar el punto en el mapa, que es la única vía de los ya registrados. **Pieza 2**: `Order.originCitySlug`/`destCitySlug`/`isIntercity`/`intercityFee`, **sellados al crear** (si el dueño se muda o sube su tarifa mañana, el pedido de ayer no cambia de ciudad ni de precio) — migración `20260917120000_envio_intermunicipal`, con relleno de la plaza de los comercios existentes reusando el SQL de `city_scope`. **Las reglas que impiden que esto cobre mal** viven sueltas en `lib/destinos-envio.ts` (19 pruebas): la lista vacía significa «a NINGUNA ciudad» y no «a todas» (al revés habría abierto el catálogo local a todo el país el día del despliegue); **declararse a sí mismo como destino se RECHAZA** (es el error más caro: cobrarle flete intermunicipal a un cliente de la misma cuadra); la ciudad repetida se rechaza en vez de quedarse con un precio que el dueño no revisó; tope de cordura de 1.000.000 por el cero de más; y **sin coordenadas de entrega el pedido es LOCAL** — un dato que falta no puede convertirlo en intermunicipal y cobrar de más. El flete va en `intercityFee` **aparte de `deliveryFee`**, que es el pago del repartidor urbano: mezclarlos descuadraría las dos liquidaciones. El checkout rechaza diciendo **a qué ciudades sí despacha**, y el `etaMinutes` pasa a ser el que declaró el comercio para ESE destino (un envío a otra ciudad no llega en 30 minutos). Portal: `DestinosEnvio` en Ajustes de `/negocio/[token]`, reusando `useMunicipios`; sin plaza, en vez de un formulario muerto, manda a ubicarse en el mapa que está justo encima. `PUT /business/:token/shipping` valida además contra `municipalities` (un destino inexistente se cae ahí y no cuando un cliente intente comprar). Verificado: typecheck + **626 pruebas** (de 607), web tsc + next build, y **`e2e/envio-intermunicipal.ts` contra PostgreSQL 16 real** (25 comprobaciones, incluido el relleno de la migración sobre filas anteriores y el punto en mitad del mar que queda en NULL); las dos reglas puras y la guarda del checkout comprobadas ROMPIÉNDOLAS —quitar la guarda hace que un pedido de la MISMA ciudad se rechace—. **PENDIENTE de este modelo (piezas 3, 4 y 5)**: hora de corte de bodega («pide antes de las 4 p.m.»); enganchar el pedido intermunicipal como una línea de `FreightManifest` de un `CargoTrip` o de una salida, que es la pieza barata porque **el remito ya existe y ya concilia**; y la última milla en destino. Y **nada de esto se nota todavía en la app cliente**: no hay UI para elegir una dirección en otra ciudad. **RIESGOS QUE HAY QUE RESOLVER ANTES DE OPERAR, no son de código**: reporte al RNDC con manifiesto electrónico de carga (cero menciones en el repo), quién responde si se pierde la mercancía (hoy se registra la discrepancia pero no quién paga), y remitente identificado + declaración de contenido + lista de mercancía no admitida — `cargoDescription` es texto libre y operando desde Norte de Santander eso es exposición penal real.


También HECHO (Pieza 4 — la encomienda viaja en el despacho y se cierra con el acta de la taquilla): **el hueco que abrió la pieza 2 y que había que cerrar primero**: `acceptOrderByBusiness` llamaba a `startOrderMatchingCycle` sin mirar nada, así que un pedido de Cúcuta a Bucaramanga se le ofrecía a una **moto local** para que llevara la caja a otra ciudad. Ahora el ciclo urbano solo arranca si `!isIntercity`; el pedido intermunicipal espera en PREPARING a que una empresa lo suba a un despacho. **NO hay modelo nuevo, y es deliberado** (la bitácora ya registra que construir `CargoTrip` al lado de `FreightRequest` fragmentó la trazabilidad): un pedido se convierte en UN `FreightManifest` —que YA era el remito: un destinatario, su ciudad, sus bultos conciliables uno a uno y el acta firmada— colgado de un `CargoTrip`, y hereda gratis el rastro GPS, los gastos y la cuenta de cobro. `FreightManifest.orderId` **ÚNICO** (migración `20260917180000_encomienda_en_despacho`) es a la vez el vínculo y la **toma atómica**: dos despachadores a la vez y el segundo choca contra el índice en vez de crear un segundo remito de la misma caja. `OrderStatus += IN_INTERCITY_TRANSIT` (`ADD VALUE ... BEFORE 'IN_TRANSIT'`), separado de `IN_TRANSIT` porque son cosas distintas —aquí la caja va en un bus, allí en la moto de un repartidor—. **`lib/encomiendas.ts` (17 pruebas)**: `motivoParaNoDespachar` devuelve el MOTIVO y no un booleano (quien pulsa tiene el bus a punto de salir); PENDING no entra (el negocio aún puede rechazar por existencias); **`motivoParaNoAdmitir` compara la RUTA** — subir a un bus que va a Bogotá una caja para Bucaramanga es el error caro, el destinatario se enteraría un día después y en la ciudad equivocada—, y un despacho SIN destino declarado no se rechaza (solo se exige coincidencia de lo que se sabe); `itemsDeRemito` pone un renglón por producto para poder conciliar «me llegaron 2 de 3», y un bulto nunca mide cero porque sería inconciliable. `encomiendas.service.ts`: tablero `listarEncomiendasPendientes` **filtrado por RUTA y no por empresa** (una encomienda no es de nadie hasta que alguien la sube, igual que el tablero de fletes), `adjuntarEncomienda`, `soltarEncomienda` (borra el remito: uno sin pedido ni líneas no es documento de nada y ensuciaría el consecutivo) y **dos ganchos**: despachar el viaje O despachar el remito ponen el pedido en tránsito —hacen falta los dos porque son acciones distintas del portal y cualquiera puede ir primero; despachar solo el remito dejaba el pedido en «preparando» con la caja ya en el bus—, y **recibir el remito ENTREGA el pedido**: así opera de verdad una encomienda en bus (el destinatario va a la taquilla, muestra cédula y firma), así que ese es el cierre real y la última milla a la puerta es una mejora ENCIMA, no un requisito. Con faltantes se entrega igual y la discrepancia queda anotada: negarle la entrega a quien SÍ recibió dos de tres sería peor, y la constancia es lo que sostiene el reclamo. Rutas `GET /operator/encomiendas?origen&destino`, `POST /operator/cargo-trips/:id/encomiendas`, `DELETE /operator/encomiendas/:orderId` + **`EncomiendasPanel`** en las secciones Intermunicipal y Carga del portal (primero en la lista: es plata esperando en una bodega). App cliente: `CustomerOrderStatus.inIntercityTransit` con sus dos `switch` de EXPRESIÓN actualizados —en Dart eso es error de compilación, la trampa que ya documenta la bitácora— y **un fallo latente corregido**: el `firstWhere` de `fromJson` (la caché local) no tenía `orElse`, así que un estado desconocido lanzaba y el `try` del datasource **descartaba el historial ENTERO** del usuario. Verificado: typecheck + **643 pruebas** (de 626), web tsc + next build, y **`e2e/encomienda-en-despacho.ts` contra PostgreSQL 16 real (20 comprobaciones)**. **OJO DE MÉTODO, el hallazgo más útil de la tanda**: la primera versión del E2E comprobaba el ESTADO del pedido tras aceptarlo y **pasaba igual con la guarda quitada** — `startOrderMatchingCycle` no escribe en la base hasta que alguien acepta, así que sin conductor cerca el pedido se queda en PREPARING de las dos maneras. Es el mismo falso positivo que la concurrencia de las reservas. Corregido sembrando un repartidor ONLINE con GPS fresco y **capturando la oferta** con `registerSendToDriver` (patrón de `e2e/paradas-viaje.ts`), más una **contraprueba**: un pedido LOCAL con el mismo repartidor SÍ recibe `order_request` — sin ella, la comprobación pasaría también si el despacho estuviera roto para todo. Las dos roturas (guarda urbana y comprobación de ruta) verificadas cayendo. **PENDIENTE**: la pieza 3 (hora de corte de bodega) y la 5 (última milla a la puerta); y **sigue sin notarse en la app cliente**, que no tiene UI para elegir una dirección en otra ciudad.


También HECHO (Los dos reportes del portal de negocios: el pin invisible y las direcciones del recibo): la captura del usuario mostraba el mapa pintado y **ningún pin**. Causa exacta: la superposición del pin era `absolute inset-0` **sin z-index**, y Leaflet le pone z-index positivo a sus propios paneles — una superposición con z-index `auto` cuenta como 0 y se pinta DEBAJO de las teselas, así que el dueño movía el mapa a ciegas sin saber qué punto iba a guardar. `FleetMap`, el otro mapa Leaflet del portal, ya usaba `z-[400]` para lo mismo; a `LocationPicker` se le olvidó. Corregido a `z-[500]` (por encima de los paneles, por debajo de los controles de zoom). **OJO: el proxy de esta sesión bloquea unpkg (403 en el túnel CONNECT)**, así que los valores exactos del CSS de Leaflet no se pudieron descargar — el número se eligió por la evidencia del propio repo. **El segundo reporte —«direcciones que se ponen exactamente como dicen en los recibos no salen»— era de fondo**: el buscador del portal usaba SOLO Places Autocomplete, que está afinado para sitios con nombre y direcciones ya conocidas, y la nomenclatura colombiana tal como está en un recibo (`carrera 4a#10-53`) no la predice casi nunca. Quien SÍ la resuelve es la **Geocoding API**, que ya existía en `geo.service` (`geocodeAddress`) y el buscador no usaba. Ahora: **`lib/direccion-colombiana.ts` (15 pruebas)** deja la dirección en forma canónica antes de preguntar — expande las abreviaturas que de verdad se escriben (cra/kra/kr/k, cl/cll, av, dg, tv, mz…, ninguna oficial pero todas en los letreros), acepta `#`/`No.`/`Nro`/`N°`/`numero` como marcador, **le pone espacios al `#`** (sin ellos «4a#10-53» le llega a Google como un solo token), pasa la letra de la vía a mayúscula, pega el guion de la placa y **pone el marcador cuando falta** («Carrera 4A 10-53»); es idempotente, y **lo que no parece una dirección NO se toca** (`pareceDireccion`): con «Centro Comercial Ventura» geocodificar no ayuda, y es mejor que Google no encuentre algo a que nosotros le cambiemos la calle al negocio. `geocodeAddress` normaliza, nueva ruta `GET /geo/geocode?address=&city=` (devuelve también la forma normalizada para ENSEÑÁRSELA al dueño), y el `LocationPicker` cae a ella **solo cuando el autocompletado se queda en blanco** —una petición por tecleo sería tirar cuota— ofreciendo una fila «Ir a Carrera 4A # 10-53» que mueve el mapa y pide comprobar el pin. **Trampa que cazó una prueba**: en la expresión del marcador de número, `num` iba antes que `numero` y se comía el prefijo — «Calle 5 numero 3-40» salía como «Calle 5 # ero 3-40». Las alternativas largas van primero. Verificado: typecheck + **658 pruebas** (de 643), web tsc + next build. **Confirmado de paso por la captura: `GOOGLE_MAPS_API_KEY` YA está puesta en producción** — el mapa del portal sale con el estilo oscuro de Google y su atribución, que solo se sirve si `/geo/tile-ticket` entrega pase.


También HECHO (Entrada por WhatsApp — webhook, puente de identidad y enlace de un solo uso): el usuario pidió poder pedir taxi por WhatsApp «ya que no todos querrán descargar la app» y que la solicitud llegue a la app conductor. **Eso último ya estaba hecho y no se tocó una línea**: el despacho no sabe de dónde viene el viaje (`POST /client/trips/request` crea el Trip y arranca el ciclo; el `trip_request` sale por WS igual), así que AppTransport no cambia. Lo que faltaba era el canal — `notification.service.ts` tenía tres plantillas con formato de Meta pero es un **mock que escribe en consola y no lo importa nadie**. **Se eligió el enlace y NO un chat conversacional, por dinero medido**: desde el 1 de octubre de 2026 Meta cobra los mensajes de servicio pasados los 1.000 gratis al mes por número, y un chat que pregunta origen, destino y confirma son ~7 salientes por carrera (≈140 viajes/mes) frente a 1–2 del enlace (**>500 viajes/mes gratis**, y a ~8 pesos el viaje después); además por chat el pasajero no tiene mapa, ni SOS, ni conductor a la vista. Cero IA por mensaje a propósito: un modelo por turno costaría más que el mensaje y es peor para direcciones. **`GET/POST /webhooks/whatsapp`** (handshake + mensajes) con **firma `X-Hub-Signature-256` sobre el cuerpo CRUDO** — index.ts le monta un `express.raw` propio ANTES del JSON global, porque reserializar el objeto parseado da otro hash y no validaría nunca; **falla cerrado**: sin `WHATSAPP_APP_SECRET` responde 503, ya que esta ruta acaba emitiendo una sesión para el teléfono que diga el cuerpo. Responde 200 y procesa después (Meta reintenta si el 200 tarda, y un reintento es otro mensaje cobrado). **`enlace-magico.service.ts`** es el puente: `usuarioParaTelefonoVerificado`/`tokenParaTelefonoVerificado` emiten **la misma sesión que `verifyClientOtp` sin pedir OTP** —correcto solo porque Meta ya verificó el teléfono— y `enlace-magico-alcance.test.ts` (9 pruebas) **falla si esas funciones aparecen en cualquier archivo de `src/routes/`**, si el webhook deja de validar la firma o si deja de fallar cerrado. El token NO viaja en la URL (un enlace de chat se reenvía, se ve en la vista previa y queda en el historial): va un **código opaco de 192 bits, un solo uso y 15 min**, detrás del `#` —que no llega al servidor ni al robot de la vista previa— y se canjea en `POST /client/auth/magic-link` (bajo `/client/auth`, o sea con `authLimiter`), con consumo **atómico** (`updateMany` con `usedAt: null` en el where). Migración `20260917210000_whatsapp_enlace_magico`: `MagicLink` y `WhatsappInbound`, cuyo índice **único sobre `waMessageId` es el antirreintento** y cuyo `outcome` deja por escrito por qué un mensaje no tuvo respuesta. Reglas puras probadas: `lib/whatsapp-payload.ts` (26 pruebas con `enlace-magico.ts`) — los **acuses de entrega llegan por el mismo webhook y NO son mensajes**, el `timestamp` son SEGUNDOS (leerlo como ms daría 1970 y todo se descartaría por viejo), lo malformado devuelve `[]` y nunca lanza (un 500 hace que Meta reintente durante horas), y **un número extranjero se RECHAZA en vez de deformarlo** — `normalizeColombianPhone` le pegaría «+57» a un venezolano y crearía una cuenta con un teléfono inexistente, y operamos en la frontera; más tope de 10 respuestas por teléfono cada 24 h y descarte de mensajes de más de 10 min. App cliente: `redeemMagicLink` en datasource/repositorio (con `_persistirSesion` compartido con el login por OTP), `authProvider.entrarConEnlace` que **devuelve el motivo** (vencido / ya usado / mal copiado son tres arreglos distintos) y pantalla **`/entrar`** que canjea sola y cae a «Entrar con mi teléfono» si falla. `/health` += `whatsapp: cloud-api|configuracion-incompleta|apagado` — el modo intermedio es el error fácil (poder recibir pero no mandar se ve como un canal muerto sin un solo error en los registros). Guía de activación en **`docs/ACTIVAR_WHATSAPP.md`** (verificación de negocio en Meta, número dedicado, las 5 variables, y el aviso de que **hay que cargar medio de pago antes del 30/09/2026 o dejan de entregarse los mensajes aunque estés bajo los 1.000 gratis**). Verificado: typecheck + **693 pruebas** (de 658) y **`e2e/whatsapp-enlace.ts` contra PostgreSQL 16 real (38 comprobaciones)**, con la guarda de firma comprobada QUITÁNDOLA: sin ella, una petición falsificada **crea la cuenta y emite dos enlaces para un teléfono ajeno**. **Un defecto real lo cazó el E2E**: el contador del tope usaba igualdad `outcome === 'respondido'`, pero cuando el envío falla el valor es `respondido:sin-salir` — o sea que el tope se apagaba solo justo cuando Meta estaba rechazando envíos. **Tres trampas de método que costaron cuatro corridas y conviene recordar**: (a) `indexOf('firmaValida')` sobre el archivo entero encontraba la línea del **import**, así que la guarda de orden pasaba aunque la validación se hubiera movido detrás del procesamiento — hay que mirar solo el CUERPO del manejador; (b) `spawn('npx', ['tsx', ...])` + `kill` mata a `npx` pero **no al node que cuelga de él**, así que quedaban servidores huérfanos y la corrida siguiente medía un servidor VIEJO con el código de antes (se arregló con `detached` + matar el grupo, y con una guarda que aborta si el puerto ya está ocupado); (c) los scripts de `e2e/` **no entran en el `typecheck`** (tsconfig solo incluye `src/`), así que un nombre de columna equivocado —`userId` por `passengerId`— no lo caza el compilador, solo la ejecución.

También HECHO (Piezas 3 y 5 de la encomienda — hora de corte y última milla; **y el cobro de un servicio que nadie prestaba**): al abrirlas apareció primero un defecto que había introducido yo en la pieza 2 — **al pedido intermunicipal se le cobraba el `deliveryFee` urbano ADEMÁS del flete**, cuando en un envío a otra ciudad nadie hace un domicilio: el comercio deja la caja en la terminal y el cliente la recoge en la taquilla. Se cobraba un servicio inexistente. La pieza 5 lo vuelve coherente: `desgloseEnvio` (`lib/ultima-milla.ts`, 14 pruebas) cobra el domicilio **solo** si el cliente pidió que se la lleven a la puerta, y entonces paga al repartidor de DESTINO. **Pieza 3 — hora de corte** (`lib/corte-bodega.ts`, 16 pruebas): `DestinoEnvio.cutoff` opcional POR DESTINO (el bus a Bogotá no sale a la misma hora que el de Bucaramanga; sin migración, es JSON), `proximoDespacho`/`estimaLlegada` en **hora de Colombia** —a las 02:00 UTC del martes aquí son las 21:00 del lunes— saltándose los días en que el comercio no opera (reusa `Business.hours`), y `Order.promisedAt` SELLADO al crear: «doce horas» dicho a las cinco de la tarde con el bus de las cuatro ya ido es mentira, y `etaMinutes` no puede expresar «mañana a las 8». «Antes de las 4» significa ANTES: a las 16:00 clavadas ya no alcanza. Campo `type=time` en `DestinosEnvio` del portal. **Pieza 5 — última milla** (migración `20260918100000_corte_y_ultima_milla`): `Order.lastMile/hubLat/hubLng/hubAt` + `OrderStatus.AT_DESTINATION_HUB`; al recibir el remito, si el cliente la pidió, el pedido NO se cierra — queda esperando repartidor **anclado en el punto donde el conductor del bus firmó el acta**, tomado de su GPS real (`puntoDeEntrega`), nunca en el centroide del municipio, que sería mandar al repartidor a un sitio donde la caja no está; sin posición se cae al cierre en taquilla en vez de dejarlo colgado. `getOrderOfferInfo` ancla en el hub y le dice al repartidor dónde está la caja, no la dirección del comercio (que está en otra ciudad). **CUATRO bloqueantes del mismo tipo, cada uno habría dejado la función muerta al nacer, y tres solo los cazó el E2E**: (1) `acceptClientOrder` exigía `status === 'PREPARING'` ⇒ la oferta salía y todo «aceptar» devolvía null; (2) `assertCustodyPin` pedía en la taquilla el **PIN de recogida que solo tiene el comercio de origen** — nadie en destino puede darlo — y mi primer arreglo miraba el estado, que `acceptClientOrder` ya había cambiado a DRIVER_TO_PICKUP, así que seguía pidiéndolo: la marca durable es `hubAt`; (3) `startOrderMatchingCycle` con la misma comparación ⇒ el ciclo salía sin ofrecer ni registrar nada; (4) **una CUARTA copia en `_offerOrderToCandidate`**, que calculaba el candidato correcto a 0 m y lo descartaba en silencio. Por eso la condición vive ahora en `ESTADOS_DESPACHABLES`/`esDespachable` (`lib/estado-pedido.ts`) y el barrido de rescate (`dispatch-recovery`) la usa también: sin eso, una caja cuyo primer ciclo se agotara se quedaría en la taquilla para siempre sin que nadie se enterara. **Defecto de la pieza 4 corregido de paso**: `_toSummary` traducía el estado con un mapa a mano y un `?? o.status.toLowerCase()` de respaldo; nadie añadió `IN_INTERCITY_TRANSIT`, salía `in_intercity_transit`, la app compara con `s.name` (camelCase), no casaba y caía a su `orElse` ⇒ **el cliente cuyo paquete iba en el bus veía «Confirmado»**. Ahora el mapa es `Record<OrderStatus,string>` (añadir un estado sin traducirlo NO COMPILA) y `estado-pedido.test.ts` **lee el enum de Dart y comprueba que los dos lados coinciden** — se ganó el sueldo al primer intento. El PIN de ENTREGA en la puerta se sigue exigiendo igual. Verificado: typecheck + **732 pruebas** (de 693), web tsc + next build, y **`e2e/corte-y-ultima-milla.ts` contra PostgreSQL 16 real (22 comprobaciones)** más los E2E de las piezas 2 y 4 sin regresión; la regla del cobro comprobada ROMPIÉNDOLA. **El E2E de la pieza 2 afirmaba el defecto** («cobra el flete APARTE del domicilio») y se corrigió dejando escrito por qué cambió la expectativa. **Trampas de método de esta tanda**: mi parser del enum de Dart partía por comas ANTES de quitar comentarios y un comentario lleva una coma dentro, así que perdía valores (misma familia que la trampa del parser de `copyWith`); y la contraprueba «la oferta NO le llega al de origen» **pasaba con la lista vacía** — hay que exigir además que HAYA ofertas. **PENDIENTE**: nada de esto se nota aún en la app cliente, que sigue sin UI para elegir una dirección en otra ciudad ni para pedir la entrega a domicilio en destino (el backend ya acepta `lastMile` en `POST /client/orders` y expone `promisedAt`); y si el cliente pidió puerta pero no aparece repartidor, hoy la salida es que la recoja en taquilla — no hay aviso que se lo diga.

También HECHO (El canal de atención al titular, y la política que no se podía corregir): el usuario señaló que usar su Gmail personal como contacto de soporte era mala idea. Al mirarlo salieron dos cosas peores. **(1) La política de privacidad decía «Contacto: el canal de soporte dentro de la app»** — circular: quien la desinstaló no puede pedir que borremos sus datos, Play exige que la política sea accionable sin instalar nada y la **Ley 1581 de 2012** obliga al responsable a publicar un canal de atención. **(2) Y no se podía arreglar**: `getActiveLegalDoc` solo sembraba la v1 si no existía ninguna y **no había ninguna función para publicar otra**, así que en cuanto producción sembró la suya, cambiar el texto en el código dejó de tener efecto — un documento legal congelado para siempre. **`lib/contacto.ts` (10 pruebas)**: `SUPPORT_EMAIL`/`PRIVACY_EMAIL`/`LEGAL_EMAIL` con herencia a soporte (un solo buzón basta para arrancar sin dejar huecos), validación de forma —**una dirección rota dentro de un documento legal es peor que ninguna**: el titular escribe, rebota, y queda constancia de que el canal que publicamos no sirve— y, sin configurar, **no se imprime nada parecido a un correo**: el texto dice la verdad y `/health` lo marca (`contactoLegal: publicado|sin-configurar`). Los textos legales pasaron de constantes a **funciones** (`textoTerminos`/`textoPrivacidad`): como constante quedarían congelados con el entorno del arranque y publicar tras configurar el correo no cambiaría nada. **`publishLegalDoc`** desactiva la anterior y crea la nueva **en una transacción** —dos activas y `getActiveLegalDoc` devolvería una u otra según el orden, con usuarios aceptando textos distintos bajo la misma etiqueta—, rechaza reutilizar una versión (la constancia de consentimiento dejaría de probar nada), un cuerpo de menos de 200 caracteres y una etiqueta vacía. Ruta `POST /admin/legal/:kind/publish` + `GET /admin/legal`, **con su bloque en el panel** (pestaña SOS, junto a los retiros DMCA) porque una ruta sin botón es código muerto — lección ya pagada en este repo. `docs/DMCA_AGENTE.md` deja de sugerir `legal@zipa.app`, un dominio que nadie registró, y remite a `LEGAL_EMAIL`. Guía nueva **`docs/DOMINIO_Y_CORREOS.md`** (qué dominio comprar y dónde, Zoho gratis frente a Cloudflare Email Routing —que solo RECIBE, y para responderle a un titular hace falta enviar—, las variables en los DOS servicios, y el aviso de publicar la versión nueva **antes** de tener usuarios porque obliga a todos a re-aceptar). Verificado: typecheck + **744 pruebas** (de 732, con 2 que EJECUTAN el bloque nuevo del panel), y **`e2e/legal-contacto.ts` contra PostgreSQL real (18 comprobaciones)**: sin correo no se inventa ninguno, con correo la política lo publica y cita la ley, nunca hay dos versiones activas, los tres guards, y **publicar invalida el consentimiento anterior**. La invariante de «una sola activa» comprobada ROMPIÉNDOLA (quedaron 3). **Trampa de la tanda, la del propio repositorio**: escribí `\'` en el JS del panel y en un template literal eso colapsa a `'` y cierra la cadena — la forma correcta en el fuente es `\\'`. El panel entero quedó con error de sintaxis y **lo cazó `admin-panel.test.ts`**, que es justo para lo que existe.

También HECHO (El botón de ubicación de WhatsApp — la entrada pasa de un paso a dos, y ninguno exige escribir): el usuario pidió que la persona pueda mandar su ubicación **con un botón**. `location_request_message` es un tipo de mensaje NATIVO de Meta (verificado en la documentación, no citado de memoria): al tocarlo, WhatsApp abre su propia pantalla de compartir ubicación, con su mapa y su permiso, y la respuesta vuelve por el MISMO webhook como un mensaje `type: 'location'`. Eso resuelve justo la parte cara del formulario —escribir una dirección en un teclado, que es lo que más gente abandona— sin que tengamos que pedir permisos ni mantener nada. Flujo: escribe cualquier cosa → **botón** → manda el punto → **enlace con la recogida ya puesta**, y solo le queda elegir a dónde va. **Sigue sin haber bot que entienda frases** y el costo apenas se mueve: 2 salientes por carrera = 500 viajes dentro de los 1.000 gratis del mes (un chat conversacional son ~7 y da para 140). **Sin estado propio a propósito**: no hay tabla de conversaciones — «¿ya se le pidió?» sale de la fila del mensaje anterior en `whatsapp_inbound`, y una máquina de estados aquí sería un sitio más donde quedarse colgado. Las reglas viven sueltas en `lib/whatsapp-conversacion.ts` (14 pruebas) porque son donde esto se rompe sin que se note: **la ubicación MANDA sobre «ya pedida»** (si no, la respuesta a nuestra propia petición se iría sin origen), **quien no quiere mandarla no se queda encerrado** (texto insistiendo → enlace seco, que escriba la dirección en la app, que tiene buscador y mapa), y **`esFueraDeCobertura` FALLA ABIERTO** — con la tabla de municipios vacía el resolutor devuelve null para TODO el mundo, y bloquear con eso dejaría a la ciudad entera sin poder pedir por un problema nuestro. `MagicLink.originLat/Lng/Label` (migración `20260918150000_ubicacion_whatsapp`) lleva el punto hasta la app: **va en la BD y no en la URL**, porque un enlace de chat se reenvía y con las coordenadas dentro se estaría reenviando dónde está esa persona. **La trampa cara, y la que más se nota: al REUTILIZAR un código vivo el origen se ACTUALIZA** — sin eso, quien manda su ubicación, camina dos cuadras y manda otra recibiría un enlace con la primera, y el taxi iría a donde estuvo, no a donde está; nada en pantalla delataría el error (comprobado rompiéndolo: 3 comprobaciones caen). Un texto posterior NO borra el punto ya mandado. Parseo: **Meta manda lat/lng como CADENAS decimales**, se descarta lo fuera de rango y **(0,0)**, que es el Golfo de Guinea, o sea «falta el dato» (misma regla que `puntoDeEntrega`). El tope subió de 10 a **20 salientes/24 h** —con dos mensajes por carrera, 10 dejaba a un pasajero frecuente en cinco viajes al día— y `OUTCOME_UBICACION` empieza por `respondido` para que el botón CUENTE: si se llamara de otra forma, alguien podría hacernos mandar cien botones —cada uno se cobra— sin tocar el límite. **Y no se abre cuenta para mandar un botón**: quien escribe y no sigue no deja un usuario fantasma. App cliente: `OrigenSugerido` (provider en memoria a propósito — guardado en disco, quien entró ayer abriría hoy con la recogida de ayer, y un punto de partida equivocado que parece correcto es peor que ninguno) con `fromJson` **sin casteos directos** y 6 pruebas, porque esto se lee en el camino por el que el pasajero ENTRA: si lanza, no falla el punto, falla la entrada entera; `entrarConEnlace` devuelve `(motivo, origen)` y con punto va derecho a pedir el viaje (`go` al inicio + `push`, para que el atrás no cierre la app); `transport_booking_screen` lo consume y lo BORRA —manda sobre la dirección guardada y el GPS: es de hace segundos y lo eligió para ESE viaje—. Verificado: typecheck + **764 pruebas** (de 744) y **`e2e/whatsapp-enlace.ts` contra PostgreSQL 16 real, ahora 51 comprobaciones** (de 38), con la migración aplicada sobre una base con datos previos. **OJO de método**: en modo mock NINGÚN envío «sale», así que todos los resultados quedan `…:sin-salir` — las comprobaciones tienen que usar `startsWith`, y por eso mismo el camino «texto tras pedir ubicación → enlace seco» solo lo cubre la prueba unitaria; y con algo en rojo, el E2E ahora vuelca la cola de registros del servidor, que es la única pista cuando el procesamiento corre en segundo plano. **PENDIENTE**: nada de esto se ve hasta poner las cinco variables en Render (`/health` debe pasar de `whatsapp: apagado` a `cloud-api`); `docs/ACTIVAR_WHATSAPP.md` trae ahora el atajo del **número de prueba de Meta** (gratis, 5 destinatarios, sin verificación de negocio) y el aviso de que **migrar un número de la app WhatsApp Business a la Cloud API borra su historial y lo inutiliza en la app**.

También HECHO (El pedido entero dentro de WhatsApp — se retira el enlace a la web): decisión del usuario, «todo debe ser por WhatsApp, el cliente no pedirá por una página». Y el cálculo que sostenía el enfoque anterior estaba MAL PLANTEADO: decía «un chat completo agota los 1.000 mensajes gratis en 140 viajes», que es cierto pero no es el costo. Pasados los gratis, Colombia está entre las tarifas más bajas del mundo (~4 pesos/mensaje): con siete salientes por carrera, **mil carreras al mes cuestan unos 24.000 pesos, menos del 3 % de su comisión**. El costo que no se estaba contando era el otro — la gente no abre la página, y justo la que no la abre es para la que existe esta puerta. **`lib/whatsapp-flujo.ts`** (18 pruebas) es la máquina de estados PURA: `inicio → esperando_origen → esperando_destino → esperando_confirmacion → viaje_en_curso`. El ORDEN de las comprobaciones es lo que la sostiene y no es arbitrario: (1) un viaje en curso manda sobre todo —quien ya tiene taxi y escribe quiere saber dónde está el suyo, no pedir otro—; (2) cancelar funciona desde cualquier paso; (3) **una ubicación NUEVA reinicia el origen esté donde esté la conversación** (si no, quien camina dos cuadras y manda su punto otra vez tendría el taxi donde estuvo — el mismo error más caro que ya se corrigió en la versión de dos pasos); (4) la caducidad (20 min) se mira DESPUÉS, porque cancelar y mandar ubicación valen aunque esté vieja. Comprobado rompiéndolo: mover la ubicación tras la caducidad hace caer la prueba. `WhatsappConversation` (migración `20260924180000_whatsapp_conversacion`, una fila por teléfono que se reescribe — NO es historial, eso sigue en `whatsapp_inbound`); `whatsapp-pedido.service.ts` ejecuta (geocodifica, cotiza con `getTripOptions`, crea el viaje con `requestClientTrip`); `enviarBotones` para confirmar (el `id` del botón es lo que vuelve por el webhook, no el título: así el texto se cambia sin romper el flujo) y `botonId` nuevo en el parser. **Avisos de vuelta al chat**: `registerAvisoWhatsapp` inyectado en ws.handler (patrón del repo, evita el ciclo client↔whatsapp) engancha en `_avisarPasajero`, que es el punto ÚNICO, así que reusa `avisoDeEstado` y los dos canales dicen lo mismo y callan en los mismos estados; solo avisa a quien pidió por WhatsApp (la conversación guarda el `tripId`) y se cierra sola al terminar. **CONTRADICCIÓN QUE CAZÓ EL E2E Y QUE HABÍA ESCRITO YO**: el comentario afirmaba «el precio que se cobra es el que la persona vio», y el backend RECALCULA y descarta lo del cliente (regla correcta, no se toca: es lo que impide pedir una carrera de mil pesos). Se vio en el log —«cotizado 8500 · sellado 5000»— y se arregló haciendo que el mensaje de confirmación lleve el precio del VIAJE, no el de la cotización, y solo mencione el cambio si lo hubo. El enlace a la web **no desaparece**: va OFRECIDO dentro de un mensaje que ya salía («ver en el mapa»), cuesta cero mensajes extra y le da mapa y SOS a quien los quiera — porque en WhatsApp no hay ninguno de los dos y eso no se disimula. Purgado `lib/whatsapp-conversacion.ts` y sus 14 pruebas (enfoque de dos pasos, código muerto); sobrevive lo único suyo que sigue siendo cierto en `lib/whatsapp-cobertura.ts` (5 pruebas): la cobertura no depende de cómo se pida el taxi, y **falla ABIERTO** al revés que la firma del webhook. La guarda `enlace-magico-alcance.test.ts` se amplió SOLO para `whatsapp-pedido.service` (extracción del mismo módulo, no una puerta nueva; cualquier otro archivo que aparezca ahí es una fuga). Verificado: typecheck + **814 pruebas** (805 +23 nuevas −14 del código purgado) y `e2e/whatsapp-pedido.ts` contra PostgreSQL real (21 comprobaciones: la conversación entera, que el «ok» no cotice un trayecto inventado, que el viaje exista con el punto que mandó, que escribir con viaje en curso NO cree un segundo, y la coherencia del precio). **PENDIENTE de esta puerta**: quien entra por WhatsApp NO acepta términos (`recordConsent` se llama en el login por OTP, el registro de conductor y el de empresa — este camino no pasa por ninguno), así que con `LEGAL_CONSENT_ENFORCE=true` queda como puerta trasera al consentimiento; y por WhatsApp solo se pide viaje urbano, no intermunicipal ni encomienda.

También HECHO (Fuera el sufijo de región, y un correo de contacto que existe): dos peticiones del usuario. **(1) «/SANTURBÁN» fuera de la app cliente.** El chip de ubicación del home pintaba «ZIPA/SANTURBÁN» con la zona que el servidor resolvía por municipio; ahora vuelve a decir «Tu ubicación» y se borró `zona_marca_provider.dart`. **El dato se limpia además en la base** (migración `20260924210000_quitar_zona_marca`, `UPDATE municipalities SET zone = NULL`) y esa es la parte que importa: las apps YA INSTALADAS guardan la última zona conocida en SharedPreferences y solo la olvidan cuando `/geo/zona` les contesta que no hay ninguna — **por eso la ruta y `etiquetaDeZona` se quedan aunque la app nueva no los pinte**; quitarlos dejaría «ZIPA/SANTURBÁN» pegado para siempre en esos teléfonos. La columna sigue siendo nullable por si una plaza quiere su nombre algún día: sería una fila. **(2) El correo del titular.** `SUPPORT_EMAIL`/`PRIVACY_EMAIL`/`LEGAL_EMAIL` no tenían valor por defecto a propósito —para no publicar una dirección inventada— pero el caso que de verdad se desplegaba era el malo: backend y portal son servicios distintos, nadie había puesto la variable en ninguno de los dos, y la política de privacidad y la página que lee el revisor de Play salían **sin canal de atención**, que es justo lo que la Ley 1581 y Play no admiten. Ahora el valor por defecto es `zipalegalcolombia@gmail.com`, un buzón REAL (no un placeholder), con las variables mandando encima para el día que haya dominio. Está en dos ficheros porque son dos servicios (`backend/src/lib/contacto.ts` y `app/contacto.ts`) y **`contacto-portal.test.ts` falla si divergen** — dos direcciones distintas en el mismo trámite son dos canales contradictorios, y la página de eliminar cuenta perdió su rama «sin correo», que ya no se alcanza. **OJO, ACCIÓN PENDIENTE DEL USUARIO**: producción ya sembró la v1 de la política SIN correo y los documentos legales están congelados una vez sembrados — hay que **publicar una versión nueva** desde `/admin` → SOS → Documentos legales para que el buzón aparezca, y eso obliga a todos a re-aceptar (mejor hacerlo antes de tener usuarios). Verificado: typecheck + **823 pruebas** (de 819), web tsc + next build, y contra PostgreSQL real `e2e/zona-de-marca.ts` (la migración aplicada **sobre la fila que ya tenía 'Santurbán'**: 0 municipios con zona, y añadir una sigue siendo una fila) y `e2e/legal-contacto.ts` (18 comprobaciones, reescrita la primera sección: lo que se exige ya no es que NO haya correo sino que lo haya sin configurar nada).

También HECHO (Las tres piezas de redBus que faltaban: comodidades, condiciones del tiquete y la nota de la empresa): **(1) Comodidades** (`lib/amenidades.ts`, 13 pruebas): catálogo CERRADO —con texto libre una empresa escribe «Aire acondicionado», otra «A/C» y otra «climatizado», y ya no se puede comparar ni filtrar—, `PooledTrip.amenities`, casillas en el portal al publicar y chips en la tarjeta y en la hoja del cliente. **Van en la SALIDA y no en la empresa** porque dentro de la misma flota un bus tiene aire y la buseta vieja no. **El baño NO se declara: se DERIVA del plano de sillas** (`seatConfig.bano`) en las dos direcciones — con baño dibujado se anuncia aunque no lo marquen, y sin baño en el plano no se anuncia aunque lo marquen; dos fuentes acabarían prometiendo un baño que el dibujo no tiene, y en un nocturno de nueve horas esa es la queja más cara. Una clave desconocida del formulario se RECHAZA nombrándola (quien publica es el portal con casillas: una clave que no existe es un error nuestro), pero una guardada en la base se ignora sin tumbar la consulta. **(2) Condiciones del tiquete** (`lib/politicas-tiquete.ts`, 15 pruebas): `Operator.policies` con equipaje/mascotas/menores/cancelación, formulario en «Mi empresa» y sección en la hoja de reserva y en «Mis reservas». **Sin declarar NO se inventa nada** —un «2 piezas de 20 kg» por defecto sería una condición reclamable que la empresa nunca fijó— y la app lo dice. **Cuelgan de la EMPRESA** (al revés que las comodidades): la regla de mascotas vale para toda la flota. **La cancelación no promete devolver el dinero**: el tiquete no se paga en la plataforma (`SeatBooking` no tiene ningún campo de dinero), así que se declara la antelación y el texto dice que la devolución se acuerda con la empresa — prometer un reembolso que este sistema no puede ejecutar es la peor mentira posible porque involucra plata. El texto se REDACTA EN EL SERVIDOR (`lineasDePolitica`) y el portal enseña `policyLines`, el mismo que lee el pasajero: si cada lado redactara el suyo, la empresa creería publicar una regla y en la app saldría otra. **(3) Nota de la empresa**: `Operator.rating Float?`+`ratingCount` (NULL de nacimiento; el 5,0 de fábrica ya se pagó dos veces, en `Business` y en `Driver`) + `SeatBooking.rating/ratingComment`, hoja de estrellas en «Mis reservas» y chip «Nuevo» mientras nadie la califique. `lib/calificar-salida.ts` (7 pruebas) decide cuándo se puede: COMPLETED, **o EN CURSO si por la duración de la ruta ya debería haber llegado** — quien cierra la salida es el conductor, y si se le olvida el pasajero que sí viajó no podría calificar nunca, o sea que la nota dependería de la persona a la que juzga; sin duración conocida se exige COMPLETED, porque dar por llegado un bus que rueda sería peor. **Y el defecto que destapó**: `rateIntercityBooking` guardaba la estrella en su fila y **no la llevaba a ninguna reputación** —ni al conductor, cuya nota salía solo de los `Trip` urbanos, ni a la empresa—; `reputacion.service.ts` nuevo promedia AHORA las tres fuentes por conductor (urbano + intermunicipal + salidas) y las dos por empresa (salidas + intermunicipales, no los urbanos: ahí la empresa no pone vehículo ni operación). Migración `20260924230000_politicas_amenidades_reputacion`, todo aditivo y nullable. Verificado: typecheck + **858 pruebas** (de 823), web tsc + next build, y **`e2e/condiciones-y-nota.ts` contra PostgreSQL real (22 comprobaciones)** con las dos guardas más delicadas comprobadas ROMPIÉNDOLAS (el baño derivado del plano, y la empresa promediando sus dos servicios). **Un fallo mío que cazó el E2E**: lancé los recálculos con `void` en vez de `await` «para no bloquear», y como las funciones ya son best-effort por dentro eso solo abría una carrera — la empresa seguía sin nota justo después de calificarla. El patrón del repo (viaje urbano) era `await`; ahora los tres coinciden.

También HECHO (Puntos de embarque y cupones de pasaje — **y el bug que hacía imposible comprar una silla desde la app**): **EL HALLAZGO DE LA TANDA, encontrado al tocar esa ruta**: `POST /client/intercity/pool/:id/book` recibía `seats` de la app y **NO se lo pasaba a `bookSeats`** (se quedaba con `seatsBooked`, `pickupAddress` y `notes`), así que en una salida numerada el servidor respondía **«Elige al menos una silla»** a quien acababa de elegirla: **el mapa de asientos entero era inalcanzable desde la app**. No lo vio nadie porque las pruebas y el E2E de sillas llaman a `bookSeats` DIRECTAMENTE. Por eso el E2E nuevo golpea las RUTAS por HTTP (levanta el servidor, hace login con OTP y compra), y se comprobó restaurando el fallo: sale el mensaje exacto. **(1) Puntos de embarque** (`lib/puntos-embarque.ts`, 17 pruebas): `PooledTrip.boardingPoints` `[{id,name,address?,time}]` (máx. 6) — **no son los `stops`**, que son los municipios por los que pasa el bus; estos están en la ciudad de origen y sirven para subirse, y mezclarlos habría hecho creer que se puede abordar en un pueblo por el que el bus pasa de largo. La hora se guarda **como la escribe la empresa** («06:15», lo que anuncia en su taquilla) y la aritmética vive en `horaAbsolutaDePunto`, que resuelve **el cruce de medianoche**: un bus que sale 23:40 y pasa a las 00:05 lo hace veinticinco minutos después, no veintitrés horas antes — sin eso la app diría que la recogida ya pasó, y ordenar por hora pondría el último punto el primero. Se ordenan por hora, se rechaza el repetido y el que quede a más de 3 h de la salida (el filtro del dedo). El pasajero **elige uno** (radio en la hoja) y **se SELLA en su reserva**: si la empresa le cambia la hora mañana, a esa persona le dijeron otra cosa. Sin elegir se sella el primero —la terminal—, porque las apps ya instaladas no mandan el campo y dejarlas sin comprar sería peor; un id inexistente SÍ se rechaza. El manifiesto del portal muestra quién sube dónde. **(2) Cupones de pasaje** (`lib/cupon-pasaje.ts`, 9 pruebas): la pregunta que lo decide es **quién paga el descuento**. En un viaje urbano lo paga la plataforma y sale de nuestra comisión (`descuento-viaje.ts`) porque estamos en medio del dinero; **en un pasaje de bus no lo estamos** —el pasajero le paga a la empresa y por la app no pasa un peso—, así que un cupón «de ZIPA» haría que la empresa cobrara menos sin decidirlo y sin que nadie le reponga: **se RECHAZA diciéndolo**. Lo que sí opera hoy: `PromoCode.operatorId` + `PromoScope.INTERCITY`, cupones que **emite y asume la EMPRESA** desde su portal (`PromosManager`, `GET/POST /operator/promos`, toggle) y que solo valen en SUS salidas (el de otra empresa y el de un particular se rechazan con su motivo). Canje **dentro de la transacción de la reserva** —si la silla se la lleva otro, el cupón no puede quedar gastado— y `PromoRedemption` con contexto `pooled` para auditarlo. **(3) De paso, un hueco real**: `SeatBooking` no guardaba NINGÚN importe, así que al subir la tarifa el pasajero veía en «Mis reservas» un precio que nunca aceptó; ahora se sellan `fareTotal`/`discount`/`promoCode` y el DTO expone `amountToPay` ya restado — **el conductor ve en el manifiesto cuánto cobrar**, que es lo que evita la discusión en la puerta del bus. Migración `20260925010000_embarque_y_cupon_pasaje`. Verificado: typecheck + **884 pruebas** (de 858), web tsc + next build, y **`e2e/embarque-y-cupon.ts` contra PostgreSQL real por HTTP (17 comprobaciones)** más los E2E de condiciones, sillas y encomienda sin regresión. **OJO de método**: en desarrollo el OTP lo pone `OTP_DEV_CODE`, no `OTP_FALLBACK_CODE` (ese es el de producción), y un servidor huérfano en el puerto hace que la corrida mida el código viejo — el E2E aborta si el puerto está ocupado.

También HECHO (Las vans recogen en la casa — **regresión mía, corregida antes de mergear**): el usuario lo señaló y tenía razón: al publicar los puntos de embarque dejé el campo «dónde te recogen» en el `else`, así que **una empresa que declarara puntos dejaba sin domicilio a sus vans** — y el puerta a puerta es justo por lo que la gente elige la van en vez del bus. Las dos formas **CONVIVEN** ahora (`lib/recogida-salida.ts`, 15 pruebas): `PooledTrip.doorToDoor Boolean?` (migración `20260925030000_recogida_a_domicilio`) con **el valor por defecto deducido del vehículo** —VAN sí, BUSETA/BUS no, salida sin vehículo declarado sí (como han funcionado todas hasta hoy)—; nullable a propósito, porque un default fijo mentiría para la mitad de las salidas que ya existen (un bus prometiendo recoger en casa, o una van negándolo) y la empresa lo cambia con una casilla en el portal que además le dice qué se deducirá si no la toca. **El fallo sutil que apareció al cablearlo**: mandar «sin punto» desde la app significaba lo MISMO que manda una app vieja, a la que hay que sellarle la terminal, así que quien pidiera recogida en casa habría acabado con «Sube en: Terminal · 06:00» en su reserva. De ahí `PUNTO_DOMICILIO` (marcador explícito) y `elegirAbordaje`, que decide en un solo sitio las cuatro ramas: domicilio explícito (exige dirección — sin ella el conductor no sabría a dónde ir — y que la salida lo admita), punto elegido (y la dirección sobra: guardarla dejaría al conductor con dos sitios para la misma persona), app vieja con dirección escrita (se entiende como domicilio) y app vieja sin nada (la terminal). `elegirPunto` se retiró: dos funciones decidiendo lo mismo acaban divergiendo. **App conductor**: la lista de pasajeros pasó de una línea a tres — nombre, **dónde recogerlo** (el punto con su hora o su dirección) y **cuánto cobrarle** ya descontado; sin eso, a quien usó un código de la propia empresa se le pediría la tarifa completa. **App cliente**: radio con los puntos + «En mi dirección», el campo solo cuando elige domicilio, y chip «Te recogen en tu dirección» en la tarjeta de la búsqueda, que es donde se compara una van con un bus. Sin puntos y sin domicilio se dice dónde subir en vez de dejar un formulario vacío. **Panel admin**: `adminListPromos` ahora mezcla los cupones de las empresas con los nuestros, así que la tabla enseña de quién es cada uno — sin eso, un admin desactivaría la promoción de una empresa creyendo que es nuestra. **Verificación de cierre pedida por el usuario antes de mergear**: typecheck + **895 pruebas**, web tsc + next build, y contra PostgreSQL real los SIETE E2E del intermunicipal (`embarque-y-cupon` 24 comprobaciones, `condiciones-y-nota` 22, `sillas-configurables` 11, `sillas-numeradas`, `encomienda-en-bus` 13, `encomienda-en-despacho`, `salidas-bus` 8, `envio-intermunicipal`), todos en verde.

También HECHO (Lo que el tablero de la flota NO podía ver: los viajes que se caen y a qué hora se trabaja): el usuario pidió seguir con el dashboard analítico y **lo primero que salió es que las cuatro piezas que el PENDIENTE listaba ya estaban hechas** (serie por día, comparativa contra el período anterior, espera de aceptación y ETA medio; `lib/analitica.ts` + `AnalyticsPanel`, verificado de punta a punta antes de escribir una línea). La nota estaba obsoleta desde la tanda de mapas oscuros. Medido lo que de verdad falta, el agujero es de fondo: **cada número del panel sale de servicios COMPLETADOS**, así que una flota cuyos viajes se caen a la mitad ve un tablero impecable — un viaje cancelado no entra en ninguna suma. `lib/analitica-operacion.ts` (25 pruebas) con las reglas donde esto miente: **sin denominador la tasa es `null`, no 0 %** (un «0 % de cancelación» para quien no cerró nada felicita a quien no trabajó); **las 24 horas se dibujan, incluidas las vacías** (la madrugada en blanco ES la respuesta a «¿monto turno de noche?»); y **no se señala hora pico bajo 20 servicios ni en empate** (una flecha deducida de tres servicios movería el turno de la gente por ruido; el empate es la misma regla con la que el selector no marca «la más barata» cuando dos cuestan igual). **Tres tarjetas nuevas**, todas sin una sola consulta extra salvo una: (1) **«A qué hora te piden servicio»** — 24 barras en hora de COLOMBIA, contando cuándo se PIDIÓ, no cuándo se cerró; para un viaje reservado la demanda es a la hora acordada (`scheduledFor ?? createdAt`), que es cuando hace falta el carro en la puerta. Se arma dentro del mismo recorrido de `getFleetFinance` que ya construía la serie diaria, así que la gráfica no puede sumar distinto que el KPI de arriba. (2) **«Viajes que se cayeron»** con tasa, motivo y conductor — única consulta nueva; filtra por `updatedAt` porque **no hay columna de cancelación y cancelar es la ÚLTIMA escritura que recibe un viaje** (después no se liquida ni se califica: calificar exige COMPLETED), y añadir la columna habría dejado fuera todo el historial, que es justo el que alguien mira la primera vez que abre la tarjeta. **Un viaje que nadie tomó no tiene `operatorId` sellado, así que jamás se le reprocha a una flota lo que no aceptó.** El motivo del admin es texto libre, así que `motivoLegible` lo muestra tal cual en vez de meterlo en «Otro», que escondería el caso interesante. (3) **Días activos en el ranking** — `diasActivos` y servicios/día salen del mismo Set del recorrido: cuarenta servicios en dos días y en veinte se ven idénticos en un ranking por plata. **Defecto que habría introducido yo y se cazó a tiempo**: el estado vacío del panel era `totalServices === 0`, así que una flota que aceptó diez viajes y los perdió todos habría visto «Aún no hay actividad» — justo la que no puede quedarse mirando ese cartel; ahora también mira los caídos. De paso, `admin.service` tenía su **propia copia de `HORAS_UTC_COLOMBIA`**: el riesgo no es que Colombia cambie de huso, es que dos tableros acaben cortando el día en momentos distintos al tocar una sola copia. Verificado: typecheck + **920 pruebas** (de 895), web tsc + next build, y **`e2e/analitica-operacion.ts` contra PostgreSQL real (37 comprobaciones)** con las dos guardas más delicadas comprobadas ROMPIÉNDOLAS: leer la hora en UTC tumba 7 comprobaciones —incluida la que persigue, un servicio de las 9 de la noche contado a las 2 de la madrugada del día siguiente— y quitar el mínimo de muestra hace que 3 servicios «tengan hora pico». **OJO de método**: la primera corrida salió toda en rojo por el FIXTURE, no por el código — sembraba `createdAt = hora − 3 min` sobre una hora en punto, así que cada viaje caía en el bucket anterior.

También HECHO (Los dos reportes del intermunicipal: ver sin buscar, y decir quién viaja y cómo se paga): con capturas de redBus al lado. **(1) «Solo puede ver las rutas cuando busca un destino».** `searchPooledTrips` YA aceptaba origen y destino vacíos: el candado estaba en la app, que nacía con `Pamplona → Cúcuta` FIJOS y buscaba solo ese par. Una salida Cúcuta → Bogotá existía y el pasajero leía «no hay viajes» — la pantalla afirmaba algo falso sobre la oferta, que es peor que no filtrar. Ahora los dos municipios nacen nulos («Desde cualquier parte» / «A cualquier destino», en gris para que no parezcan un filtro puesto), la pantalla abre mostrándolo TODO, y hay **«Ver todas las salidas»** porque los municipios se eligen pero no se des-eligen: sin esa salida, quien filtra una vez se queda dentro. El estado vacío distingue los dos casos —con filtro sugiere quitarlo; sin filtro dice que aún no hay salidas publicadas, porque «prueba otra ciudad» sería un consejo absurdo cuando ya se está viendo todo—. **(2) «Al reservar no da la opción de registrar datos ni cómo pagar».** Dos huecos distintos. **La planilla**: `BookSeatsDTO` no pedía ningún documento y `SeatBooking` guardaba UN nombre —el de la cuenta— sin importar los puestos, así que una familia de cuatro quedaba registrada como una sola persona y el conductor subía con una lista que no podía contrastar con nada. No es comodidad: el intermunicipal se opera con planilla identificada. `SeatBooking.passengers` + `lib/pasajeros-tiquete.ts` (16 pruebas): uno POR silla (aceptar menos dejaría el mismo agujero con más formularios), **documento repetido rechazado** comparando sin puntos ni guiones («1.090.123-456» y «1090123456» son la misma persona, y casi siempre es un copiar y pegar), `CC/TI/CE/PA` porque en la frontera el pasaporte y la cédula de extranjería se ven a diario, y el error nombra el PUESTO que falla. En la app, `DatosPasajeros` crece y se encoge con las sillas **liberando los controladores** del formulario que sobra, y el botón apagado DICE qué falta. **Compatibilidad**: las apps instaladas no mandan el campo, así que el servidor es tolerante y la exigencia se enciende con `PASAJEROS_EXIGIR_DOCUMENTO` (patrón de `KYC_ENFORCE`; visible en `/health`). **El cobro**: decisión del usuario preguntada antes de tocar el modelo — **la empresa cobra, ZIPA no toca la plata**, así que esto no es una pasarela sino lo que la empresa DECLARA. `Operator.paymentInfo` + `lib/cobro-pasaje.ts` (20 pruebas): catálogo cerrado (con texto libre una escribe «Nequi», otra «nequi» y otra «transferencia bancaria»), **sin declarar NO se inventa «efectivo»** —a la empresa que cobra por transferencia le llegaría gente sin efectivo a la puerta del bus— y **transferencia SIN cuenta se rechaza**, que es el caso caro: el pasajero reserva, lee «transfiere» y no hay a dónde. El texto lo redacta el SERVIDOR (`lineasDeCobro`) y lo enseñan igual el portal y la app; siempre cierra con «ZIPA no cobra el pasaje», que es lo que separa esto de una compra en la app. Hasta ahora había ahí un texto FIJO («acuerda el medio con el conductor») idéntico para todas. Migración `20260925120000_pasajeros_y_cobro`, las dos columnas nullable: las reservas viejas quedan «sin datos» en el manifiesto en vez de repetir el nombre de la cuenta cuatro veces, que sería inventar pasajeros. El manifiesto del portal y el del conductor muestran la planilla. Verificado: typecheck + **956 pruebas** (de 920), web tsc + next build, y **`e2e/pasajeros-y-cobro.ts` contra PostgreSQL real (20 comprobaciones)**, con las dos guardas comprobadas ROMPIÉNDOLAS (una por una, cada rotura tumba exactamente su comprobación) más los cuatro E2E del intermunicipal sin regresión. **Dos fallos MÍOS cazados antes de subir**: `library;` escrito DESPUÉS de los imports (no compila — el error que ya dejó el CI en rojo, ahora con un comprobador de balance y colocación en el barrido), y los tres rechazos del E2E compartían usuario, así que al romper la primera guarda los otros dos fallaban por «ya tienes una reserva» en vez de por lo que medían: un usuario por rechazo. **PENDIENTE decidido, no olvidado**: el pago EN LÍNEA (PSE, Nequi, tarjeta, Efecty — las capturas de redBus del usuario) queda fuera a propósito; exige Wompi en producción y, sobre todo, que ZIPA reciba la plata y se la gire a la empresa con su conciliación. El medio declarado hace falta igual en ese mundo: una empresa que solo toma efectivo tiene que poder decirlo aunque exista la tarjeta.

También HECHO (Punto 1 de los cuatro — la billetera tenía el SIGNO INVERTIDO en efectivo): al responder «¿cómo logramos que paguen la comisión?» el código dio una respuesta peor que la pregunta: **hoy el sistema hace lo contrario**. `recordCompletedTrip` escribía `netEarning` en `driver_earnings` en CADA servicio completado **sin mirar `paymentMethod`**, y `getDriverBalance` calculaba `suma(netEarning) − pagado − pendiente`. En una carrera de $6.000 en efectivo el conductor ya tiene los $6.000 en el bolsillo y el sistema decía que **le debíamos $5.100** — cuando la realidad es que él nos debe $900. Y no era teórico: `AppTransport/lib/features/wallet/` tiene pantalla de retiro, así que un taxista con cincuenta carreras en efectivo abría la app, veía ~$255.000 «disponible» y lo pedía. Sin explotar solo porque nadie había pedido un retiro — el mismo tipo de arma cargada que el `MOCK_DRIVER.id`. **La distinción ya existía y nadie la miraba**: `lib/metodos-pago.ts` clasifica cada método por `quienCobra`, y solo `en_linea` es `plataforma` — efectivo, Nequi, Daviplata y las transferencias son `conductor`, la plata le llega a él y por la app no pasa un peso. `lib/saldo-conductor.ts` (17 pruebas) con las tres reglas: **solo se le debe lo que NOSOTROS recaudamos**; **sin método declarado se asume EFECTIVO** (las apps viejas no lo mandan, y asumir «plataforma» acreditaría plata que nunca recibimos — el error caro frente al barato, que como mucho deja una comisión sin cobrar); y **la deuda se VE aparte, no escondida detrás de un cero** (un «$0 disponible» a secas se lee como fallo de la app; «debes $12.400 de comisiones» se lee como lo que es, y es lo que hace falta para poder cobrárselo). `DriverEarning.platformHeld` + `driverOwes` (migración `20260925150000_saldo_real_conductor`); `netEarning` NO se toca, sigue siendo «lo que ganó» y como cifra del panel es correcta. **Las filas anteriores quedan en CERO en las dos columnas a propósito**: no se puede saber con qué se pagó cada servicio viejo, y las dos formas de adivinarlo son malas de distinta manera —darlas por cobradas por la plataforma acredita plata que nunca entró; darlas por efectivo estrena el sistema cobrando una deuda que nadie avisó que corría—. La propina SÍ es retirable (la cobra Wompi). Billetera del conductor con la deuda visible y explicada. Verificado: typecheck + **977 pruebas** (de 956) y **`e2e/saldo-real-conductor.ts` contra PostgreSQL real (12 comprobaciones)** cerrando viajes de verdad con cada método — con el defecto restaurado caen 9 de 12, incluida «el conductor de solo efectivo NO puede retirar un peso», que pasaba a dejarle $36.125. **Dos pruebas existentes AFIRMABAN el defecto** (daban por bueno que el disponible saliera de `netEarning`): corregidas dejando escrito por qué cambió la expectativa, patrón ya usado con el E2E de la encomienda. **PENDIENTE de los otros tres puntos acordados**: (2) recargo por solicitud/reserva como comisión legal sobre tarifa regulada —antes hay que confirmar el decreto de Pamplona—, (3) taxi por puesto urbano sobre el motor pooled —el usuario confirma que los taxis YA lo hacen recogiendo persona por persona—, y (4) recaudo por Bre-B/Nequi a llave de ZIPA vía Wompi. **La distinción que decide el punto 4**: con la llave del CONDUCTOR la comisión sigue siendo una deuda que perseguir; con la llave de ZIPA se cobra sola. Mismo gesto para el pasajero, resultado opuesto para el negocio.

También HECHO (Punto 3 — el puesto de taxi urbano, y cierra con dinero): los taxis de Pamplona ya salen recogiendo persona por persona sobre un trayecto conocido en vez de quedarse quietos en el paradero; esto le pone publicación, reserva y un precio que no se discute a bordo. **Sobre `PooledTrip` y NO un modelo nuevo** (construir `CargoTrip` al lado de `FreightRequest` ya fragmentó la trazabilidad de la carga, y está escrito arriba): se distingue con `kind INTERCITY|URBANO` y se cambia solo lo que de verdad es distinto — los extremos de un trayecto urbano son dos PUNTOS de la misma ciudad, no dos municipios (`routeName`, `originLabel`/`destLabel`, las cuatro coordenadas y `soloFareRef`; migración `20260925180000_puesto_urbano`, todo aditivo con default). **Las tres reglas viven sueltas en `lib/puesto-urbano.ts` (18 pruebas)**: (1) **la MISMA ciudad** — es la guarda que sostiene lo demás, porque el camino urbano se salta la ruta intermunicipal, el tope de gasto compartido y la exigencia de empresa habilitada del modelo dual: sin ella, publicar «urbano» de Pamplona a Cúcuta sería la puerta de atrás para correr una troncal sin habilitación; (2) **dos puestos como mínimo** —una sola silla no es compartir, es una carrera entera a un precio que pone el conductor, esquivando el decreto— y máximo cuatro, que son las sillas de pasajero de un taxi; (3) **tope sacado de la carrera sola** (una vez y media repartida entre los puestos, redondeando a $50 hacia abajo): sin tope, llenar el carro valdría cuatro veces el taxímetro y «compartir» sería un recargo con otro nombre, y con él **un puesto SIEMPRE cuesta menos que el carro entero** (con dos sillas el tope ya es tres cuartos de la carrera). El rechazo DICE el número. La carrera de referencia se MIDE (geocodificado + Routes + tarifa del decreto vía `precioCategoria`) y, si no se puede, **se cae a la carrera mínima** —el piso real, y por tanto el tope más estricto— diciéndolo con `medida:false`; queda sellada en `soloFareRef` para auditar el precio después y para calcular el ahorro que ve el pasajero. **Y CIERRA CON DINERO**: completar una salida urbana por fin liquida (`recordCompletedTrip` con la comisión resuelta flota→ciudad→global sobre lo REALMENTE cobrado, que es el `fareTotal` sellado de cada reserva menos su cupón); el pasajero le paga al taxista en la mano, así que **la comisión queda como DEUDA suya** y se ve en su billetera — o sea que el punto 3 aterriza justo sobre los rieles del punto 1. Sin esto sería el único servicio que mueve plata sin dejar rastro (el intermunicipal de un particular NO liquida a propósito: es gasto compartido). **Regresión evitada**: la búsqueda intermunicipal abre sin filtro desde la tanda anterior, así que ahora filtra `kind: INTERCITY` (y `getOperatorPooledTrips` también) — si no, los puestos urbanos saldrían mezclados en «ver todas las salidas», y una ruta Terminal→Universidad no es alternativa para quien va a Cúcuta. Rutas: `GET /driver/pool/urbano/tope`, `POST /driver/pool/urbano/publish`, `GET /client/pool/urbano?lat&lng` (**la plaza la resuelve el servidor con `plazaDeCoordenadas`, el MISMO criterio que sella los viajes**; si la app decidiera por su cuenta pediría los puestos de una ciudad y el despacho la contaría en otra). Apps: el FAB de «Mis viajes compartidos» pregunta qué se publica (urbano o intermunicipal: las reglas de precio no son las mismas) y el formulario **no deja escribir el precio a ciegas** —enseña la carrera sola, el máximo y lo que se llevaría con el carro lleno—; el pasajero tiene pantalla propia (`/puesto-urbano`) con entrada en el home de movilidad bajo el hero intermunicipal, y reservar son dos cosas, cuántos puestos y dónde recoger —pedir documentos como en el bus sería un formulario más largo que el viaje—. Los cuatro estados van separados porque se arreglan distinto: sin ubicación, falló la red, ciudad sin cobertura, y ningún taxi con puestos ahora. **Fallo evitado a tiempo**: el `copyWith` de la salida no pasaba los tres campos nuevos, así que refrescar los cupos convertía un puesto urbano en intermunicipal y la tarjeta pasaba a decir «Pamplona → Pamplona» a mitad de la reserva — el mismo descuido que perdió el PIN del envío, y lo vigila `copywith_completo_test`. Verificado: typecheck + **995 pruebas** (de 977) y **`e2e/puesto-urbano.ts` contra PostgreSQL real (39 comprobaciones)**, con las dos guardas comprobadas ROMPIÉNDOLAS (quitar el filtro de tipo tumba 2; quitar la liquidación tumba 5) y los cinco E2E del intermunicipal sin regresión. **Nota de método**: la comprobación «queda debiendo la comisión» pasaba en verde con la liquidación quitada, porque comparaba 0 con 0 — hizo falta exigir aparte que la deuda sea MAYOR que cero. **PENDIENTE de este punto**: un puesto urbano no aparece en el historial de viajes del conductor (no es un `Trip`; la plata sí entra en `driver_earnings`, que es lo que sostiene la billetera y la comisión), y el tope de 1,5× no es configurable por plaza.

También HECHO (Punto 2 — el Decreto 049 de 2023 de Pamplona, y lo que destapó): el usuario subió el decreto, que era lo que bloqueaba este punto. **La respuesta al punto 2 es NO**: el artículo segundo tiene EXACTAMENTE cuatro renglones —carrera mínima, servicio por hora, recargo nocturno y recargo dominical— y **ninguno es por solicitud telefónica, por radio ni por aplicación**. Cobrar un «recargo por pedir en la app» sobre la tarifa de taxi sería cobrar por encima de lo autorizado, por el mismo razonamiento que ya impide aplicarle surge. Queda zanjado, no hay que volver a abrirlo. **Y destapó algo más grande: Pamplona NO tarifa el taxi por kilómetro.** No hay banderazo ni valor por km en el decreto; el artículo primero es una TABLA DE SECTORES con precios fijos (5.000 / 5.500 / 6.000 / 7.500 / 8.500 / 9.500). El código exigía el juego completo banderazo + km + mínimo para dar por cargada la tarifa oficial, así que **ese juego no existe para esta plaza y el taxi se estaba cotizando con la fórmula genérica — o sea con un precio que pone Nexum**, que es justo lo que `tarifa-categoria.ts` dice en su cabecera que existe para impedir; y rellenar las dos variables «a ojo» habría sido inventar dos números que la alcaldía nunca fijó y presentarlos como oficiales. **`lib/tarifa-decreto.ts` (23 pruebas)**: (a) **festivos de Colombia calculados**, no tabulados —una tabla se acaba el año que nadie la actualiza— con Pascua por Meeus, la Ley Emiliani al lunes siguiente y los móviles atados a Pascua; **contrastado casilla por casilla con el calendario publicado de 2025**, que además tiene 17 festivos y no 18 porque San Pedro y Pablo cae en el Sagrado Corazón (mi primera prueba afirmaba 18 a ojo y falló: la aserción era falsa, no el código); (b) **los dos recargos**, que hoy no se cobraban y son plata del conductor — **se SUMAN**, y lo dice el decreto («a partir de las 9:00 PM TODOS LOS DÍAS» sería redundante si excluyera los domingos); (c) **el sobre** mínima–máxima y **los escalones**, de modo que todo precio que cotiza la app es una casilla que existe en el documento, **bajando nunca subiendo** (subir cobraría más de lo que la estimación justifica, que en tarifa regulada es el error caro). `modoTarifaTaxi()` pasa a tener TRES valores (`decreto-por-km` | `decreto-por-zonas` | `generica`) y la descripción de la categoría dice «precio dentro de la tarifa oficial» —no «tarifa autorizada»— porque sin polígonos de barrios no sabemos la casilla exacta. **Defecto cazado al probarlo con los números reales**: la pendiente genérica está calibrada para una ciudad grande y **a los 6 km ya saturaba el techo**, así que en Pamplona casi toda carrera se habría cotizado en el sector MÁS CARO — cobrar de más en el caso común. De ahí `TAXI_KM_CARRERA_MAXIMA`, que NO sale del decreto sino que lo declara quien conoce la plaza, y reparte el sobre por distancia. **Variables para Render** (el usuario las pone): `TAXI_CARRERA_MINIMA_COP=5000`, `TAXI_CARRERA_MAXIMA_COP=9500`, `TAXI_TARIFAS_SECTOR_COP=5000,5500,6000,7500,8500,9500`, `TAXI_KM_CARRERA_MAXIMA=7` (calibrar), `TAXI_RECARGO_NOCTURNO_COP=1000`, `TAXI_RECARGO_DOMINICAL_COP=1000`; `/health` lo publica en `tarifaTaxi` y `recargosTaxi`. **AMBIGÜEDAD REAL del decreto, resuelta hacia el lado que NO cobra de más**: dice desde cuándo empieza el nocturno pero no hasta cuándo, así que se aplica de 21:00 a medianoche y una carrera de madrugada NO lo lleva; si la Secretaría de Tránsito confirma que cubre la madrugada se extiende con `TAXI_RECARGO_NOCTURNO_DESDE` sin tocar código. **PENDIENTE**: los polígonos de los barrios, que es lo único que convierte la estimación en la casilla exacta; y el «servicio por hora» de $30.500 del decreto es un producto que no tenemos.

También HECHO (Auditoría de privacidad y normativa antes de subir las apps + el portal que se caía en «Equipo y vehículos»): cuatro preguntas del usuario sobre la política, y dos huecos reales. **(1) Los terceros no estaban nombrados.** La política declaraba qué datos se recogen, para qué, el uso de IA con derecho a revisión humana y el habeas data, pero hablaba de «proveedores» en abstracto: la Ley 1581 y la ficha de Seguridad de los datos de Play exigen nombrarlos. Ahora la sección 4 lista **Google Maps Platform, Google Firebase (Cloud Messaging), Meta Platforms (WhatsApp Business Cloud API), Twilio, Wompi (Bancolombia), Cloudflare R2 y Render/Vercel**, uno a uno con para qué se usa cada uno. **(2) Faltaba la transferencia internacional** (artículo 26): todos esos proveedores tienen servidores fuera de Colombia y sin la cláusula la autorización no cubre sacar el dato del país. Sección nueva. La sección 6 dice además CÓMO se borra la cuenta («Eliminar mi cuenta» dentro de la app, más la página pública), no solo que existe el derecho. `legal-privacidad.test.ts` (9 pruebas) vigila las DOS direcciones —que no se retire de la política un proveedor que sigue integrado, y que no se nombre uno que ya no se usa— porque el texto es una cadena dentro de un `.ts` que no compila nadie; lo que NO caza es integrar un proveedor que no esté en su lista, y queda escrito ahí. **(3) `docs/PRIVACIDAD_DATOS.md` mandaba marcar «ubicación en segundo plano»** en la ficha de Play: el manifiesto del conductor NO declara `ACCESS_BACKGROUND_LOCATION` (se quitó a propósito: el foreground service basta) y Play cruza las dos cosas — era un rechazo esperando, con su prueba. **(4) El portal `/empresa` se caía entero en «Equipo y vehículos»** («This page couldn't load»): una sola línea, `d.rating.toFixed(2)`, sobre un campo que la interfaz escrita a mano declaraba `number` mientras `Driver.rating` es `Float?` desde la migración de calificaciones — reventaba con el PRIMER conductor sin votos. `tsc` no podía verlo: `listOperatorDrivers` devuelve Prisma sin DTO, así que el portal se escribió su propio tipo y un tipo que miente es indistinguible de uno correcto. Arreglado a `number | null` con «Nuevo», y **`portal-tipos.test.ts`** compara el `select` del servicio contra las columnas nullable de `schema.prisma` y contra la interfaz del portal. **NO se añadió una regla de texto que prohíba `.rating.toFixed(`**: en `AnalyticsPanel` ese mismo texto está dentro de su guarda y es correcto — una prueba que marca usos buenos acaba desactivada.

También HECHO (Registro del pasajero: la app cliente no preguntaba el nombre y todos se llamaban igual): auditadas las dos apps. **El conductor está sano** (teléfono → OTP → pantalla de registro con nombre, documento, datos bancarios y vehículo; `registerDriver` valida los dos formatos de placa colombianos, normaliza a E.164, el clickwrap gatea el botón y `recordConsent` deja constancia con IP; si se interrumpe, `needsRegistrationKey` devuelve a la pantalla de registro en el siguiente arranque). **El cliente NO TENÍA registro**: se entra con teléfono y código, y `verifyClientOtp` creaba la cuenta con el literal `'Usuario ZIPA'`. Eso no se quedaba en la pantalla de Cuenta — `buildTripRequestDTO` lee `trip.passenger.name`, así que era **el nombre que el conductor veía en la oferta y en «a quién recojo»**, el del manifiesto de un intermunicipal y el que veía el negocio en su pedido: todos los pasajeros de la plataforma se llamaban igual y el taxista que llegaba no podía llamar a nadie. Peor, el literal EN LA COLUMNA destruye la única señal que permite preguntarlo una vez y no volver a molestar: no hay forma de distinguir «no lo ha dicho» de «se llama así». Ahora la columna queda en **NULL** y la señal viaja en `ClientDTO.needsName`/`ClientProfileDTO.needsName`; migración `20260925210000_nombre_real_del_pasajero` **borra el relleno de las cuentas ya creadas** (las que tienen un nombre de verdad no se tocan) y el genérico visible pasa a ser «Pasajero», que es el que ya usaban el despacho y el historial del conductor —dos genéricos distintos harían creer que son dos cosas distintas—. App cliente: **`NombreScreen`** (`/nombre`) como último paso del registro, a la que el OTP y el enlace de WhatsApp saltan solo si falta; **se puede aplazar a propósito** —quedarse sin poder entrar por un fallo de red en la pantalla que sigue al código sería peor que un viaje con el nombre en genérico— y quien la aplaza encuentra la invitación en Cuenta, donde además **el campo de edición nace VACÍO** si no hay nombre: precargar «Pasajero» convertiría un «Guardar» distraído en el nombre de la cuenta. Quien llega desde WhatsApp con su ubicación ya mandada sigue a pedir el viaje al terminar, para no deshacer lo que el botón de ubicación vino a ahorrar. El nombre se guarda también en la sesión local (`guardarNombre`), porque quien restaura la sesión es `getStoredClient()` y sin eso se le volvería a preguntar en cada arranque. Longitud mínima comprobada **también en el servidor** (esta ruta pasó a ser el registro). Verificado: typecheck + **1036 pruebas** (de 1033, con `nombre-pasajero.test.ts` sobre el fuente: ningún archivo escribe el relleno y la creación no toca la columna) y **`e2e/nombre-del-pasajero.ts` contra PostgreSQL real (20 comprobaciones)**, las dos comprobadas ROMPIÉNDOLAS —restaurar el literal tumba 4 del E2E y 2 de las unitarias—. **PENDIENTE de este flujo**: quien entra por WhatsApp sigue sin aceptar términos (`recordConsent` solo se llama en el login por OTP, el registro de conductor y el de empresa), que ya estaba anotado.

También HECHO (La lupa de la barra viaja como cristal líquido — y la premisa había que corregirla): el usuario pidió «que la barra Inicio·Pedidos·Favoritos interactúe moviendo el cristal, con el desplazamiento que tiene Instagram». **Medido antes de tocar: la píldora YA se desplazaba** (`AnimatedAlign`), así que el problema no era que se teletransportara sino CÓMO se movía, y eran tres cosas concretas: (1) **`Curves.easeOutBack` SOBREPASA el destino y vuelve** — con los ítems a un ancho de distancia ese rebote mete la lupa encima del vecino y retrocede, que se lee como un fallo, no como fluidez; (2) **420 ms** es lento para una selección: el dedo ya se levantó y la barra sigue viajando; (3) **el cristal no se deformaba**, que es justo lo que separa un rectángulo que se mueve de algo que parece líquido. Nuevo **`shared/widgets/lupa_vidrio.dart`** (en las DOS apps, porque las dos barras eran la misma implementación copiada): posición interpolada con `easeOutCubic` y duración que crece con la distancia pero no proporcionalmente (220 ms + 45 por columna, tope 380 — el triple de tiempo para tres columnas se arrastra), **estirón en la dirección del viaje** con campana `sin(pi·t)` graduada por la distancia del salto, y aplastado del 45 % del estirón para conservar el volumen aparente, como una gota. **El estirón NO mide la velocidad real a propósito**: derivarlo frame a frame daría lo mismo y dependería del `dt`, que en un teléfono lento salta. **Y la campana vale 1 en los dos extremos, que es lo que impide que la lupa se salga del recorte de la barra**: sobre la primera o la última columna el margen de 6 px no daría para un 22 % más de ancho, pero ahí ya no está estirada — la invariante se sostiene por construcción, no por un `clamp`. El cristal gana además **borde asimétrico** (reflejo arriba, casi nada abajo: un borde uniforme se ve como una línea dibujada, así se lee el grosor) y un **blur propio flojo (4 frente a los 22 de la barra)**: detrás de la lupa el fondo YA viene desenfocado por el padre, así que subirlo no se notaría y solo costaría otro `saveLayer` por fotograma — la lección de los dos `ColorFiltered` anidados del mapa. El ítem activo acompaña con `AnimatedScale` a 1,06 (más alto hace reflowear la etiqueta de diez puntos y la fila entera parece moverse). Las dos reglas que rompen esto **en silencio** —la lupa entre dos ítems, o cortada por el clip— viven sueltas y probadas (`alineacionDeColumna`, `estironDeViaje`; 13 casos en cada app, incluido que en el peor caso —cinco columnas en pantalla de 320— el ancho estirado nunca alcanza el borde). **Guarda de divergencia**: el test compara los dos ficheros módulo el nombre del paquete y **el mismo test corre en las dos apps** (busca la otra copia a los dos lados y normaliza), comprobada rompiéndola; es la lección del formateador de moneda, que acabó en diecisiete copias y dos formatos. **PENDIENTE de QA visual**: la curva, los 380 ms y el 22 % de estirón solo se juzgan en un teléfono. **Hallazgo anotado y NO arreglado a propósito** (ampliar una tanda de animación a otra cosa es justo lo que este archivo dice que no se haga): **la app del conductor tiene 8 emojis renderizados en UI** —`phone_input_screen:315` la bandera `🇨🇴`, `earnings_screen` 824 y 919, `delivery_proof_sheet` 199 y 220, `pickup_proof_sheet:291`, `performance_screen:268`, `trip_history_screen:417`— y ahí NO existe la prueba que los prohíbe, porque el catálogo `ZipaIcon` es solo del cliente; la bandera es exactamente el caso que el cliente ya corrigió (en muchos Android sale como dos letras en un recuadro).

También HECHO (El identificador de las apps pasa a `com.zipa.*` — la última decisión irreversible antes de Play): antes del primer envío a Play, el `applicationId` seguía diciendo `com.nexum.nexum_client` y `com.nexum.driver_app`. Es **lo único de una app que no se puede cambiar nunca** una vez publicada: cambiarlo después crea una ficha nueva con cero instalaciones y cero reseñas, y quien la tuviera deja de recibir actualizaciones. Ahora son **`com.zipa.cliente`** y **`com.zipa.conductor`**, también en el `PRODUCT_BUNDLE_IDENTIFIER` de iOS y en el `userAgentPackageName` con que los mapas se identifican ante los servidores de teselas. **El `namespace` NO cambia, y es deliberado**: es el paquete del código —dónde vive `MainActivity`, de dónde salen `R` y `BuildConfig`— y el manifiesto la declara como `.MainActivity`, relativo a él; moverlo obliga a mover los `.kt` y reescribir sus `package`, y hecho a medias la app COMPILA y revienta al abrirse con `ClassNotFoundException`. No lo ve nadie y no es la identidad en Play. Queda escrito en los dos gradle para que el siguiente no lo tome por un olvido. **CONSECUENCIA INMEDIATA, y no es un defecto nuestro**: el `google-services.json` de Firebase está atado al paquete, así que `tools/preparar-google-services.sh` —que ya comprobaba eso— **para el build** con «El archivo no menciona com.zipa.cliente». Los tres checks obligatorios (typecheck/analyze/tests) siguen verdes y el merge no se bloquea, pero **no habrá APK ni AAB hasta crear las dos apps nuevas en Firebase y renovar `GOOGLE_SERVICES_BASE64` y `GOOGLE_SERVICES_CLIENTE_BASE64`**. Es lo correcto: la alternativa sería un APK que no recibe un solo aviso sin un error por el camino, que es exactamente lo que esa guarda existe para impedir. **El id vivía en DOS sitios que deben decir lo mismo** (el `build.gradle.kts` y el argumento del workflow); `google-services.test.ts` lo **LEE del gradle** en vez de copiarlo —con una tercera copia la prueba habría pasado a afirmar el valor viejo en lugar de vigilarlo— y falla si el workflow se separa, comprobado rompiéndolo. **Dejados a propósito**: `linux/CMakeLists.txt`, `windows/runner/Runner.rc` y el bundle de macOS siguen en `com.nexum` — esas plataformas ni se compilan en CI ni se distribuyen, y tocarlas es diff sin beneficio. La firma NO cambia: la llave es la misma y las huellas siguen siendo las de siempre.

## PENDIENTE (Tanda 35) — punteros exactos

-1. **Del reporte del usuario (requiere SU información):** "las apps no interactúan" → heartbeat verificado SANO en código (LocationService.startTracking cada 4 s al ir online, fallback Pamplona); falta la CAPTURA de la tabla /admin → Conductores → Diagnóstico de despacho para ver qué filtro falla en su entorno. "No permite iniciar viaje publicado" → los botones depart/complete EXISTEN en my_pooled_trips_screen:83-90; falta el error exacto que ve. Cliente intercity → conductor: mismo diagnóstico (requiere conductor ONLINE+intercityEnabled+≤25 km del origen).
0. **Dark mode**: HECHO y verificado (ver la tanda de modo oscuro). Queda solo QA visual en dispositivo. **Activar S3/R2 y FCM en producción = config del usuario siguiendo `docs/ACTIVAR_S3_FCM.md`** (R2 + Firebase + secrets; el código ya está listo y E2E-verificado; `/health` debe mostrar `uploads: s3-r2` y `push: firebase`). Opcional UI: mostrar la prueba de entrega (pickup/delivery photo) al cliente y al negocio en el detalle del pedido/mandado (los DTOs ya exponen `pickupPhotoUrl`/`deliveryPhotoUrl`).

0. **Fase B restante**: B4 opcional "Nexum Pro" tipo DiDi Élite (niveles con datos reales de rating/viajes).

0.4 **Subida a Play Store:** guía completa y verificada en **`docs/PUBLICAR_EN_PLAY.md`** (de dónde sale el AAB, firma, política de privacidad web, eliminación de cuenta, permisos y el orden recomendado). Cambios hechos para ello: `/legal/{privacidad,terminos,eliminar-cuenta}` en el portal (antes solo existía el JSON del backend, que Play no acepta como política publicada); el manifiesto del conductor perdió `ACCESS_BACKGROUND_LOCATION` (innecesario: el foreground service tipo `location` basta desde Android 10, y declararlo obliga al formulario de permisos + video de Play) y **dos componentes fantasma** — `.BootReceiver` y `.LocationForegroundService`, cuyas clases NO existen; el receiver escuchaba BOOT_COMPLETED y MY_PACKAGE_REPLACED, así que reventaba con ClassNotFoundException al encender el teléfono y tras cada actualización desde Play. Pendiente del usuario: `NEXT_PUBLIC_SUPPORT_EMAIL` en el portal, `REVIEW_DEMO_PHONE`+`REVIEW_DEMO_CODE` en Render (cuenta para el revisor), y comprobar el targetSdk que ponga Flutter 3.44.

0.5 **Config manual pendiente AHORA (2026-08-12):** quitar `INTERCITY_SIMULATE` del panel de Render (el blueprint dice "false", así que está puesta a mano); poner `WOMPI_PUBLIC_KEY`+`WOMPI_PRIVATE_KEY` si se quiere cobrar en línea (sin ellas la app oculta el botón sola, que es lo correcto); y **las reservas intermunicipales que ya tienen conductor falso guardado en BD lo seguirán mostrando** — hay que cancelarlas.

1. **Config manual del usuario (bloqueante para probar):** Render nexum-api → Manual Deploy del último main (el /health debe mostrar `commit` nuevo); `OTP_FALLBACK_CODE` = código privado de 6 dígitos (vale para TODAS las superficies en modo local, admin incluido); `ADMIN_PHONES`; nexum-store (portales) → redeploy con Clear build cache. GitHub → secrets `ANDROID_KEYSTORE_BASE64` + `ANDROID_KEYSTORE_PASSWORD` (keystore generado en sesión 2026-07-10, entregado al usuario; alias `nexum`) y relanzar los builds de APK.
   - **Despliegue del portal (DIAGNOSTICADO 2026-07-12):** el portal Next.js (`app/`) está publicado en DOS lados — **Vercel** (proyectos `nexum-store`/`-hdo9`/`-jc9f`, auto-deploy en cada push, son los checks del PR) **y Render** (`nexum-store.onrender.com`, servicio manual NO declarado en render.yaml, **sin auto-deploy** → se queda en build viejo). `PORTAL_BASE_URL` apunta a onrender.com y el usuario abre onrender.com → por eso "sigue siendo demo" pese a que el fix está en main: **Render no se ha reconstruido**. Solución: en Render, servicio `nexum-store` → Manual Deploy (Clear build cache) + activar Auto-Deploy; o migrar el canónico a Vercel y repuntar `PORTAL_BASE_URL`. Diagnóstico en vivo: `GET /api/version` del portal (404 = build viejo; 200 con `businessSelfRegistration:true` = build nuevo).
2. **Activar `INTERCITY_DUAL_MODEL=true` en Render** cuando existan empresas verificadas con rutas autorizadas (env var; no es cambio de código).
3. **`driver_mock.dart` restante:** lo usan `editable_profile_provider` (seed antes del load real) y `auth_mock_datasource` (web/tests). Sustituible por estados vacíos si se quiere cero mocks.
5. **Producción real de OTP:** configurar Twilio Verify (TWILIO_ACCOUNT_SID/AUTH_TOKEN/VERIFY_SID) — al definirlos, TODOS los códigos fijos se apagan solos (SMS reales).

## Regla de oro (eficiencia de tokens)

Antes de explorar: lee este archivo y `docs/ESTRUCTURA_EMPRESAS_FLOTAS.md`. No re-verifiques lo listado en HECHO. Para lo PENDIENTE, ve directo a los archivos citados. Verifica local (backend + web) antes de commitear; Flutter se verifica en CI tras el push. **Al verificar con pipes usa `set -o pipefail`**: un `npm run typecheck | tail` sin pipefail se traga el exit code y deja pasar errores (ya pasó). Una tanda = auditar poco, arreglar, verificar, commit, push, confirmar CI.
