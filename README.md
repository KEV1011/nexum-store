# nexum-store

## Correr el servidor localmente

```bash
cd backend && npm install && npm run dev
# Acceso: http://localhost:3000
# WS: ws://localhost:3000
```

O usando el helper (crea `.env` automáticamente si no existe):

```bash
cd backend && bash start.sh
```

## Con Docker

```bash
docker-compose up
```
