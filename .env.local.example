# ── Portal de negocios (Next.js) ─────────────────────────────────────────────
# URL pública del backend Nexum (API REST + WebSocket).
# En Vercel: Settings → Environment Variables.
NEXT_PUBLIC_BACKEND_URL=https://nexum-api.onrender.com
